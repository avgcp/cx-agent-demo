---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
stopped_at: Quick task 260817-taq COMPLETE and SHIPPED ON VOICE — **voice v20 `d8fe1e86-0af6-4eed-adb4-1588d08cb27f` is LIVE on `d28bbcb0`** (repointed off v19 `e484ce3e` at `2026-08-18T02:47:19.748978Z`, read back not inferred, `channelProfile` sha `e7197eb367fa9cf0` byte-identical across the write, `GOOGLE_TELEPHONY_PLATFORM`), the cut AND the repoint **gated in code on a 130-assertion battery's EXIT STATUS run twice** (pre-cut and again immediately pre-repoint), with an 11-assertion **version-snapshot** check between them and a **14-assertion DEPLOYED battery** after, and the rollback target **re-read live immediately before the PATCH** with the ship script aborting on a moved version OR a moved `updateTime`. **THE PHONE AGENT INVENTED A CUSTOMER'S CLAIM — AND IT HAD DONE IT TWICE BEFORE, UNREPORTED.** The reported call `065rK0K0` (`2026-08-18T01:58:54Z`, AUDIO/LIVE, v19) told Maria Santos her claim `CLM92837` *"was approved and your device was shipped"* with **`lookup_claim` NEVER CALLED**. Scanning EVERY audio conversation on the app found two more on the same build: `0810JFoa` (08-14 12:22) invented `CLM88342` **and a "payment of 825"**, and `103NCt8c` (08-14 17:14) invented a **NOT-FOUND** — *"I can't seem to find any claims for this policy"* — on `PDP100294`, **which holds 27 claims**, then offered to open a duplicate. **Across the four status calls ever made to the phone, `lookup_claim` ran ONCE; all three skips fabricated.** **THE BRIEF'S LEADING HYPOTHESIS IS DISPROVEN BY THE SPAN TREE: this is NOT 8rv's batching defect.** `065rK0K0` turn 2 reads `LLM 0.795s -> Tool(verify_identity) -> LLM 7.599s -> TURN ENDS` — **the second round HAPPENED and ran for 7.6 seconds; the model simply spoke instead of calling the tool.** 8rv's dependency statement would have been a no-op. **MY OWN follow-up hypothesis ALSO died, by evidence: text does NOT terminate an audio turn** — 17 of 166 agent turns across 22 audio conversations emit text and then call a tool in the same turn, v19 included. **THE REAL MECHANISM: 06-04's `SPEAK BEFORE YOU LOOK` instruction (voice only; chat never had it) required the model to compose speech in the very step where it should emit the tool call.** On live audio that acknowledgment is generated as ordinary speech, and *"let me look that up"* is not a socially complete utterance — it demands a result in the same breath, so the model supplied one from nothing and never called the tool. All three failures free-composed *"One moment while I look..."* and ran straight into an invented answer; the ONE success said 06-04's literal example clause and STOPPED. **06-04 recorded that it could not make this gate fire on TEXT/API under three revisions and shipped the ordering as a harmless unfulfilled preference. On the channel it could not test, the instruction fired exactly as written — and firing IS the failure.** **REPRODUCTION, STATED HONESTLY: the defect is AUDIO-ONLY and does not reproduce from a script** — seven clean TEXT/API runs of the exact failing shape. The reproduction is the conversation record, three times, on the channel that broke. **THE FIX IS STRUCTURAL, IN TWO PARTS, AND ONLY THE SECOND IS LOAD-BEARING.** (1) Instruction: `claims_concierge` 11,745 -> **12,699** (+954), **ONE contiguous region** (prefix 8,174 / suffix 2,975, old span 617 / new span 1,571), the `SPEAK BEFORE YOU LOOK` paragraph **REPLACED not appended to** — `lookup_claim` is now the FIRST thing on the turn, a short silence is named as correct, and the agent is told it has **no claim information of its own** until the tool returns (covering the compounding *"It's CLM92837"*). (2) **A STRUCTURAL GUARD in the `afterModelCallbacks[0]` that ALREADY does this job** — its own comment reads *"The concierge has no device information yet and must not invent one"* — 2,092 -> **4,717** (+2,625), a **PURE INSERTION** asserted literally (`new.replace(gate,'',1) == old`), placed **BEFORE** the `auth_status == verified` early return because the fabrication happens AFTER verification. It records every `lookup_claim` call the model emits in session state and **refuses any utterance carrying a claim fact while no such call has been made**, replacing it with the store contract's own `STORE_DOWN` literal. **PROBES ESTABLISHED IT NEEDS NOTHING NEW: `callback_context.state` is a writable dict and an UNDECLARED key PERSISTS ACROSS TURNS** (`PRIOR=None -> 1 -> 2`), so **no new declared variable, no app-config change and no tool patch** — the 33 `variableDeclarations` canary is untouched and `lookup_claim` (which carries the store HMAC key) was never read or patched. `callback_context` also exposes `events`, `get_last_agent_output`, `set_variable`. **PLATFORM FINDING WORTH CARRYING: CALLBACK CODE IS CACHED — a patch is NOT immediately live** (probe 2 returned probe 1's output despite a verified read-back); every later test was spaced 60-100 s. **STATED BOUND: the gate covers the tool-never-called case (all three observed fabrications), not a fabrication invented AFTER a genuine response** — an `afterModelCallback` sees only model output; asserted explicitly in the unit battery rather than left to be discovered. **A REGRESSION I CAUSED WAS CAUGHT BY A LIVE CONTROL AND FIXED BEFORE SHIPPING.** Pass 1 removed the old paragraph's closing *"the tool's line THIRD"*, which had been carrying the verbatim expectation, and the agent began **speech-shaping** the tool string (*"C L M two four five nine nine ... 560 dollars ... Dell X P S fourteen"*). Controls: **pre-edit v19 byte-identical x2, pass-1 shaped x2, pass-2 byte-identical x2, shipped v20 byte-identical x2**. Pass 2 adds a fixed-order/no-spelling-out paragraph and **was rebuilt from the PRISTINE baseline**, so "exactly one contiguous region differs from the baseline" is true of the SHIPPED instruction, not merely of each increment. **VERIFICATION, all cited to CONVERSATION-RECORD `toolCall`/`toolResponse` objects:** `lookup_claim` fired in **8 of 8** status conversations; **every reference/amount/date spoken appears in a `lookup_claim` response** (unsourced set empty every time); **`PDP100583` surfaces the TRUE claim `CLM-31018263`** — the very one the phone invented over — with the confirm path routing through `claim_ref` (no `selector`); `PDP100017` clean single match `CLM-24599` x4; not-found returns **exactly the five contract keys** with zero `$` spoken; **`lookup_claim` now precedes ALL agent text in its turn (no preamble)**; a failed identity produced zero claim references; and **five deliberate attempts to make the agent state a claim fact without a lookup were all refused by the model itself**. **The guard's own 22/22 battery runs against the EXACT SHIPPED BYTES and refuses all three real fabricated utterances verbatim**, plus the spoken-digit forms — while passing every tool-sourced line and preserving all three pre-existing guards. **HONEST GAP: the guard has never been seen FIRING live, because the fixed build always calls the tool first.** **CHAT WAS CHECKED AND IS NOT EXPOSED, SO IT WAS NOT TOUCHED** — three grounds: the `SPEAK BEFORE YOU LOOK` clause is absent from chat entirely; chat is `WEB_UI` with no audio conversation in its history; and **all 92 chat conversations since `lookup_claim` was wired were scanned — every status conversation called the tool and ZERO references were unsourced**. Consequence recorded rather than hidden: **the two apps' `afterModelCallbacks` are no longer byte-identical** (was 2,092/sha `402a1619` on both; voice is now 4,717) — a deliberate enumerated divergence, and **chat should adopt the identical guard in its own gated task**. **CANARIES ALL GREEN:** FNOL end to end still works; tariff `420/25/750` `DL-1` and `3000/25/1500` `DL-3`+`DL-2` `total_loss_flag`; the spoken decision **byte-identical to `resolve_claim.explanation` and emitted exactly once** on both branches (TEXT/API); **the send-away line still SPOKEN**; cross-sell fires; the assessor packet files with six sections each exactly once, zero braces, `[ASSESSOR] [VOICE]` at **200**; `escalate_to_human` once; the **869-char greeting callback byte-identical** with all three branches present in the draft AND the version snapshot (near-free `{"event": {"event": "session start"}}` probe returned the exact opener); `GTP_SURFACE`/`bargeInAwareness`/`languageSettings`/`globalInstruction` byte-identical; **Spanish still survives the `claims_concierge` -> `claim_intake` handoff**; `record_claim` still unwired and **the store is byte-for-byte unchanged** (3/27/1, 31 `by-ref`); `claim_intake` 20,711 and `case_summary` 6,447 byte-identical; **all 9 voice tools byte-unchanged, `resolve_claim` never read**. **DEVIATIONS:** the callback band was **re-declared before running** (1,700-2,600 -> 1,700-2,900; the provenance comment took it to 2,625) and recorded rather than silently widened; **four of my own gate assertions were WRONG and stopped the ship** — the `runSession` envelope repeats chunks and echoes user text, so they were fixed by **re-basing the behavioural half on the canonical conversation record** (the stricter source), not by loosening them; 19 conversations including **3 deliberate live controls** against the pre-edit deployment and one lost to my own fixture error (`PDP100017` is **Alex Chen**, not Jordan Rivera) which became a useful negative control; **no 429 and no 529**, no retry loop. **⚠ THE APP LIST IS SIX, NOT FIVE, AND NOT BECAUSE OF ME: a sixth app `902be2ac` "demo" was created today at `2026-08-17T19:43:53Z`, not by me. I created no probe app** (the three callback probes ran on the voice DRAFT and were each restored byte-identically) **and I did not delete an app I did not create — please confirm `902be2ac` is yours.** **📞 THE PHONE CHECK IS THE ONLY THING THAT CAN CLOSE THIS: dial the number, ask for a claim status as Maria Santos / `PDP100583`, and you must hear `CLM-31018263` or the graceful not-found. Any 5-digit unhyphenated reference means the fix did not hold.** Also note the pause before the answer is now 2.9-3.8 s by design — the acknowledgment that used to cover it is what invented the claims; if it reads badly the fix is a prefix message in a CALLBACK, never a prompt. Chat `d7bfbb93` still serves v20 `8a95ab02` with `updateTime` unmoved and received **zero definition writes**. Fork `9ae7a0c3` zero calls, no probe app, no git commit. Previously: Quick task 260814-8rv COMPLETE and SHIPPED ON CHAT — **chat v20 `8a95ab02-e653-4d1e-baee-eb500c52710c` is LIVE on `d7bfbb93`** (repointed off v19 `619e13a1` at `2026-08-14T11:40:17.051321Z`, read back not inferred, `channelProfile` sha `76f98fed6482c930` byte-identical across the write, `WEB_UI`), the cut AND the repoint **gated in code on a 116-assertion battery's EXIT STATUS run twice** (before the cut and again immediately before the repoint), the rollback target **re-read live immediately before the PATCH** with the ship script aborting on a moved version OR a moved `updateTime`, and a further **44-assertion DEPLOYED battery** re-proving everything against `d7bfbb93` itself. **TWO DEFECTS FIXED. DEFECT ONE — THE MISSING "YES" BRANCH, NOW CLOSED ON CHAT.** 06-03's chat region said only *"call `lookup_claim` again with `selector` set to what they said"*; `selector` resolution is ordinal -> reference -> date -> device words and **none of the four has a branch for a bare agreement**, so *"yes"* fell through to `NO_MATCH` and the agent said *"there is nothing for me to read back"* **immediately after reading the customer's claim back** — Beat 2 of the demo script. **Fixed by mirroring 06-04's voice design, not by inventing a second one: an affirmative is routed through `claim_ref` taken from the disambiguation line the tool itself supplied.** Edit: `claims_concierge` 9,591 -> **10,475** (+884, band +700..+1,300 declared before running), **ONE contiguous region** (prefix 7,917 / old span 148 / new span 1,032), anchor sliced from chat's own CRLF bytes and asserted unique, region read **from a file**, byte-for-byte read-back (sha `1737036c…`), **`beforeModelCallbacks` SURVIVED byte-identical at 869 chars with all three branches**. **CHAT AND VOICE NOW BEHAVE IDENTICALLY ON CONFIRMATION**, with three enumerated deliberate prose differences (register; chat lists more affirmatives plus the catch-all *"or any other plain agreement"*; and chat carries an explicit negative voice lacks — *"a bare agreement is NOT a selector"* — which 06-05 may want to carry back). **PROVEN ON THREE DIFFERENT AFFIRMATIVES ACROSS TWO BUILDS**: bare *"yes"* -> `lookup_claim{claim_ref: "CLM-24690"}` `found: true` `match_count: 1` with NO selector passed (draft); *"that's the one"* (draft); ***"correct"* on the DEPLOYMENT**. The negative still works — *"no, the older one"* routes through `selector` (not `claim_ref`) to a DIFFERENT claim, `CLM-60202`, with **zero of the alternatives read out**; an unknown reference still returns `found: false` with **exactly the five contract keys**, zero claim fields and zero `$` in the turn. **DEFECT TWO, FOLDED IN MID-TASK — THE CROSS-SELL WIDGET WAS EATING THE `[RECORD]` EMAIL.** A real user filed a claim and got no record email. **THE COORDINATOR'S PROPOSED FIX WOULD HAVE BEEN A NO-OP**: the instruction ALREADY ordered `generate_case_summary` -> `send_case_record_email` -> `cover_offer_actions`, in that document order. **The defect was REPRODUCED FIRST on a byte-identical `claim_intake` (conversation D2) rather than reasoned about**, which is what caught it: `send_claim_email` + `generate_case_summary` + `cover_offer_actions` were issued in ONE batch and the turn ended before the mailer could run. **THE MECHANISM IS 06-03's FINDING TAKEN ONE STEP FURTHER, AND IT GENERALISES: `send_case_record_email` takes the composer's RESPONSE as `packet_text`, so it needs a SECOND ROUND OF TOOL CALLS INSIDE THE TURN — and a turn-ending widget issued in the first batch throws that round away. A two-round dependency and a widget cannot share a batch, and the model batches by default even with `toolExecutionMode: SEQUENTIAL`.** `record_claim` -> `claim_decision_card` survives only because nothing follows it. **Fixed by STATING THE DEPENDENCY, not the order**: two anchored in-place amendments at the OPENING of each step's `<action>` (06-03's rule that the opening lines decide the turn's tools; o5l's that appending after a closing rule is how v11 silencing was created) — `claim_intake` 32,498 -> **34,311** (+1,813, band +900..+2,200 declared before running), **exactly TWO changed spans, both pure insertions (1,228 and 585), NO original byte deleted or moved**, each anchor preserved verbatim as its replacement's prefix, byte-identical read-back. **BOTH TOOLS NOW FIRE IN THE SAME TURN — THE TURN DID NOT NEED SPLITTING.** Deployed run P2 on v20: `send_case_record_email` `sent: true`, **HTTP 200**, **`attached: TRUE`**, `photo_b64_len: 208`, subject **`[RECORD] [CHAT] CLM-30707672 - Jordan Rivera - APPROVED`**, followed by `cover_offer_actions` with `Add it`/`Not now` verbatim as the turn's LAST call. **THIS ALSO SETTLES 80f's CROSS-SELL CANARY: the cross-sell was never independently broken.** The two tools were competing for the last slot of one turn and whichever the model batched first ended it — 80f watched the mailer win, the user watched the widget win. Same defect from two ends. With the dependency stated, **both fire on every run of the fixed build** (draft D3, deployed P2) and the bogus `end_session {"reason":"escalation after errors"}` was seen on **none of the nine conversations**. **CANARIES: draft 116/116, deployed 44/44, none skipped and none held only "in substance"** — deterministic tariff `420/25/1500` `DL-1` and `3000/25/1500` `DL-3`+`DL-2` `total_loss_flag: true`; the spoken decision **byte-identical to `resolve_claim.explanation` and emitted exactly once** on both branches, draft and deployed; **the send-away line SPOKEN on every run and emitted BEFORE the first tool call of its turn**; `record_claim` `recorded: true` with **`store_calls: 4`** and `precheck_status: 404`, still ordered BEFORE `claim_decision_card`; `lookup_claim` still reads by policy (`policy_source: "session"`); the decision card fired with `units` an int; `capture_claim_photo captured: true`; `[ASSESSOR] [CHAT]` also at 200 with `attached: true`; six packet headings each once with **zero `{placeholder}` braces on all four shipped-build packets**; cross-sell correctly ABSENT on escalation; the **869-char `beforeModelCallbacks` asserted present in the VERSION SNAPSHOT before the repoint**; the greeting still opens with *"How can I help you today?"* (near-free `{"event": {"event": "session start"}}` probe); `case_summary` byte-unchanged and **all 13 chat tools byte-unchanged including `resolve_claim`, which was never read**. **METHOD WORTH CARRYING: the photo path was proven BY CONVERSATION, not by `executeTool`** — `SessionInput.image` (`{"image": {"data": "<b64>", "mimeType": "image/png"}}`) pushes an image into a live conversation over the API exactly as the widget does; o5l found this and no later plan had used it. **STORE LEFT CLEAN: all five claims filed by this task were removed** (`by-ref` objects deleted and `by-policy/PDP100294.json` rewritten with 06-01's serialisation), re-audited to **30 objects / 27 entries / 27 `by-ref` / 0-0-0 collisions / `PDP100294` back at 25** — byte-for-byte 80f's closing state, no fixture touched, no HMAC key handled. 9 conversations (one a deliberate live REPRODUCTION that stopped a no-op shipping, two on the deployment), **no 429 and no 529**, no retry loop. **Voice `6e01e4a5` received ZERO non-GET calls; `d28bbcb0` still serves v19 `e484ce3e` with `updateTime` unmoved.** Fork `9ae7a0c3` zero calls, app list five, no probe app, no git commit. **⚠ STILL OPEN: `PDP100294` holds 25 claims so Beat 2 opens with *"I can see 25 claims on this policy"* (third task in a row to flag it); `generate_case_summary` leaked a literal `{dx_answers}` into ONE packet on the control run (pre-existing, did not recur on any shipped-build packet, but it would reach an assessor's inbox); and NOBODY HAS WATCHED v20 IN A BROWSER.** Previously: Quick task 260814-80f COMPLETE and SHIPPED ON BOTH APPS — **chat v19 `619e13a1-2a31-4627-aca3-2ff3a32e336b` is LIVE on `d7bfbb93`** (repointed off v18 `d0e4bfef` at `2026-08-14T11:10:30.653633Z`, `channelProfile` sha `76f98fed6482c930` byte-identical across the write, `WEB_UI`) and **voice v19 `e484ce3e-00d6-4fae-b180-d789645f7280` is LIVE on `d28bbcb0`** (repointed off v18 `a6f6b620` at `2026-08-14T11:10:40.558076Z`, `channelProfile` sha `e7197eb367fa9cf0` byte-identical, `GOOGLE_TELEPHONY_PLATFORM`), both read back not inferred, both cuts and both repoints **gated in code on a 121-assertion battery's EXIT STATUS**, both rollback targets **re-read live immediately before their PATCH** with the ship script aborting if the serving version had moved. **CLAIM REFERENCES WERE COLLIDING AND THE GENERATOR WAS NEVER RANDOM.** `resolve_claim` on both apps minted `CLM-{24000 + (int(time.time()) %% 900) + seq}` — **a DETERMINISTIC 900-SECOND WHEEL that repeats every fifteen minutes**, not a small random range. Established without ever reading, echoing or persisting `resolve_claim` (it holds the live Resend key): boolean/shape probes proved `random`/`randint`/`uuid`/`secrets`/`hashlib` all ABSENT and `time.time` present once, digit-only regex captures recovered the literals, and an identifier-whitelisted sanitiser with every string literal masked recovered the three lines. **The formula was then verified against the store 22/22** — for every generated reference, `ref − 24000 − seq` equals `t_resolve %% 900` with a 1–5 s `record_claim` lag, no outliers. The collision is exact, not lucky: the MacBook claim on `PDP100294` (2026-08-13T12:25:04Z) and the Dell claim on `PDP100017` (2026-08-14T10:40:00Z) both resolved at `t%%900 = 598`, 22 h 15 min apart — a whole multiple of the wheel. **AUDIT: EXACTLY ONE COLLISION IN THE WHOLE STORE**, checked in three classes (same ref under >1 policy = **1**; same ref twice within one policy = 0; `by-ref` disagreeing with or orphaned from its `by-policy` entry = 1, the same one). **FIX 1 — the wheel was widened**: three digit/letter-only anchored edits per app, **+11 chars each**, `%% 900` → `%% 10000000` and `CLM-{24000 + …}` → `CLM-{24000000 + …}`, taking the space from **900 to 10,000,000** and the repeat period from **15 minutes to 115.7 days**, with the format now **always 8 digits** (`CLM-30705868`, range 24,000,001–34,000,000). Chosen over the raw epoch (10 digits is unreadable on a card, in a subject line and out loud), over a policy-derived component (fixes cross-policy collision but leaves same-policy collision, which destroys a claim just as badly), and over relying on the refusal alone (a refusal is a degraded claim — the customer has already been told the reference). **FIX 2 — `record_claim` NOW REFUSES A CROSS-POLICY OVERWRITE ON BOTH APPS**: a 1,718-char block does one `GET` of `by-ref/{KEY}` **before any write** — `404` proceed, `200` same policy falls through to `duplicate_replaced`, **`200` DIFFERENT policy REFUSES with `recorded: false`, `ref_collision: true`, `status_code: 409`, `error_stage: "collision"`, `store_calls: 1` and NEITHER PUT ISSUED**. Proven by invocation 17/17 per app on a throwaway policy AND against the real key: *"claim_ref CLM24599 already belongs to policy PDP100017 - refusing to overwrite it"*. **Two new return fields (`precheck_status`, `ref_collision`) and `store_calls` is now 4 on a full write, not 3 — 06-05 MUST NOT ASSERT `store_calls == 3`.** Both key-bearing tools patched with the 05-04 single-process technique: anchor count asserted ==1, `compile()` checked, exactly-one-module-`def` asserted, **a secret census (counts of `re_…`, `GOOG1…`, 38–44-char base64) asserted UNCHANGED across every edit by counting and never printing**, declared delta matched, read-back byte-identical to the intended string. Old shas matched the recorded baselines (`9eb78770`/`7c55cf0a` from 06-02, `890678c4` from 260812-trc). **`pythonFunction.description` byte-unchanged on all four tools; NO AGENT INSTRUCTION CHANGED BY A BYTE ON EITHER APP.** **REPAIR — STATED, NOT SILENT: the Dell claim on `PDP100017` KEEPS `CLM-24599`** (it is named live demo data, and it is the claim `by-ref` already correctly described) **and the MacBook claim on `PDP100294` was RE-ISSUED as `CLM-30623899`**, minted by the new scheme from its own resolve time so it is reproducible, carrying **`superseded_ref: "CLM-24599"` and a `repair_note`** so the loss is visible in the object. Re-issue was chosen over deletion (the claim is not at fault, only its reference) and over making the key unresolvable (that would sacrifice a live demo claim to protect a test one). **Two claims were issued one reference, so one had to lose it; the choice and its grounds are recorded rather than picked silently.** Verified on both apps: `CLM-24690`→PDP100294 found, `CLM-24599`→PDP100017 found (Dell), `CLM-30623899`→PDP100294 found, and `CLM-24599`→PDP100294 returns the graceful NOT-FOUND literal rather than confidently describing someone else's Dell. **STORE AT CLOSE: 30 objects, 27 entries, 27 `by-ref`, 0/0/0 collisions, `PDP100294` still 25 claims — every probe, canary and conversation write made by this task was removed, no debris.** **CANARIES 121/121** structural + invocation, plus five conversations: deterministic tariff 420/25/1500 `DL-1` and 3,000/25/1500 `DL-3`+`DL-2` `total_loss_flag` on both apps; **the spoken decision byte-identical to `resolve_claim.explanation` and emitted exactly once on all four conversations** (explanation 199→202 / 240→243, the three characters the reference grew); **the send-away line SPOKEN on every run, both branches, both channels**; `record_claim recorded: true` live on the decision turn with `store_calls: 4`, `precheck_status: 404`, still ordered BEFORE `claim_decision_card`; `lookup_claim` still reads by policy and still ignores a model-supplied `policy_id` (`policy_source: "session"`, `policy_id_arg_ignored: true`); packets file at `[RECORD] [CHAT] CLM-30705868 - Jordan Rivera - APPROVED` and `[ASSESSOR] [VOICE] CLM-30705723 - Jordan Rivera`, both 200, zero `{placeholder}` braces; the decision card fired with `units` an int and `capture_claim_photo` fired on the deployed run; voice `GTP_SURFACE`/33 `variableDeclarations`/2,859-char `globalInstruction` intact and **`claims_concierge`'s 869-char `beforeModelCallbacks` asserted present in each cut version's SNAPSHOT before the repoint**; all 11 other chat tools and 7 other voice tools byte-unchanged; port parity re-asserted (`lookup_claim` still raw-identical, `record_claim` differing only in the single enumerated `channel` line). **ONE CANARY DID NOT FIRE AND WAS PROVEN PRE-EXISTING BY A LIVE CONTROL, NOT BY REASONING: the cross-sell.** The identical three-turn script run against **live chat v18 `d0e4bfef`** — the byte-for-byte pre-change build — produced `cover_offer_actions` **0 calls** and the same `end_session {"reason":"escalation after errors"}` closure; a second draft run on an expanded five-turn opener did the same. **Three runs across two builds agree, `claim_intake` is byte-identical at 32,498 and `cover_offer_actions` is byte-unchanged, so the edit cannot be the cause.** Recorded as pre-existing, not claimed as a pass; the repoint was not blocked on it. **A DEPLOYED end-to-end run on chat v19 AFTER the repoint confirms it in production: `CLM-30705868`, 8 digits, `store_calls: 4`, `precheck_status: 404`, decision byte-identical, `[RECORD]` at 200.** 5 conversations (one a deliberate LIVE control, one deployed), 13 Resend sends, **no 429 and no 529**, no retry loop. Fork `9ae7a0c3` zero calls, app list five, no probe app, no git commit. **NOBODY HAS WATCHED v19 IN A BROWSER OR DIALLED THE PHONE.** Previously: Plan 06-04 COMPLETE and SHIPPED — **voice v18 `a6f6b620-af15-4e43-b0d8-bfbbb2d64a46` is LIVE on `d28bbcb0`** (repointed off v17 `dcc20863` at `2026-08-14T03:50:48.984372Z`, read back not inferred, `channelProfile` sha `e7197eb367fa9cf0` byte-identical across the write, `GOOGLE_TELEPHONY_PLATFORM`), the repoint **gated in code on a 47-assertion battery's EXIT STATUS**, and the rollback target **re-read live immediately before the PATCH and asserted unmoved**. **THE PHONE CAN NOW READ A CLAIM BACK THAT WAS FILED ON CHAT.** Session `v04-status-c`: *"Hi, this is Jordan Rivera, policy P D P one zero zero two nine four, and I'm just checking on a claim I put in"* -> `verify_identity` -> `lookup_claim` -> 25 matches -> the agent reads the most recent back and asks -> *"Yes, that's the one"* -> **`CLM-24690`, `channel: CHAT`, spoken byte-identically to `lookup_claim.status_line`, alone in its chunk**: *"Claim CLM-24690 on policy PDP100294: your Apple MacBook Pro 16", keyboard replacement. It was approved for $420.00, less your $25.00 excess, so $395.00 comes to you. Filed on 14 August 2026 **via chat**."* **That is a real cross-channel proof, not a 06-02 fixture** — the fixtures were available and were not needed. **CRITERION 4 HOLDS: the caller is NEVER asked to read a claim reference aloud** (zero request-shaped hits across every agent chunk in three status conversations); `verify_identity` fired **strictly before** `lookup_claim`; **T-06-03 survived wiring and was proven live** — the model DID pass `policy_id: "PDP100294"` and the tool ignored it (`policy_source: "session"`, `policy_id_arg_ignored: true`); several claims disambiguate by reading the most recent one back with **zero of the 24 `alternatives` spoken**; an ambiguous opener (*"I'm calling about my claim"*) gets **exactly one** clarifying question and no tool call; a new-loss opener still reaches `claim_intake` with `lookup_claim` firing **zero** times. **GRACEFUL FAILURE: a cross-policy reference and an unknown one return BYTE-IDENTICAL responses** (five keys, zero claim fields, zero currency and zero `CLM-` in any agent chunk); the empty-policy line is proven by `executeTool` and stated as such because no synthetic identity exists for `PDP-100746`. **A caller who FAILS identity reaches nothing** — an unplanned control (`PDP100583` is Maria Santos, not Jordan Rivera) proved `lookup_claim` is never called across four turns, two of which volunteered references. **[Rule 2] A GAP IN 06-03's CHAT REGION WAS FOUND AND FIXED HERE BEFORE ANY CONVERSATION WAS SPENT: `selector` has no branch for "yes"**, so confirming the disambiguation would have returned `NO_MATCH` — telling a caller there is nothing on file immediately after reading their claim back. Voice routes the confirmation through **the reference the tool itself supplied** as `claim_ref`. **THIS DEFECT IS STILL LIVE ON CHAT v18 `d0e4bfef` — 06-05 must carry the fix across.** **`ASYNCHRONOUS` was re-opened as 06-03 asked and REJECTED, with a sharper reason: the status line IS the tool's result, so async buys no latency and only removes the `toolResponse` twelve assertions read from.** `lookup_claim` measures **0.66-1.24 s**; the status turn **3.84 s**, the confirm turn **2.87 s** — the fastest substantive turns on the app, against 05-07's 9.53 s escalation turn. **PLATFORM FINDING, THIRD OF ITS FAMILY: the text-before-tool-call gate is NOT ACHIEVABLE BY PROMPTING** — a model cannot compose speech before the value that speech must contain has arrived, so a lookup turn is necessarily `call -> response -> text`. Three successive instruction revisions could not move it. The acknowledgment IS spoken and is the first thing heard (*"Thanks, Jordan. Of course, let me see where that has got to…"*), which is what matters on an audio channel, but it does not cover the pause. **The repoint was not blocked on that literal gate; the judgement and the measurement are both recorded, and the fix if the ear disagrees is a prefix message in a CALLBACK, not a stronger prompt.** Edit: `claims_concierge` 6,642 -> **11,745** (+5,103), **one contiguous region** (prefix 6,072 / old span 0 / new span 5,103), anchor sliced from voice's own CRLF bytes and asserted unique, region read **from a file**, byte-for-byte read-back, and the **version snapshot re-verified equal to the intended string BEFORE the repoint**; `beforeModelCallbacks` **survived byte-identical** (869 chars). **Canaries 47/47**, every one cited to a `toolCall`/`toolResponse`: tariff 840/25/1500 `DL-1` and 3,000/25/1500 `DL-3`+`DL-2`, `DECISION_SPEECH_EN` byte-identical at 197 chars exactly once, one customer email via `resolve_claim` on both branches, **the send-away line SPOKEN on both branches**, cross-sell fired, packet with six headings and zero braces at `[ASSESSOR] [VOICE]` 200, zero packet and zero store vocabulary anywhere, `claim_intake` byte-identical at 20,711, all 9 voice tools byte-unchanged including `resolve_claim` (never read), `GTP_SURFACE`/`languageSettings`/`globalInstruction` byte-unchanged, 33 `variableDeclarations`, `record_claim` still unwired. **TWO CANARIES HELD IN SUBSTANCE BUT NOT LITERALLY AND ARE NAMED AS SUCH**: the send-away chunk followed three non-closing tools before preceding `escalate_to_human`/`end_session`, and `generate_case_summary` returned `null` so `packet_text` byte-identity was not provable on this run. **A PRE-EXISTING DEFECT WAS SURFACED AND PROVEN PRE-EXISTING BY A LIVE CONTROL, NOT BY REASONING: the liquid-damage escalation STALLS on voice** — the model passes `run_diagnostic` its answers wrapped in literal quote marks (`q1: "\"no_power\""`), the tool correctly refuses, and the agent re-asks forever. The identical script against live v17 `dcc20863` reproduced it exactly. **Runbook fixed: script the escalated PHONE beat as a DROP, not a spill.** **TWO DEMO-QUALITY ITEMS FOR THE USER: (1) 👂 the relayed sentence contains a literal inch mark `Apple MacBook Pro 16"` which the agent is forbidden to reword — if TTS says *"sixteen quote"* the fix is pre-specified (speech-shaped template inside `lookup_claim`'s Python, both copies, parity re-asserted); the speech-shaping fallback deliberately DID NOT FIRE because the mishandling is unobservable without an ear and no tool was patched. (2) ⚠ `PDP100294` now holds 25 test claims, so the agent says *"I can see 25 claims on this policy"* — prune the store or run the beat on Maria Santos / `PDP100583`.** **THE VOICE WRITE IS STILL OUT OF SCOPE — `record_claim` is attached to no agent; 06-05 owns it, so the loop closes one way only (chat writes, voice reads).** 7 conversations (one a deliberate LIVE control, one lost to my own fixture error which became the best control of the plan), **no 429 and no 529**, no retry loop. Chat `d7bfbb93` still serves v18 `d0e4bfef` with `updateTime` unmoved and every chat agent and tool byte-unchanged — **zero non-GET calls to chat**. Fork `9ae7a0c3` zero calls, app list five, no probe app, no git commit. **status: awaiting-human-phone-check — criterion 2 is proven server-side on the TEXT/API channel and is UNCONFIRMED BY EAR. The four-call script is `06-04-VOICE-EVIDENCE.md ## PHONE_SCRIPT`.** Previously: Quick task 260813-ui0 COMPLETE and SHIPPED ON BOTH APPS — **chat v18 `d0e4bfef-6d5f-43b3-b490-3d9036d030e2` is LIVE on `d7bfbb93`** (repointed off v17 `64be15eb` at `2026-08-14T03:15:20.124453Z`) and **voice v17 `dcc20863-3746-4e43-a2c9-ed30e0611479` is LIVE on `d28bbcb0`** (repointed off v16 `09a1f14d` at `2026-08-14T03:15:07.395898Z`), both read back not inferred, both `channelProfile` sha byte-identical across the write (`e6977330c6e3294b` WEB_UI / `7d89c16219fd2322` GOOGLE_TELEPHONY_PLATFORM), both repoints **gated in code on a 93-assertion battery's EXIT STATUS**. **260813-tgq's instruction change was being SILENTLY OVERRIDDEN ON BOTH CHANNELS.** `claims_concierge` carried a byte-identical 915-char `beforeModelCallbacks[0]` that hard-returned the opening greeting as a literal `LlmResponse` on `<event>session start</event>`; returning an `LlmResponse` short-circuits the model, so the instruction never ran on turn one and the live phone call kept opening with *"To pull up your policy, could I take your full name and your policy ID?"*. **CHAT HAD THE IDENTICAL DEFECT — same 915 bytes, same sha `10cf58b1…` — and it was WORSE than voice: proven on live chat v17 through `config.deployment`, the widget opened with the identity demand AND THEN DEMANDED IDENTITY A SECOND TIME on turn 1**, the exact double-ask tgq existed to remove. FIXED by one anchored `str.replace()` inside the callback on each app, 915 → **869** (−46, one contiguous region, prefix 451 / old span 70 / new span 24, 20 CRLF and 0 bare LF unchanged on BOTH apps — the "chat is CRLF, voice is LF" rule does not hold for callback source). **The new opener, byte-identical on both apps: `Hello, you're connected to Meridian Device Protection. I'm Alex. How can I help you today?`** — the third sentence is `claims_concierge`'s own instruction text verbatim; the first two sentences are byte-untouched. **THE OTHER TWO BRANCHES SURVIVE, proven structurally AND functionally on the newly-serving versions**: `sys.remote-call-disconnected` → `Part.from_end_session(reason='caller disconnected')` (the branch that closed the 11-second call) and `no user activity detected` → *"Are you still there?"*, both byte-identical PRE vs POST. **THE BRIEF'S VERIFICATION PREMISE WAS WRONG AND THIS IS THE MOST REUSABLE PART: `runSession` DOES deliver session start** — the `event` arm of `SessionInput` is a message whose own field is also `event`, so **`{"event": {"event": "session start"}}`** reproduces the phone's turn 0 from a script; tgq's probes missed the bug because they opened with `{"text": "Hi there."}`, an ordinary user turn the branch never matches. Beware `{"event": {"name": "session start"}}` — it returns 200 and a plausible greeting **from the wrong code path**. So the fix is verified PRE/POST on the draft, in each cut version's SNAPSHOT (`snapshot.agents[0].beforeModelCallbacks[0].pythonCode`, 869 chars, sha `42a4fe1e…`, new literal present, old absent, all three branches present, instruction byte-unchanged), and again through `config.deployment` on the deployed builds — **only TTS delivery is unheard. THE USER MUST DIAL: the first sentence must be the new one; then hang up mid-call (clean close) and stay silent (*"Are you still there?"*). Chat is a ten-second browser check — read the widget's first message before typing.** **Canaries 93/93**, including `DECISION_SPEECH_EN` byte-identical at 197 chars and emitted exactly once, escalated 3,000/25/1,500 with `DL-3`+`DL-2`, `[ASSESSOR] [VOICE]` and `[RECORD] [CHAT] … - APPROVED` both at 200 with the photo attached, six packet headings with zero placeholder braces, the send-away line SPOKEN (before all five tool calls on the escalated turn), cross-sell fired with `Add it`/`Not now` verbatim, decision card + `record_claim recorded: true` ordered before the card, `capture_claim_photo captured: true`, `GTP_SURFACE`/`bargeInAwareness`/`languageSettings` byte-identical, `record_claim` still unwired on voice, all 13 chat and 9 voice tools byte-unchanged including `resolve_claim` (never read, echoed or patched), and **`claims_concierge`'s INSTRUCTION byte-unchanged on both apps — the callback was edited, not the instruction.** **One apparent regression — the bogus `end_session {"reason":"escalation after errors"}` killing the chat cross-sell — was cleared AGAIN by the live control plus a second draft run**; three near-identical runs produced three different turn alignments and every canary is green on at least one. **RECORDED, NOT A REGRESSION AND UNFIXABLE BY PROMPTING: the session-start greeting is ALWAYS ENGLISH**, because the model never runs on turn 0 — a Spanish caller hears the English opener then the agent switches (verified end to end, handoff included). Also new: `[RECORD] [CHAT]` files on the **send-away turn**, so a photo arriving on that turn consumes it and the record email is skipped — attach the photo when asked. 9 draft conversations, 1 live control, 12 near-free event probes, 6 Resend sends, **no 429 and no 529**. App list five, fork `9ae7a0c3` zero calls, no git commit. Previously: Quick task 260813-tgq COMPLETE and SHIPPED ON BOTH APPS — **chat v17 `64be15eb-947f-4f36-8af2-2aefd225742b` is LIVE on `d7bfbb93`** (repointed off v16 `be4c83bb` at `2026-08-14T02:35:48.599636Z`) and **voice v16 `09a1f14d-be24-40ba-abbd-a06e495f5d0d` is LIVE on `d28bbcb0`** (repointed off v15 `17b2e438` at `2026-08-14T02:35:55.602430Z`), both read back not inferred, both `channelProfile` sha byte-identical across the write (`e6977330c6e3294b` WEB_UI / `7d89c16219fd2322` GOOGLE_TELEPHONY_PLATFORM), both repoints **gated in code** on the same 86-assertion battery re-run inside the ship script. **NEITHER APP OPENS BY DEMANDING IDENTITY ANY MORE** — both now greet, name the carrier, and ask *"How can I help you today?"*, and ask for full name and policy ID only in the reply to the customer's stated intent. **Rollback: chat v16 `be4c83bb-825a-4feb-af88-379113543aa7` (`updateTime 2026-08-14T02:03:42.286752Z`), voice v15 `17b2e438-b132-49f1-8b32-190b132225ae` (`updateTime 2026-08-13T23:42:50.390492Z`), both re-read live immediately before their PATCH.** **THE GREETING WAS NEVER A LITERAL** — it was emergent from `claims_concierge`'s `<role>` plus one line inside `<step name="Collect details">`, so the fix is a single in-place replacement of that step's opening prose, **not** a new step and **not** an appended paragraph; the step's last paragraph (the policy-ID digit normalisation) is untouched, so "last paragraph wins" is preserved. Identical +1,516 delta on both apps (chat `claims_concierge` 8,075 -> **9,591**, voice 5,126 -> **6,642**), one contiguous region each (prefix 2,658 / old span 224 / new span 1,740), anchors derived from each app's own CRLF bytes, replacement read **from a file**, byte-for-byte read-back, version snapshots equal to the intended string. **ALL FOUR PROBES PASS ON BOTH APPS**, including probe 3 — a customer who volunteers name, policy and loss in one opening message is verified and transferred **on that same turn with zero re-asks** — and probe 4, a Spanish opener still greeted in Spanish end to end. **Canaries 86/86**: decision byte-identical exactly once on both branches and both apps, `DECISION_SPEECH_EN` 197 chars exact, deterministic tariff, **the send-away line SPOKEN and emitted before every tool call**, cross-sell fired (`Add it`/`Not now` verbatim), decision card + `record_claim recorded: true` ordered before the card, `capture_claim_photo` fired with `attached: true` on every record email, six packet headings, `[RECORD] [CHAT]`/`[ASSESSOR] [CHAT]`/`[ASSESSOR] [VOICE]` at 200, `GTP_SURFACE` and `bargeInAwareness` byte-identical, `record_claim` still **unwired on voice**, all 13 chat and 9 voice tools byte-unchanged including `resolve_claim` (never read, echoed or patched). **TWO APPARENT REGRESSIONS WERE CLEARED BY LIVE CONTROLS, NOT BY REASONING** — a silent escalated turn and an `end_session` that killed the cross-sell both reproduced on the pre-change builds through `config.deployment`, so both are pre-existing; a second draft run then showed both canaries green. **ALSO SURFACED, NOT FIXED (top follow-up): a cracked-screen CHAT claim DEADLOCKS when the photo arrives in the same turn as the damage description** — `assess_screen_crack` is called before `run_diagnostic` sets `dx_issue`, p23's callback gate correctly refuses it, and `resolve_claim` then refuses with `PHOTO_REQUIRED` forever; reproduced identically on live chat v16, so it is a p23 defect, not a regression. **EMAIL DELIVERY CONFIRMED BY INBOX AND IT IS A DEMO RISK: both `[ASSESSOR] [CHAT]` (photo attached) and `[RECORD] [CHAT]` from p23 ARRIVED — p23's headline claim is verified by delivery, not just Resend acceptance — but only about TWO OF SEVEN test emails arrived, a ~30% delivery rate. Do not script a beat around a live-sent email until a sending domain is verified.** 15 conversations (3 of them deliberate live controls), 17 Resend sends, one 429 on an already-ended session, no retry loop, no 529. App list five, fork `9ae7a0c3` zero calls, no git commit. Previously: Quick task 260813-p23 COMPLETE and SHIPPED — **chat v16 `be4c83bb-825a-4feb-af88-379113543aa7` is LIVE on `d7bfbb93`** (repointed off v15 `129f8b31` at `2026-08-14T02:03:42.286752Z`, read back not inferred, `channelProfile` sha `76f98fed6482c930` byte-identical across the write, repoint **gated in code** on 48 assertions taken from `toolCall`/`toolResponse` objects on the final configuration — the gate refused twice and was only opened once both branches passed). **A photo is now asked for on EVERY chat claim** — total loss and liquid damage included, not only cracked screens — **and EVERY resolved chat claim files a record email carrying that photo**: `[RECORD] [CHAT] CLM-… - Jordan Rivera - APPROVED` on auto-approve (NEW; that path previously produced **no photo-carrying artifact at all**, which defeated the whole purpose) and `[ASSESSOR] [CHAT] CLM-… - Jordan Rivera` **byte-identical to v15** on escalation. Both observed live: `attached: true`, `photo_b64_len: 1530736`, Resend 200 (`CLM-24283` approved / `CLM-24064` escalated). New capture-only tool **`capture_claim_photo`** does the filing with **zero vision reasoning and no field describing the image**; `assess_screen_crack` is refused **deterministically** by `beforeToolCallbacks[0]` (`PHOTO_NOT_ASSESSED_FOR_THIS_ISSUE`) on any non-cracked-screen claim — proven as **zero invocations** on a live liquid-damage claim. `resolve_claim` and `assess_screen_crack` byte-unchanged; no Resend key read, echoed or printed. **Rollback: chat v15 `129f8b31-f06e-48cc-90fb-15dcf8611db1`, `updateTime 2026-08-13T17:56:20.954538Z`.** **DELIVERY UNCONFIRMED — seven emails await an inbox check (spam folder included); a Resend 200 is acceptance, not delivery.** Previously: Quick task 260813-olv COMPLETE — **FIX BUILT AND VERSIONED, DEPLOYMENT DELIBERATELY HELD. `d28bbcb0` still serves voice v14 `5d02f14c`; the new build is voice v15 `17b2e438-b132-49f1-8b32-190b132225ae` and is NOT deployed.** THE LANGUAGE RESET AT THE HANDOFF IS FIXED — a caller who opens in Spanish and switches to English before authentication now keeps English across the `claims_concierge` → `claim_intake` transfer (probe 3, the exact live failure from conversation `119vrJXUcbjQCO4DWJ_0w7xxw`, where the caller hung up). **THE BRIEF'S HYPOTHESIS WAS WRONG: it is not the first utterance.** A mirror control (English first, Spanish latest) also reset to Spanish — neither position predicts the outcome, **Spanish anywhere in the transcript does**. `claim_intake` resolves its language on sub-agent entry against the WHOLE transcript, then never re-decides; `claims_concierge` tracks the latest turn correctly, so the asymmetry is that a `globalInstruction` is not re-applied per turn inside a sub-agent. Two scripted edits, both voice-only: `globalInstruction` 2,364 → **2,859** (the *"from their very first words"* phrasing removed, replaced with a most-recent-message anchor) and `claim_intake` 18,215 → **20,711** (rules 22/22b/22c in `<constraints>`, a **recency tie-break inside the transfer step's own opener paragraph**, and three few-shot demos in the previously-empty `<examples>`). Edit 1 alone did NOT fix the handoff — the o5l "a step's own action out-competes a distant rule" lesson is what carried it. `case_summary`, `claims_concierge` and all nine tools are byte-identical to v14; `resolve_claim` never read or patched. **PROBES 1, 2, 3 PASS; PROBE 4 FAILS — mid-call switching INSIDE `claim_intake` does not work in either direction**, on clean non-decision turns, and it has never worked on any build of this project (it is 05-03's original finding, not a regression). **That failure is why the repoint was held per the gate.** Canaries **42/42** including `DECISION_SPEECH_EN` byte-identical at 197 chars, `decision_turn_agent_chunks == 1` on BOTH the English AND the Spanish run, the send-away line spoken, cross-sell fired, assessor packet six sections `[ASSESSOR] [VOICE]` at 200, `record_claim` still unwired. **nnm's double-delivery did NOT recur — but for a worse reason: the model TRANSLATED the decision on the decision turn itself, so `spoken != explanation` and the figures were model-composed. Threat T-05-12 is still open and 05-09 still owns it.** **⚠ `draft_equals_v14` is now FALSE — the voice draft equals v15, so 06-04 will ship these edits in any version it cuts.** Also recorded, not fixed: speech-to-text mangled the policy ID over five turns (`PGP`→`PDP`→`1000294`→`TDP100294`, three `verify_identity` calls) — the Phase 6 criterion 4 problem, on the auth path where 06-01's key normalisation does not reach. Previously: Quick task 260813-nnm COMPLETE and SHIPPED — **voice v14 `5d02f14c-8cba-4bf4-aa3a-b9caf57ffddc` is LIVE on `d28bbcb0` — THE PHONE AGENT NOW SPEAKS SPANISH** (repointed off v13 `5d9df25c` at `2026-08-13T22:23:57Z`, read back not inferred, `channelProfile` byte-identical, gated in code on 41 assertions against the new version's own snapshot). **Rollback: voice v13 `5d9df25c-3771-45bb-bd20-b28978cc5955`.** THE PREMISE THAT DROVE THIS TASK WAS WRONG — there is **no English-only clause** in any instruction, tool description or guardrail on **either** app; the live call's *"I can only speak English"* was emergent from voice's monolingual `languageSettings`. AND CONFIG ALONE DOES NOT FIX IT: applying chat's exact `languageSettings` to voice changed nothing (Spanish opener still answered in English) — **the response language is governed by the INSTRUCTION**. `05-03`'s `LANGUAGE_SWITCH_VERDICT` is corrected `LOCKS_AT_FIRST_UTTERANCE` → **`FOLLOWS_WHEN_INSTRUCTED`**. Spanish now runs end to end conversationally; **it reverts to English at exactly one place — the deterministic strings** (`resolve_claim.explanation`, the customer email), which is precisely 05-09's remaining job. Full English canary set green (21/21 approve, 16/16 escalate). **05-09 is now much smaller: its Task 2 Steps A/B are DONE; only the bilingual tool literals remain — plus one NEW defect this task surfaced, the decision being delivered TWICE (English tool string, then a model-composed Spanish restatement with the figures reworded).** Previously: plan 06-03 COMPLETE and SHIPPED — **chat v15 `129f8b31-f06e-48cc-90fb-15dcf8611db1` is LIVE on `d7bfbb93`** (repointed off v14 `cdca14e3` at `2026-08-13T17:56:20Z`, read back not inferred, `channelProfile` byte-identical). Every claim resolved on chat is now written to the shared store on the decision turn, on BOTH branches, and a verified chat customer can hear the status of a claim filed in another session, relayed byte-identically from `lookup_claim.status_line`. Three claims filed live and read back out of GCS independently. **Rollback target: chat v14 `cdca14e3-5b0e-4675-9861-f5f22736362f`.** Voice `d28bbcb0` untouched, still `5d9df25c`. **06-04 (wire voice) is unblocked and MUST read `06-03-SUMMARY.md` first — a widget tool call that carries a `textResponse` TERMINATES the model turn, which is why `record_claim` goes BEFORE `claim_decision_card`.** Still outstanding from 260812-trc, unaffected: (1) rotate the Resend key, (2) say which of 13 test emails arrived.
last_updated: "2026-08-18T02:55:00.000Z"
last_activity: "2026-08-11 - Phase 5 plan 05-05: the assessor briefing packet SHIPPED. Chat v10 658472a0-05be-4a28-9d9f-9774ebe0dd05 is live on d7bfbb93 (repointed from v9 160dc3b2, read back). An escalated chat claim composes a six-section packet from session state and delivers it as a second email, subject [ASSESSOR] CLM-24413 - Jordan Rivera, Resend HTTP 200, packet_text byte-identical (520 chars) to the composer's response, zero {placeholder} braces, zero leakage to the customer. All nine live assertions and a full English auto-approve canary passed on the DRAFT before the repoint. The 260811-suy repeated-question fix and 05-03's es-US multilingual config both shipped in v10 without regressing English. Turn-boundary finding: the packet is filed on the turn AFTER the email confirmation - give it one more customer turn. Double-email question CLOSED: single-send holds, confirmed by inbox. Unconfirmed: neither email seen by eye, and nobody has watched v10 in a browser."
progress:
  total_phases: 5
  completed_phases: 1
  total_plans: 7
  completed_plans: 5
  percent: 20
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-08)

**Core value:** A specification concrete and compelling enough that the implementation team can build a show-don't-tell demo that lands every wow moment — voice + mid-demo language switching, a visible autonomous-vs-human decision branch, the backend claims-processing reveal, and the cross-sell "cost center → profit center" moment.
**Current focus:** Phase 01 — foundations

## Current Position

Phase: 6 (active) — plans 06-01, 06-02, **06-03 and 06-04 COMPLETE**; the store is live, both store tools exist on both apps, **CHAT IS WIRED AND SHIPPED** (now **v20 `8a95ab02`** on `d7bfbb93`, rollback v19 `619e13a1`) and **VOICE IS WIRED FOR THE READ AND SHIPPED** (now **v20 `d8fe1e86`** on `d28bbcb0`, rollback v19 `e484ce3e` — ⚠ **rolling back reintroduces a FABRICATING agent**, see `260817-taq`) — both repointed 2026-08-14T11:10 by quick task **260814-80f**, which fixed the colliding claim-reference generator (a deterministic 900-second wheel, now a 10,000,000-second one; references are **8 digits**, `CLM-3xxxxxxx`) and gave `record_claim` a **cross-policy overwrite refusal** (`ref_collision`, `status_code: 409`, `store_calls` now **4**, not 3 — **06-05 must not assert 3**). The store's one collision (`CLM-24599`) is repaired: the Dell claim on `PDP100017` keeps the reference, the MacBook claim on `PDP100294` is re-issued as **`CLM-30623899`** carrying `superseded_ref`. Store audits **0/0/0** collisions. A caller on the phone hears the status of a claim filed on chat, relayed byte-identically from `lookup_claim.status_line`, never having been asked to read a reference aloud. **06-05 (cross-channel assertion) is the next and last dispatch, and it OWNS THE VOICE WRITE** — `record_claim` is still attached to no agent on voice, so the loop is closed one way only. **✅ The confirm-path fix has been carried across to chat by quick task `260814-8rv` (chat v20 `8a95ab02`) — an affirmative now routes through `claim_ref` on BOTH channels and 06-05 no longer owns it.** That task also fixed a second defect on chat: the cross-sell widget was being batched with `generate_case_summary` and ending the turn before `send_case_record_email` could run, so **no `[RECORD]` email was reaching the assessor mailbox on the auto-approve path**; both tools now fire in the same turn, mailer first. **06-05 must re-read its baselines live** — chat `claims_concierge` is now **10,475** chars (was 9,591) and chat `claim_intake` **34,311** (was 32,498); voice `claims_concierge` is now **12,699** (was 11,745) and its `afterModelCallbacks` **4,717** (was 2,092, and no longer byte-identical to chat's — see `260817-taq`), voice serves **v20 `d8fe1e86`** (rollback v19 `e484ce3e`), chat serves **v20 `8a95ab02`** (rollback v19 `619e13a1`), and voice `claim_intake` is unchanged at 20,711. Phase 5 build work continues alongside; Phase 2 still unstarted
Plan: 05-05 complete pending a blocking human checkpoint (chat v10 `658472a0` cut and LIVE on `d7bfbb93`; the assessor briefing packet proven end to end server-side, Resend 200; awaiting an inbox check and one browser pass); 05-03 and 05-04 complete; 05-01 complete; 05-02 in progress (decision-card rendering VERIFIED LIVE through the real widget embed on chat v7 `bb14cdcc` (2026-08-09); cover_offer_actions FIXED and FIRED for the first time on chat v8 `3f85b1d8` (2026-08-10) — payload proven live, on-screen render still unconfirmed; the silent decision turn FIXED on chat v9 `160dc3b2` (2026-08-10) — the explanation is now spoken byte-identically and exactly once, on-screen render still unconfirmed; wrong-subject guard resolved by removal 2026-08-06 — all three of 05-02's on-screen items are still unwatched, now on chat v10 `658472a0`); 05-06 through 05-10 outstanding
Status: Build work running ahead of the spec phases. Phases 2-4 author specification text and remain unstarted; Phase 5 changes the deployed agent and is where activity currently is.
Last activity: 2026-08-17 - **quick task 260817-taq (the voice agent fabricated a claim status) COMPLETE and SHIPPED — voice v20 `d8fe1e86-0af6-4eed-adb4-1588d08cb27f` is LIVE on `d28bbcb0`**, repointed off v19 `e484ce3e` at `2026-08-18T02:47:19.748978Z`, read back, `channelProfile` byte-identical, the cut and the repoint both gated in code on a 130/130 battery's exit status run twice, plus a version-snapshot check between them and a 14/14 deployed battery after. **THE MOST SERIOUS DEFECT FOUND ON THIS PROJECT, AND IT HAD HAPPENED THREE TIMES, NOT ONCE.** On live audio the phone answered a claim-status question **without ever calling `lookup_claim`** and invented the answer: `CLM92837` *"approved ... device was shipped"*, `CLM88342` *"approved ... payment of 825"*, and — worst and previously unnoticed — a flat **invented NOT-FOUND** on `PDP100294`, a policy holding **27 claims**, followed by an offer to open a duplicate. **`lookup_claim` ran on exactly one of the four status calls the phone has ever taken.** **THE BRIEF'S LEADING HYPOTHESIS WAS DISPROVEN BY THE SPAN TREE** — the second LLM round ran for 7.6 s after the tool response, so nothing was thrown away and 8rv's batching fix would have been a no-op; **my own follow-up hypothesis was disproven too** (text does not end an audio turn: 17 counterexamples in 166 turns). **The cause is 06-04's `SPEAK BEFORE YOU LOOK` clause** — it made the model compose speech in the step where it should have emitted the tool call, and on audio *"let me look that up"* is not a complete utterance, so the model finished it with an answer it did not have. 06-04 could not make that ordering fire on TEXT/API and shipped it as harmless; **on the channel it could not test, it fired exactly as written, and firing is the failure.** **Fixed structurally, not by wording:** the clause is **replaced** (the tool now runs first, and the agent is told it has no claim information of its own), and the `afterModelCallbacks` that already guards against the concierge inventing a device — 2,092 -> **4,717**, a **pure insertion**, placed **before** the verified early-return — now records every `lookup_claim` call and **refuses any claim fact stated without one**. Probes proved an undeclared `callback_context.state` key persists across turns, so **no new variable, no config change and no tool patch** were needed. **A regression I introduced (the agent began speech-shaping the tool's string) was caught by live controls against the pre-edit deployment and fixed in a second pass rebuilt from the pristine baseline** — pre-edit byte-identical x2, pass-1 shaped x2, pass-2 byte-identical x2, shipped byte-identical x2. `lookup_claim` fired in **8 of 8** status conversations and **every reference spoken came from its response**; `PDP100583` surfaces the true `CLM-31018263`; not-found still returns exactly the five contract keys; the guard's 22/22 battery refuses all three real fabricated utterances verbatim. **Chat was scanned (92 conversations, zero fabrications, clause absent, no audio channel) and is NOT exposed, so it was held read-only** — but the two apps' callbacks are now deliberately divergent and chat should adopt the same guard. Full canary set green including the byte-identical decision, the send-away line, the cross-sell, the `[ASSESSOR] [VOICE]` packet at 200, the 869-char greeting callback, Spanish through the handoff, and an unchanged store. **⚠ A sixth app `902be2ac` "demo" appeared today at 19:43Z — not created by me, and not deleted by me.** **📞 NOBODY HAS DIALLED: the phone check is the only thing that can close this.** Previously: **quick task 260814-8rv (fix the missing "yes" branch in chat claim status) COMPLETE and SHIPPED — chat v20 `8a95ab02-e653-4d1e-baee-eb500c52710c` is LIVE on `d7bfbb93`**, repointed off v19 `619e13a1` at `2026-08-14T11:40:17.051321Z`, read back, `channelProfile` byte-identical, the cut and the repoint both gated in code on a 116/116 battery's exit status and re-proved by a 44/44 deployed battery. **TWO DEFECTS.** (1) **The confirm path**: `selector` had no branch for a bare agreement, so a chat customer answering *"yes"* to the disambiguation was told there was nothing on file **immediately after their claim was read back to them**. Fixed by mirroring 06-04's voice design — the affirmative routes through **`claim_ref` taken from the disambiguation line the tool itself supplied**. `claims_concierge` 9,591 -> 10,475, one contiguous region, byte-identical read-back, the 869-char callback intact. **Proven on three affirmatives across two builds** (*"yes"*, *"that's the one"*, and *"correct"* on the deployment); the negative and the not-found paths both still behave. **Chat and voice now behave identically on confirmation.** (2) **The `[RECORD]` email was never being sent on auto-approve** — the cross-sell widget was issued in the same batch as `generate_case_summary`, ending the turn before `send_case_record_email` could run. **The proposed fix would have been a no-op** (the instruction already ordered them correctly); the defect was **reproduced live on a byte-identical `claim_intake` first**, which is what caught it. **The real mechanism, and it generalises: a tool that consumes another tool's response needs a SECOND round inside the turn, and a turn-ending widget in the first batch throws that round away.** Fixed by stating the dependency in each step's opening action lines; `claim_intake` 32,498 -> 34,311, two pure insertions, nothing deleted or moved. **Both tools now fire in the same turn on the deployment**: `[RECORD] [CHAT] … - APPROVED` at HTTP 200 with `attached: true`, then the cross-sell buttons last. **This settles 80f's cross-sell canary — the cross-sell was never independently broken; the two tools were competing for one turn's last slot.** Canaries draft 116/116 and deployed 44/44, none skipped. Store left byte-for-byte at 80f's closing state (30 objects, 0/0/0 collisions, no debris). Voice received zero non-GET calls and still serves v19 `e484ce3e`. **Nobody has watched v20 in a browser.** Previously: **quick task 260814-80f (fix colliding claim references and repair the store) COMPLETE and SHIPPED ON BOTH APPS — chat v19 `619e13a1` on `d7bfbb93`, voice v19 `e484ce3e` on `d28bbcb0`**, both read back, both gated in code on a 121/121 battery's exit status, both rollback targets re-read live immediately before their PATCH. **The generator was never random: `CLM-{24000 + (int(time.time()) % 900) + seq}` is a deterministic 900-second wheel that repeats every fifteen minutes** — established by shape probes that never read, echoed or persisted `resolve_claim` (it holds the Resend key), and verified against the store 22/22. **Exactly one collision existed** (`CLM-24599`, a MacBook on `PDP100294` and a Dell on `PDP100017`, resolved at the same `t%900`). **Fixed twice over:** the wheel widened to 10,000,000 (115.7 days, references now always 8 digits) by three digit-only anchored edits per app (+11 chars), and **`record_claim` now REFUSES a cross-policy overwrite** (`ref_collision: true`, `409`, neither PUT issued) — proven by invocation 17/17 per app and against the real key. **`store_calls` is now 4, not 3.** **The repair is stated, not silent:** the Dell keeps `CLM-24599` (live demo data, and the claim `by-ref` already described); the MacBook is re-issued as `CLM-30623899` carrying `superseded_ref` and a `repair_note`. Store now 30 objects, 0/0/0 collisions, no test debris. **The cross-sell canary did not fire and was proven pre-existing by a live control on chat v18 plus a repeat draft run — three runs across two builds agree.** A deployed run on v19 after the repoint confirms the fix in production. No agent instruction changed by a byte. **Nobody has watched v19 in a browser or dialled the phone.** Previously: **plan 06-04 (claim status lookup on the phone) COMPLETE and SHIPPED — voice v18 `a6f6b620-af15-4e43-b0d8-bfbbb2d64a46` is LIVE on `d28bbcb0`**, repointed off v17 `dcc20863` at `2026-08-14T03:50:48.984372Z`, read back not inferred, `channelProfile` byte-identical, the repoint gated in code on a 47/47 battery's exit status and the rollback target re-read live and asserted unmoved. **The phone reads back `CLM-24690` — a claim written by CHAT — byte-identically from `lookup_claim.status_line`, ending *"Filed on 14 August 2026 via chat"*.** The caller is identified by name and policy ID and is never asked to say a claim reference out loud; `verify_identity` fires strictly before `lookup_claim`; the model's `policy_id` argument is ignored in favour of session state (T-06-03, proven live); several claims disambiguate by reading the most recent back with none of the alternatives spoken; an ambiguous opener gets exactly one question; a cross-policy reference and an unknown one return byte-identical not-found responses with zero figures spoken. **A Rule-2 gap in 06-03's chat region was found and fixed here (`selector` has no "yes" branch) and is still live on chat — 06-05 must port it.** `ASYNCHRONOUS` re-opened and rejected: the status line IS the tool's result, so async buys nothing and costs the evidence. **Platform finding: the text-before-tool-call gate is not achievable by prompting** — the model cannot speak before the value it must speak arrives; the acknowledgment is spoken first within the utterance and the measured pause is 2.9-3.8 s, the shortest tool-backed turn on the app. **The liquid-damage escalation stall was proven PRE-EXISTING by a live control against v17** and the runbook now scripts the escalated phone beat as a drop, not a spill. **UNHEARD: nobody has dialled. Criterion 2 is proven server-side on TEXT/API and unconfirmed by ear — the four-call script is `06-04-VOICE-EVIDENCE.md ## PHONE_SCRIPT`, and the one cosmetic risk to listen for is the literal inch mark in `Apple MacBook Pro 16"`.** Also flagged: `PDP100294` now holds 25 test claims, so the agent says *"I can see 25 claims on this policy"* — prune the store or run the beat on Maria Santos / `PDP100583`. Previously: **quick task 260813-ui0 (fix the hardcoded greeting in the `beforeModelCallback`) COMPLETE and SHIPPED ON BOTH APPS.** `d7bfbb93` serves **chat v18 `d0e4bfef`**, `d28bbcb0` serves **voice v17 `dcc20863`**, both repointed and read back, both gated in code on a 93/93 battery's exit status. **260813-tgq's instruction change had been silently overridden on both channels by a `beforeModelCallback` that hard-returns the greeting as a literal `LlmResponse` on session start — returning an `LlmResponse` short-circuits the model, so the instruction never ran on turn one.** Chat carried the byte-identical callback and was worse: on live chat v17 it opened with the identity demand and then demanded identity again on turn 1. One anchored `str.replace()` per app, 915 → 869 chars, one contiguous region, the other two branches (clean disconnect, *"Are you still there?"*) byte-identical PRE vs POST and re-fired on the newly-serving builds. **`runSession` DOES deliver session start** — `{"event": {"event": "session start"}}`; tgq's probes missed the bug because they opened with a plain text turn. Verified on the draft, in each version's snapshot, and through `config.deployment` on the deployed builds. **UNHEARD: nobody has dialled since. The user must confirm the first spoken sentence, hang up mid-call, and stay silent for *"Are you still there?"* — and read the chat widget's first message in a browser.** Recorded, unfixable by prompting: the session-start greeting is always English; the agent switches to Spanish from the caller's first real utterance. Previously: **quick task 260813-tgq (open with a greeting and "how can I help") COMPLETE and SHIPPED ON BOTH APPS.** `d7bfbb93` serves **chat v17 `64be15eb`**, `d28bbcb0` serves **voice v16 `09a1f14d`**, both repointed and read back, both gated in code on an 86/86 battery. Neither agent opens by demanding identity: both greet, name the carrier and ask *"How can I help you today?"*, and take name and policy ID only in the reply to the stated intent. The greeting was **emergent, not a literal** — one in-place replacement inside `<step name="Collect details">` on `claims_concierge`, +1,516 on each app, one contiguous region, byte-for-byte read-back, nothing else touched on either app. All four probes pass on both channels (including a volunteer-everything opener with **zero re-asks**, and a Spanish opener still greeted in Spanish). **Two apparent regressions — a silent escalated turn and an `end_session` that killed the cross-sell — were cleared by running the identical script against the pre-change builds through `config.deployment`; both reproduce there, so both are pre-existing.** **Newly surfaced and NOT fixed: a cracked-screen CHAT claim deadlocks if the photo arrives in the same turn as the damage description** (p23's two deterministic gates deadlock each other); reproduced on live chat v16. **The user's inbox check landed: p23's `[ASSESSOR] [CHAT]` and `[RECORD] [CHAT]` both ARRIVED — delivery-verified, not just accepted — but only ~2 of 7 test emails arrived (~30%), so a live-sent email must not carry a demo beat until a sending domain is verified.** Runbook updated: both scenario scripts now open with the greeting turn, the escalation script warns against the volunteer-everything opener, the chat script warns to attach the photo only when asked, and both rollback targets and version tables are corrected. Previously: **quick task 260813-olv (the language reset at the agent handoff) COMPLETE — FIX BUILT AND VERSIONED AS voice v15 `17b2e438-b132-49f1-8b32-190b132225ae`, DEPLOYMENT DELIBERATELY HELD ON v14 `5d02f14c`.** The handoff reset is fixed (probe 3 — the exact live failure that ended call `119vrJXUcbjQCO4DWJ_0w7xxw` with a hang-up), probes 1 and 2 hold, canaries 42/42, but **probe 4 — switching language after the handoff — fails in both directions and has never worked on any build**, so the gate held the repoint. The brief's hypothesis was refuted by a mirror control: it is not the first utterance, it is **Spanish anywhere in the transcript**, resolved once on sub-agent entry and never re-decided. **A `globalInstruction` is not re-applied per turn inside a sub-agent.** Two scripted edits, voice only: `globalInstruction` 2,364 → 2,859 and `claim_intake` 18,215 → 20,711; `case_summary`, `claims_concierge` and all nine tools byte-identical to v14. **⚠ `draft_equals_v14` is now FALSE — 06-04 will snapshot these edits.** Shipping is one `deployments.patch`; v15 is strictly better than the phone's current build on every tested dimension. Also recorded, not fixed: the policy ID took five turns and three `verify_identity` calls to capture on that same live call, and the runbook's over-confident "it no longer matters" note was corrected. **Nobody has dialled the number since.** Previously: **quick task 260813-nnm (Spanish on the voice channel) COMPLETE and SHIPPED — voice v14 `5d02f14c` is LIVE on `d28bbcb0`.** The phone agent follows the caller's language from the first word. Two premises died: there is **no English-only clause on either app**, and **chat's exact `languageSettings` applied to voice changed nothing** — the response language is instruction-governed. `05-03`'s verdict corrected to `FOLLOWS_WHEN_INSTRUCTED`. Spanish reverts to English at exactly one seam, the deterministic strings, which is 05-09's remaining work — plus a NEW defect this surfaced: the decision is spoken twice (English tool string, then a model-composed Spanish restatement). Full English canary set green (21/21 + 16/16); rollback voice v13 `5d9df25c`. **Nobody has dialled the number yet.** Previously: **plan 06-03 (wire the claim store into chat) COMPLETE and SHIPPED — chat v15 `129f8b31-f06e-48cc-90fb-15dcf8611db1` is LIVE on `d7bfbb93`**, repointed off v14 `cdca14e3` at `2026-08-13T17:56:20.954538Z`, read back not inferred, `channelProfile` byte-identical across it, and the repoint **gated in code** on four assertion batteries re-passing. `record_claim` now fires exactly once on the **decision turn** on both branches; `lookup_claim` is attached to the root agent `claims_concierge` behind `verify_identity`. Three claims filed by live chat conversations and read back **out of GCS independently of the tool that wrote them**: `CLM-24599` (APPROVED, $840/$25, `photo_assessed: true`, `DL-1`), `CLM-24536` (HUMAN_REVIEW, $3,000/$25, `DL-3`+`DL-2`), `CLM-24841` (APPROVED, $420/$25). The stored figures equal the figures the customer was told. A later session then read `CLM-24841` back to a verified customer — the write and read halves already meet. **The instruction-only verbatim relay HELD; the `dataMapping` widget fallback was not built and not needed.** Full Phase 5 canary set green on the shipped instruction, including the v11 send-away-line canary. See `06-03-SUMMARY.md` and `06-03-CHAT-EVIDENCE.md`. Previously: **quick task 260812-trc (diagnose the missing customer email on the live phone call) COMPLETE — DIAGNOSIS ONLY, NOTHING SHIPPED.** `resolve_claim` is NOT broken: an instrumented byte-identical copy, run inside the CES sandbox via `executeTool`, got **HTTP 200 with a real Resend message id**, and the deployed voice v13 snapshot's `resolve_claim` is byte-identical to what was tested (`sha 890678c445f9fe9c`). **Six hypotheses killed by execution** — a `5d9df25c` regression (the tool's `updateTime` is `2026-08-05`, predating the v11 that demonstrably delivered), the `.ok`/`status_code: 0` trap (the tool references neither), the `import ces_requests` trap, an unsupported kwarg, quota exhaustion (`x-resend-daily-quota` was ~21/100 and ~92/3,000 at call time, and the headers are *used* counts because they increment), and a rate limit (10/**second**, 9 remaining). The key is valid and identical in `resolve_claim` and `send_case_record_email` on both apps. **What is left is DELIVERY**: Resend's 200 is acceptance only, the sender is the shared unverified `onboarding@resend.dev` and the recipient is a corporate mailbox — the classic intermittent-drop shape, consistent with `CLM-24464` arriving and `CLM-24430` not. **A SEPARATE CONFIRMED DEFECT, on BOTH apps:** `resolve_claim` writes `email_delivery="live"` and `email_status="queued"` *before* the request, calls `raise_for_status()` inside `except Exception`, records no status code, and returns `email_queued: true` unconditionally — so a 401, 422, 429 or a thrown exception are all indistinguishable from success. **Nothing was patched, no version cut, no deployment repointed**; voice still serves `5d9df25c`, chat still serves `cdca14e3`, both re-read. Zero CES conversations. Previously: **plan 06-01 (Phase 6 claim-store spike) COMPLETE — the store mechanism and schema are LOCKED by the user (`as-proven`), rungs 3 and 4 formally rejected, and 06-02 is unblocked with no external dependency outstanding.** A CES Python tool **cannot** obtain a Google credential (metadata server `status_code: 0`, `google.auth` absent, no credential env vars) but egress is **open** to arbitrary public hosts, so the store is **GCS reached over the S3-compatible XML API and signed with a GCS HMAC key** — proven by a write→read→delete round-trip run from inside a CES Python tool (PUT 200, GET 200 byte-identical, anonymous GET 403, DELETE 204, GET-after-delete 404). Zero CES conversations spent; `apps.executeTool` on a disposable app carried every probe, and that app is deleted (GET → 404). **Nothing needs deploying and no GCP service needs enabling** — `storage.googleapis.com` was already on. Created and awaiting the user's accept/reject: bucket `gs://meridian-claim-store-500614` (private, public-access-prevention enforced, currently empty) and service account `claim-store-writer@…` scoped to that bucket alone. Both live deployments were re-read and are **unchanged** (chat `d7bfbb93` → v14 `cdca14e3`; voice `d28bbcb0` → `5d9df25c`); zero non-GET calls to either app or the fork. Previously: quick task 260812-o5l: **chat v14 `cdca14e3-5b0e-4675-9861-f5f22736362f` is LIVE on `d7bfbb93`** (repointed off v13 `1eb3fd5c`, read back not inferred). The customer's uploaded photo is now attached to the `[ASSESSOR] [CHAT]` packet, the send-away line and customer email stop asking for a photo already sent, and a cross-sell regression that the new wording introduced was diagnosed against a live-v13 control and fixed. Full canary set green **on the deployment itself**. Rollback target: chat v13 `1eb3fd5c-5aff-46c8-b572-e3fe18bf966f`. Still unwatched by a human: the photo-contradiction demo path, and 05-02's on-screen render items.

Progress: [██████████] 100%

## Performance Metrics

**Velocity:**

- Total plans completed: 5
- Average duration: - min
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01 | 5 | - | - |

**Recent Trend:**

- Last 5 plans: -
- Trend: -

*Updated after each plan completion*
| Phase 01-foundations P05 | 2min | 3 tasks | 3 files |

## Accumulated Context

### Roadmap Evolution

- Phase 5/6 METHOD, and it changed two verdicts in one task — **a single draft observation is NOT evidence about an instruction change; take the LIVE control on the identical script, and take a second draft run, before blaming the edit** (2026-08-13, quick task 260813-tgq). Two failures appeared on the patched drafts and both looked exactly like this project's known disasters. (1) **A silent escalated turn** — packet filed, escalated, closed, `outputs[0].text` literally `null`, the v11 `838b6d2b` shape. The identical four-turn script run against **live voice v15 `17b2e438`** through `config.deployment` produced the **identical silence**, so the cause is the *compressed conversation shape*, not the edit: when the caller volunteers everything in one message the flow collapses by two turns and `send_claim_email` + `generate_case_summary` + `send_case_record_email` + `escalate_to_human` + `end_session` all land on one turn the model treats as pure closure. On a normal-shaped run the line is **spoken before all five tool calls** on both apps. (2) **`end_session` on the chat record turn killing the cross-sell**, p23's arrangement-C shape verbatim, with the same odd `reason: "escalation after errors"`. The live control did not reproduce it **but ran a different turn alignment**, so a *second draft run* was taken; it aligned as live did and the cross-sell fired. Model variance on a compressed decision turn, not a property of the new instruction. **Generalisation: on this platform the same script can compress or expand by a turn between runs, and a turn-alignment difference is indistinguishable from an instruction regression until you control for it. Budget one live control and one repeat draft run per suspected regression — three of this task's fifteen conversations were spent this way and they were the best-spent three.**

- Phase 5 DEFECT SURFACED, NOT FIXED, TOP FOLLOW-UP — **a cracked-screen CHAT claim DEADLOCKS when the photo arrives in the same turn as the damage description** (2026-08-13, quick task 260813-tgq; observed on the draft AND reproduced identically on **live chat v16 `be4c83bb`**, so it is a p23 defect and not a regression). Sequence: the model calls `assess_screen_crack` **before** `run_diagnostic` has set `dx_issue`; p23's `beforeToolCallbacks[0]` therefore refuses it with `PHOTO_NOT_ASSESSED_FOR_THIS_ISSUE` — correctly by its own predicate, since at that instant the claim is not yet known to be a screen; `run_diagnostic` then terminates at `screen` and `capture_claim_photo` files the image fine; and `resolve_claim` refuses with `PHOTO_REQUIRED` because no crack was ever *assessed*. The agent then loops asking for "another photo", or on live simply `end_session`s. **p23's two deterministic backstops are each correct in isolation and deadlock each other when the photo is volunteered early.** The fix needs the gate to tolerate a not-yet-terminal screen claim, or `assess_screen_crack` to be re-callable once the diagnostic terminates — both inside `claim_intake`, and possibly `resolve_claim`, which is Resend-key-bearing. **Demo mitigation already in DEMO-RUNBOOK.md: attach the photo only AFTER the agent asks for it, or run a non-screen fault (dead keyboard, $420), where the photo is filed rather than assessed and the claim resolves normally.**

- Phase 5 DEMO RISK, now evidenced by the inbox rather than inferred — **the email delivery rate is about 30%, and no demo beat may depend on an email arriving** (2026-08-13, folded in during quick task 260813-tgq). The user confirmed by inbox that **both `[ASSESSOR] [CHAT]` (with the photo attached) and `[RECORD] [CHAT]` from 260813-p23's runs ARRIVED — so p23's headline claim is verified by DELIVERY, not merely by Resend acceptance**, and the photo-carrying record email on the auto-approve path (the artifact that path previously never produced at all) genuinely lands in a mailbox. **But only about two of the seven test emails arrived**, against seven consecutive HTTP 200s. That ~30% rate is the strongest evidence yet that the unverified shared sender `onboarding@resend.dev` is the problem rather than the mailer — 260812-trc had already killed six other hypotheses by execution and concluded what remained was delivery, and 2-in-7 against consistent 200s is exactly the intermittent-drop shape of an unverified shared sending domain. **Consequence: a scripted "and here's the email that just landed" moment fails about two times in three, in the room. Say the email has been sent; do not open a mailbox on stage.** The fix is a **verified sending domain** at resend.com/domains — a Resend account action, not a code change — and it also removes the spam-folder problem and the one-mailbox-only restriction. This is now more urgent for the demo than either the outstanding Resend key rotation or `resolve_claim`'s truthfulness fix.

- Phase 6 UNBLOCKED, WITH ROOM RESERVED — **06-04's status-lookup branch can be added without re-opening the greeting instruction** (2026-08-13, quick task 260813-tgq, by construction). The new region says nothing about routing: it states that the same details are asked for whatever the caller came for, and that *"what they came for decides what happens AFTER they are verified, not what you ask for here - so remember what they said, carry it forward, and do not act on it yet."* So (1) the status branch is a **sibling `<subtask>`**, exactly as 06-03 already built on chat (`<subtask name="Check on an existing claim">`, sitting between `Verify the caller` and `Anything else`, which never touched `Collect details` and which this task never touched); (2) the open question *"How can I help you today?"* already elicits a status enquiry and the region names the existing-claim family explicitly, so 06-04 need not re-word it to make its branch reachable; and (3) the intent is captured **before** verification but explicitly not acted on until after, which is 06-04's T-06-03 gating requirement. **06-04 MUST re-read its plan-start figures live: voice `claims_concierge` is now 6,642 chars (not 5,126) and the voice rollback target is now v16 `09a1f14d`. Voice `claim_intake` is unchanged at 20,711 and 06-04's byte-identity assertion on it still holds. `draft_equals_v1x` remains FALSE on BOTH apps, now one version further along — the chat draft equals v17 `64be15eb` and the voice draft equals v16 `09a1f14d`, so any version 06-04 cuts carries this greeting change.**

- Phase 5/6 PLATFORM FACTS from 260813-tgq, each found by execution: (1) **`runSession`'s image input key is `image`, NOT `inlineData`** — `SessionInput` is a `oneof` over `text | event | willContinue | variables | dtmf | image | toolResponses | blob | audio`, so an image is a sibling input object `{"image":{"mimeType":"image/png","data":"<b64>"}}` and a `Part`-style `{"inlineData":{...}}` returns **400 `INPUTTYPE_NOT_SET`**. This is a **different shape from p23's `apps.executeTool` context shape** (`user_content.parts[].inlineData`) — the two APIs do not share it. Proven: `capture_claim_photo` returned `{"captured": true, "photo_b64_len": 488, "parts": 1}` for a 364-byte synthetic PNG, so the whole chat photo path is now drivable from a script with no browser. (2) **`runSession`'s `outputs` is an ARRAY, one entry per speaking agent**, and only the last entry carries the fullest `diagnosticInfo` — reading `outputs[0]` alone silently loses the sub-agent's turn and makes a perfectly good handoff look silent. (3) **`diagnosticInfo.messages` is cumulative for the whole session**, not per-turn; slice at the last user chunk matching the turn you sent or every assertion double-counts. (4) **An `end_session` with `session_escalated: true` ends the session for `runSession` too** — the next turn returns 400 `SESSION_ALREADY_ENDED`; plan the last scripted turn accordingly rather than reading that 400 as a defect.

- Phase 5/6 LIVE ON BOTH CHANNELS — **neither agent opens by demanding identity; both greet and ask "How can I help you today?"** (2026-08-13, quick task 260813-tgq; **chat v17 `64be15eb-947f-4f36-8af2-2aefd225742b` on `d7bfbb93`** repointed off v16 `be4c83bb` at `2026-08-14T02:35:48.599636Z`, **voice v16 `09a1f14d-be24-40ba-abbd-a06e495f5d0d` on `d28bbcb0`** repointed off v15 `17b2e438` at `2026-08-14T02:35:55.602430Z`, both read back, both `channelProfile` byte-identical, both repoints gated in code on the same 86-assertion battery re-run inside the ship script; each app gated independently, both passed). **Rollback: chat v16 `be4c83bb`, voice v15 `17b2e438`, both re-read live immediately before their PATCH.** **THE GREETING WAS NEVER A LITERAL — that is the finding.** No instruction on either app contains *"could I take your full name and your policy ID?"*. It was emergent from `claims_concierge`'s `<role>` (*"the front door... verify who you are speaking to"*) plus the single opening line of `<step name="Collect details">`, whose `<trigger>` is *"The caller has not yet been verified"* — that step fires on turn one, so **it WAS the greeting**. The fix is therefore one **in-place replacement of that step's opening prose**, not a new step and not an appended paragraph — deliberately, because every turn-placement disaster on this project (v11's gag order, o5l's last-paragraph-wins, p23's three-way oscillation) came from *adding* prose rather than neutralising it. The step's **last paragraph is untouched** (the policy-ID digit normalisation), so its "last paragraph wins" property is preserved. Identical +1,516 on both apps (chat `claims_concierge` 8,075 -> **9,591**, voice 5,126 -> **6,642**), **one** contiguous region each (prefix 2,658 / old span 224 / new span 1,740), 7/7 must-survive literals, zero original non-blank lines lost, no bare LF and no non-ASCII introduced, byte-for-byte read-back, and each cut version's snapshot equal to the exact string the script intended to write. Both `claims_concierge` instructions are pure **CRLF** (chat 87/0, voice 63/0) — note chat `claim_intake` is pure LF, so anchors are still not transferable; the replacement was read **from a file** and normalised per app. **PROBES 4/4 ON BOTH APPS.** (1) The greeting fires and demands nothing. (2) *"I need to file a claim"* -> identity -> claim completes end to end. (3) **A customer who volunteers name, policy and loss in one opening message is verified and transferred on that same turn with ZERO re-asks** — the regression most likely to annoy an audience, and it is earned by the region's closing rule *"Never make anyone repeat something they have already told you."* (4) A Spanish opener is still greeted in Spanish end to end (*"Hola, bienvenido a Meridian Device Protection. Mi nombre es Alex. ¿Cómo puedo ayudarle hoy?"*), so olv's v15 language fix is not regressed. **CANARIES 86/86**, every one from a `toolCall`/`toolResponse`: decision byte-identical and emitted exactly once on both branches and both apps, `DECISION_SPEECH_EN` 197 chars exact on TEXT/API, deterministic tariff 420/840/3,000 with `DL-1` and `DL-3`+`DL-2`, **the send-away line SPOKEN with its chunk before every tool call on both apps and both branches**, cross-sell fired with `Add it`/`Not now` verbatim, `claim_decision_card` `textResponse` byte-identical, `record_claim recorded: true` **ordered before the card**, `capture_claim_photo` fired and `attached: true` on every record email, six packet headings each once with zero `{placeholder}`, `[RECORD] [CHAT] ... - APPROVED` / `[ASSESSOR] [CHAT]` / `[ASSESSOR] [VOICE]` all at Resend 200, zero store vocabulary and zero `[ASSESSOR]` leakage, `GTP_SURFACE` + `bargeInAwareness` + `languageSettings` byte-identical, **`record_claim` still UNWIRED on voice**, 13 chat and 9 voice tools byte-unchanged with **`resolve_claim` never read, echoed or patched on either app**, fork zero calls, app list five, no git commit.

- Phase 5 LIVE — **chat v16 `be4c83bb-825a-4feb-af88-379113543aa7` is serving `d7bfbb93`; a photo is asked for on EVERY chat claim and EVERY resolved chat claim files a photo-carrying record email** (2026-08-13, quick task 260813-p23; repointed off v15 `129f8b31` at `2026-08-14T02:03:42.286752Z`, read back not inferred, `channelProfile` sha `76f98fed6482c930` byte-identical, repoint **gated in code** on 48 assertions taken from tool objects on the final configuration). **Rollback: chat v15 `129f8b31-f06e-48cc-90fb-15dcf8611db1`, `updateTime 2026-08-13T17:56:20.954538Z`.** WHAT SHIPPED: (1) the photo ask moved off the `"See the damage"` subtask — which fires only on `REPAIRABLE` + cracked screen — onto the acknowledgement turn of **every** claim, so the $3,000 total loss and the liquid spill are no longer the two scenarios that settle with no evidence; observed live on a liquid-damage claim. (2) The Case record step now runs on **BOTH** branches, so an auto-approved claim files `[RECORD] [CHAT] CLM-24283 - Jordan Rivera - APPROVED` with `attached: true`, `photo_b64_len: 1530736`, Resend 200 — **that path previously produced no photo-carrying artifact whatsoever**, which is the gap that defeated the stated purpose (feeding a claim-photo corpus to a Gemini Enterprise compliance reviewer). Escalation still files `[ASSESSOR] [CHAT] CLM-24064 - Jordan Rivera`, **byte-identical to v15** — the corpus filter is the CHANNEL token `[CHAT]`, not the leading one. (3) NEW capture-only tool **`capture_claim_photo`**: slices `context.user_content` base64 into the same `photo_b64_*` variables the mailer already reassembles, and returns `{captured, photo_b64_len, parts}` and **nothing describing the image**. (4) `assess_screen_crack` refused **deterministically** by `beforeToolCallbacks[0]` (`PHOTO_NOT_ASSESSED_FOR_THIS_ISSUE`, +1,516 chars, pure insertion) on any non-cracked-screen claim — **proven as ZERO invocations on a live liquid-damage claim**, not refused-then-retried. The separation is the point: a total-loss device is inert, and asking a vision model to describe damage it cannot see is the contamination already proven **unfixable by prompting** (commit 2c22582). Gating on a terminal variable is only safe because capture moved to an ungated tool — a `beforeToolCallback` returning a dict short-circuits the tool before its body runs. **NOTE the gate is keyed on `dx_issue`, NOT `issue_category`** as the plan specified: `issue_category` cannot tell a cracked screen from a broken keyboard, both are `physical_damage`. UNTOUCHED and asserted byte-identical: `resolve_claim`, `assess_screen_crack`, `claims_concierge`, and all nine other pre-existing tools; the Resend key was never read, echoed or printed (count 1 before, 1 after, counted not printed). **`case_summary` HAD to be edited despite the plan forbidding it** — its first taskflow step is a hard `NO_PACKET_REQUIRED` guard on `decision != HUMAN_REVIEW`, observed live returning exactly that on an AUTO_APPROVE run, so Task 1 was structurally impossible without widening it; the escalated packet is unchanged, the only substantive addition is one ACTION clause for approved claims. **DELIVERY UNCONFIRMED: seven emails await an inbox check, spam folder included.**
- Phase 5 MECHANISM — **when you make an existing step run on a NEW branch, the danger is not the trigger, it is that the step's PROSE was written for the old branch** (2026-08-13, quick task 260813-p23, found by four live failures across seven conversations). The chat HUMAN_REVIEW email-confirmation turn broke **three distinct ways** while the Case record step was being widened to both branches: adding a "does not close the call" paragraph fixed the approve path and made the escalated turn go **completely SILENT** (packet filed, escalated, closed, not one word spoken — the chat v11 `838b6d2b` shape); merging the speak-first rule into the step's last paragraph made the escalated turn **skip the whole step**, emitting a farewell copied verbatim from the instruction *including its quote marks* and then `end_session`; restoring v15's action body byte-exactly fixed escalation and **killed the cross-sell on approve** (`end_session` on the record turn). ROOT CAUSE: on v15 the Case record step never ran on the approve path, so the last thing the model read about that turn was the "Ask for photos by email" step and its AUTO_APPROVE `end_session` prohibition. Making the step run on both branches inserted an **escalation-flavoured** block (*"File the case record for the specialist"*) after that prohibition, and the approve path started closing the call. THE FIX WAS NOT TO ADD A RULE — every added paragraph degraded the other branch — but to make the existing sentence **branch-neutral**, replaced in place, no new paragraph, step ending untouched (+301 chars). This sits alongside o5l's "the last paragraph of a step wins" and 06-03's "nothing may be instructed after a widget tool call".
- Phase 5/6 PLATFORM FACTS from 260813-p23, each found by execution: (1) **A CES tool CANNOT read a response header — at all.** `ExternalResponse`'s entire public surface is `json, ok, raise_for_status, reason, status_code, text`; there is **no `headers` member**. So `x-resend-daily-quota` is unreachable from inside a tool and the mailer now returns `"unavailable: CES ExternalResponse exposes no headers"` rather than an empty string a caller would read as "quota fine" — the same silent-success shape `resolve_claim` already had to be criticised for. Count sends or read the Resend dashboard. (2) **`apps.executeTool`'s `variables` and `context` are a `oneof`** (`tool_execution_context`) — sending both returns 400; you get session state OR a synthetic `ToolCallContext`, never both. (3) The context shape that actually populates `context.user_content` is **mixed-case and undocumented**: `{"user_content":{"parts":[{"inlineData":{"mimeType":"image/png","data":"<b64>"}}]}}` — outer key snake, inner key camel. `userContent`, `inline_data` and a snake `mime_type` inside the blob are all **silently ignored**, and every wrong spelling returns a well-formed 200 with an empty `Part`, so a wrong guess is indistinguishable from a tool that found no image. This is how the entire photo path was proven with no browser and no conversation. (4) **An `agentTool` cannot be tested by `executeTool`** — `generate_case_summary`'s `{decision}` template does not resolve from the request's `variables`, so BOTH branches return `NO_PACKET_REQUIRED`; that is a false negative, not a result. (5) **A CES session expires across a long idle** — a turn sent ~7 minutes after the previous one came back at `turnIndex 1` as a brand-new session with default variables; pace multi-turn scripts tightly (≤40 s worked).

- Phase 5 BUILT BUT **NOT** SHIPPED — **voice v15 `17b2e438-b132-49f1-8b32-190b132225ae` fixes the language reset at the agent handoff; `d28bbcb0` was deliberately HELD on v14 `5d02f14c`** (2026-08-13, quick task 260813-olv; zero `deployments.patch` calls, deployment `updateTime` still `2026-08-13T22:23:57.500510Z`, re-read after the version cut). **THE DEFECT:** on the live call `119vrJXUcbjQCO4DWJ_0w7xxw` the caller opened in Spanish, switched to English, authenticated in English — and at the `claims_concierge` → `claim_intake` handoff the agent reset to Spanish and could not be pulled back by three consecutive English turns; the caller hung up. **REPRODUCED IN TWO TEXT TURNS** against the draft (Spanish greeting → English identity → read `claim_intake`'s opener), so this no longer needs a phone call. **THE BRIEF'S HYPOTHESIS — "resolved against history where Spanish appeared first" — IS REFUTED BY A MIRROR CONTROL:** English first + Spanish latest ALSO reset to Spanish. Neither first nor latest predicts it; **Spanish appearing anywhere in the transcript does.** `claim_intake` resolves its output language once, on sub-agent entry, against the WHOLE transcript, and never re-decides. `claims_concierge` tracks the latest turn correctly on every observation — **the asymmetry is the finding: a `globalInstruction` is NOT re-applied per turn inside a sub-agent, so a sub-agent needs the rule in its own instruction.** WHAT SHIPPED INTO v15: two scripted `str.replace()` edits from a file, anchors asserted unique in voice's own CRLF bytes, must-survive literal lists, every original non-blank line re-checked, byte-for-byte read-back. (1) `globalInstruction` 2,364 → **2,859** (+495, one contiguous region): nnm's *"Answer in the same language the customer is speaking, **from their very first words**"* — readable as "the language of their first words" — replaced with a most-recent-message anchor; the never-announce and figures bullets untouched. (2) `claim_intake` 18,215 → **20,711** (+2,496, three insertions, `old_region == 0` on the first): rules `22`/`22b`/`22c` at the end of `<constraints>`, a **recency tie-break inside `<step name="Confirm the device">`'s own opener paragraph**, and three few-shot demos in the previously-EMPTY `<examples>` block. **EDIT 1 ALONE DID NOT FIX IT** — the handoff turn still reset. What carried it was the o5l lesson applied deliberately: **a step's own action out-competes a distant `<constraints>` rule on the turn that step fires**, so the rule had to go INSIDE the transfer step, not be shouted louder. `case_summary` was deliberately NOT touched (it carries the by-design *"the packet is always written in English"* clause and a follow-the-caller rule would fight it); `claims_concierge` was NOT touched (it already gets the language right); the handoff line was tested as a language carrier and **does not work** — the concierge emitted no send-away line at all on two of five handoffs. All nine tools byte-identical to v14, **`resolve_claim` never read, patched or echoed**, no Resend secret touched, `record_claim` still wired to NO agent. **RESULT: probes 1, 2 and 3 PASS; PROBE 4 FAILS.** Probe 4 — switching language AFTER the handoff, inside `claim_intake` — fails in **both** directions on clean mid-diagnostic turns (EN→ES `olv-p5` t3: caller Spanish, agent English es=0/en=4; ES→EN `olv-p6` t4: caller English, agent Spanish es=4/en=0). **It has never worked on any build of this project** — it is 05-03's original chat finding, not a regression introduced here. **That single failure is why the repoint was held**, per the task's own gate. CANARIES **42/42**: `DECISION_SPEECH_EN` byte-identical at 197 chars and `spoken == explanation`, `decision_turn_agent_chunks == 1` on the English **and** the Spanish run, tariff 840/25/1,500/3,000 `DL-1`, send-away line **spoken**, one `send_claim_email`, **cross-sell fired**, escalation `HUMAN_REVIEW` `DL-3`+`DL-2` $3,000 with the assessor packet at six sections and `[ASSESSOR] [VOICE] CLM-24176` at HTTP 200, no `[ASSESSOR]` leakage, `GTP_SURFACE` and `bargeInAwareness` byte-identical, 33 variables, `SEQUENTIAL`, `gemini-3.1-flash-live`. **nnm's double-delivery defect did NOT recur — but for a WORSE reason, not because it is fixed: the model TRANSLATED the decision on the decision turn itself, one chunk, so `spoken != explanation` on the Spanish run and the figures (840/1,500/25/CLM-24495) were model-composed rather than tool-supplied. Threat T-05-12 is still open; 05-09 still owns it and its scope is unchanged.** **⚠ `draft_equals_v14` IS NOW FALSE** — the voice draft equals v15 `17b2e438`, not the deployed v14, so **06-04 (the next dispatch) will snapshot these language edits into any version it cuts**; harmless (42/42 canaries) but it must be a decision, not a surprise. Live `claim_intake` is 18,215 chars, the draft is 20,711 — re-read both live. **SHIPPING IS ONE CALL** (`deployments.patch` `updateMask=appVersion` → `17b2e438`), and the argument for it is that v15 is strictly better than the phone's current build on every tested dimension: probe 3 goes from a catastrophic call-ending defect to a pass, probe 4 fails on both builds equally. Previously: Phase 5 LIVE — **voice v14 `5d02f14c-8cba-4bf4-aa3a-b9caf57ffddc` is serving `d28bbcb0`; the phone agent speaks Spanish** (2026-08-13, quick task 260813-nnm; repointed off v13 `5d9df25c` at `2026-08-13T22:23:57.500510Z`, read back not inferred, `channelProfile` byte-identical, repoint **gated in code** on 41 assertions run against the **new version's own snapshot**). **Rollback: voice v13 `5d9df25c-3771-45bb-bd20-b28978cc5955`.** **TWO PREMISES KILLED BY EXECUTION.** (1) **There is no English-only clause — on either app.** Every authored surface was searched on the live draft *and* the deployed snapshot: `globalInstruction` (1,720), `claims_concierge` (5,126), `claim_intake` (18,215), `case_summary` (6,447), all 9 tool descriptions, and both guardrails — **zero** language-restricting clauses; chat `a2f621e4` is equally clean (checked GET-only). The only `English` hits are `case_summary`'s deliberate *"The packet is always written in English…Never translate this packet"* (internal assessor packet, by design) and `claim_intake`'s *"The caller speaks naturally; you translate"* (colloquial device-name normalisation). The `Default Prompt Guardrail` is the platform default, byte-shared with chat, and lists **multilingual as SAFE**. The live call's *"I am sorry, I can only speak English"* was **emergent** from voice's monolingual `languageSettings`, not authored anywhere. (2) **Config alone does not make it speak Spanish.** Controlled 3-probe experiment, identical Spanish opener, voice draft, TEXT/API, 05-03 scorer: as-found → `es=0/en=3`; **+ chat's exact `languageSettings` (`supportedLanguageCodes:["es-US"]`, `enableMultilingualSupport:true`) → `es=0/en=5` — NO CHANGE**; + a `Language:` block in `globalInstruction` → **`es=7/en=0`**. **`enableMultilingualSupport` governs INPUT and declares the synthesis surface; the RESPONSE language is governed by the INSTRUCTION and nothing else reachable through configuration.** WHAT SHIPPED: three `apps.patch` edits only — `languageSettings` (mirroring chat), `synthesizeSpeechConfigs` → `{"en-US":{}, "es-US":{}}` built as snapshot-plus-one-key, and `globalInstruction` 1,720 → **2,364** (+644) by scripted `str.replace()` anchored on voice's own CRLF `"Absolute rules:\r\n"`, a **single contiguous insertion with zero bytes deleted**, placed BEFORE the absolute rules so the instruction still ends on them (the o5l last-paragraph-wins lesson). **No tool was created, patched or read; no agent instruction touched; `resolve_claim` hash unchanged (`898ebc7ab33d29af`); no Resend code or secret read.** WHERE SPANISH STOPS: the conversational layer is fully bilingual (greeting, identity, handoff, send-away line **and the cross-sell** all in Spanish), and it reverts to English at **exactly one place — the deterministic strings**: `resolve_claim.explanation` came back byte-verbatim English mid-Spanish-call, and the customer email is English (`send_claim_email` is **templated in code** → 05-09 Task 1 Step D must take the `templated` branch). **NEW DEFECT, not in 05-09's plan: the decision is delivered TWICE** — the English tool string, then the model's own unprompted Spanish restatement on the next turn, with `$840` silently reworded to `840 dólares`; the figures survived but were **model-composed, not tool-supplied**, which is exactly threat T-05-12. ENGLISH UNREGRESSED on the shipped build: **21/21** auto-approve canaries (`DECISION_SPEECH_EN` byte-identical at 197 chars, `decision_turn_agent_chunks == 1`, tariff 840/25/1,500/3,000 `DL-1`, send-away line **spoken**, one `send_claim_email`, **cross-sell fired**) and **16/16** escalation canaries (`HUMAN_REVIEW` `DL-3`+`DL-2` $3,000, assessor packet with **all six sections** and subject `[ASSESSOR] [VOICE] CLM-24240 - Jordan Rivera` at HTTP 200, no `[ASSESSOR]` leakage). GTP surface proven untouched: `bargeInAwareness: true` byte-identical, `audioProcessingConfig` minus `es-US` byte-equal to v13, no DTMF/timeout/interrupt key appeared. All 7 v13 tools and all 3 agents byte-identical to v13. **`record_claim` and `lookup_claim` are snapshotted into v14 (06-02 added them and cut no version) but are wired to NO agent and ship inert — `record_claim` stays unwired on voice, asserted as a repoint gate, per 06-04.** Also proven on the deployment itself via `config.deployment` (`nnm-live-v14-f1`): a Spanish opener to the live phone build answers in Spanish. **STILL UNHEARD BY A HUMAN: nobody has dialled the number** — whether the `es-US` voice sounds Spanish or like an English voice reading Spanish, and whether barge-in still cuts in mid-Spanish-sentence, cannot be established from a conversation record.
- Phase 5 CLOSED — **MID-CALL LANGUAGE SWITCHING WORKS AT THE HANDOFF AND ONLY AT THE HANDOFF. DO NOT SCRIPT IT AS A DEMO BEAT** (2026-08-13, quick task 260813-olv, four direct observations). 05-03 said don't script it; 260813-nnm re-opened it as *plausible but untested*; this task settles it with clean mid-diagnostic turns on the fixed build. **What works:** the caller opens in one language and settles into another **before authentication completes** — the switch survives the `claims_concierge` → `claim_intake` transfer (probe 3, fixed in v15 `17b2e438`). **What does not work, on any build ever tested:** switching language once `claim_intake` is already running. EN→ES (`olv-p5` t3 — caller *"Se me cayo y se rompio"*, agent replies in English, es=0/en=4) and ES→EN (`olv-p6` t4 — caller *"The screen is cracked, and there was no liquid at all"*, agent replies in Spanish, es=4/en=0) **both fail**, on turns deliberately chosen to be mid-diagnostic rather than the decision turn, where an English tool literal is relayed verbatim and would confound the reading. **Three interventions were tried and none moved it:** an app-level `globalInstruction` anchor, an explicit `<constraints>` rule inside the sub-agent, and three few-shot `<examples>` demonstrating exactly this switch. A fourth — appending the rule to `<step name="Ask and record">` — was **deliberately NOT attempted**, because that step's action **ends on the `260811-suy` repeated-question / `DIAGNOSTIC_INCOMPLETE` recovery rule**, and o5l's recorded warning is explicit: *"do not add a competing paragraph to the end of a step that already ends on a rule you depend on."* Trading a working diagnostic for an unproven language fix is a bad trade. **This joins the wrong-subject photo guard as the second behaviour on this project shown to resist prompting**, and it is the same family as 06-03's *"two rounds of stronger wording changed nothing, because the cause was never instruction strength."* **Also generalised from this: a `globalInstruction` is NOT re-applied per turn inside a sub-agent** — `claims_concierge` tracked the caller's latest turn correctly on every single observation while `claim_intake`, governed by the same app-level block, did not. Any behaviour a sub-agent must exhibit needs the rule in that sub-agent's own instruction, and if it must fire on a specific turn, inside the `<step>` that governs that turn.
- Phase 5 / Phase 6 DEMO RISK, RECORDED NOT FIXED — **speech-to-text mangles the policy ID on the phone, and `DEMO-RUNBOOK.md` was over-confident about it** (2026-08-13, quick task 260813-olv, evidence from live call `119vrJXUcbjQCO4DWJ_0w7xxw`, AUDIO/LIVE, v14 `5d02f14c`). **Capturing ONE policy ID took five turns and three `verify_identity` calls**: the transcript ran `PGP` → `PDP` → `1000294` → `TDP100294` before `PDP100294` landed, with a barge-in mid-sequence making it worse. The auto-approve story fits in ~2 minutes (05-06 item 6), so **roughly a third of the demo can be spent failing to authenticate**, in the room, immediately before the moment the agent is meant to look brilliant. This is the same problem **Phase 6 criterion 4** was written around: 06-01 already normalises store keys (`PDP-100294` → `PDP100294`) *"so a spoken policy id survives speech-to-text"* — but that normalisation lives in the **store lookup** path, and the **authentication** path (`verify_identity`) has no equivalent, which is why it still bites. **The runbook previously claimed *"'PDP' coming through as 'TDP' or 'BDP' no longer matters"* — that is now corrected**, replaced with say-it-digit-by-digit, don't-barge-in, expect-one-retry. Two candidate fixes for whoever picks it up, **neither attempted here** (`verify_identity` was out of scope): normalise inside `verify_identity` the way 06-01 normalises store keys, accepting a small prefix confusion set (`PGP`/`TDP`/`BDP` → `PDP`) and a digit-length tolerance; or accept it as a presenter-technique issue and leave the runbook note as the whole mitigation.
- Phase 5 CORRECTED — **`05-03`'s `LANGUAGE_SWITCH_VERDICT` is `FOLLOWS_WHEN_INSTRUCTED`, not `LOCKS_AT_FIRST_UTTERANCE`** (2026-08-13, quick task 260813-nnm). The 05-03 observation was correct and reproduced on voice; **the attribution was wrong.** 05-03 itself flagged two readings of its evidence and warned that under the second — *"the app simply always answers in `defaultLanguageCode` regardless"* — **the two-back-to-back-calls fallback would not work either.** The cheap probe 05-03 asked for was run, on voice, opening in Spanish: **the second reading is correct.** Nothing locks at the first utterance; the response language was never a function of the utterance. **The spike was NOT confounded by a prompt clause** (none exists on either channel) — it measured a real platform property, just not the one it named. CONSEQUENCES: never rely on configuration alone for a Spanish beat on any channel; the two-call fallback was never viable and is not the reason Spanish now works; and **the mid-call EN→ES switch is re-opened as an OPEN question, not a closed negative** — the shipped voice instruction explicitly tells the agent to switch mid-conversation, but **no conversation in this task exercised it**. One 2-turn probe settles it; **do not script it as a demo beat until someone runs that probe.** The instruction-level fix is live on **voice only**; chat `a2f621e4` has not received it.
- Phase 6 SHIPPED — **chat v15 `129f8b31-f06e-48cc-90fb-15dcf8611db1` is LIVE on `d7bfbb93`** (2026-08-13, plan 06-03; repointed off v14 `cdca14e3` at `2026-08-13T17:56:20.954538Z`, read back not inferred, `channelProfile` sha256 byte-identical across it, repoint **gated in code** on four batteries re-passing): **chat writes every resolved claim to the shared claim store and reads any claim back.** THE WRITE: `record_claim` fires exactly once on the **decision turn**, on **both** branches, ordered `resolve_claim` → `record_claim` → `claim_decision_card`, returning `recorded: true` / `status_code: 200` / `store_calls: 3`. Three claims filed by live conversations and read back **out of GCS independently of the tool that wrote them** — `CLM-24599` (APPROVED, 840.0/25.0, `photo_assessed: true`, `["DL-1"]`), `CLM-24536` (HUMAN_REVIEW, 3000.0/25.0, `["DL-3","DL-2"]`, `total_loss_flag: true`), `CLM-24841` (APPROVED, 420.0/25.0, `["DL-1"]`), all `channel: CHAT` with `created_at`, `device`, `excess`, `coverage_limit`, `auto_approval_cutoff`, `photo_status`, `schema_version: 1`. **The stored figures equal the figures the customer was told**, asserted per run as `record_claim.args` versus `resolve_claim`'s result. THE READ: `lookup_claim` is attached to the **root agent `claims_concierge`**, not `claim_intake` — the concierge already owns `verify_identity` so the caller is authenticated before any lookup, and the read path stays out of the instruction whose editing keeps breaking this project. Session `c7status-d9684db8` proved multi-match → `disambiguation_line` byte-identical once → `selector: "the older one"` → `CLM-60202` → `status_line` byte-identical once → a cross-policy `CLM-60203` → `found: false` verbatim with **zero currency figures and zero `CLM-` strings in any agent chunk**. **T-06-03 strong form proven live: `policy_source: "session"` on all three lookups.** **The instruction-only verbatim relay HELD — the named `dataMapping` widget fallback was NOT built and NOT needed.** Instructions: `claim_intake` 24,630 → **28,215** (+3,585, two anchored regions), `claims_concierge` 5,126 → **8,075** (+2,949, one region); note **chat `claims_concierge` is CRLF (63 CRLF, 0 bare LF) while chat `claim_intake` is pure LF** — anchors are not transferable even within one app. All twelve chat tools byte-unchanged by whole-object sha256, **`resolve_claim` included — no Resend code read, echoed or modified**. Full Phase 5 canary set green on the shipped instruction: deterministic tariff, decision byte-identical to `resolve_claim.explanation` exactly once, single customer send (structural only), **the send-away line still SPOKEN and emitted before every tool call on the escalated turn**, the assessor packet on the email-confirmation turn with six headings and `[ASSESSOR] [CHAT]` at HTTP 200, cross-sell `{actions:[…]}`, card `productItem` byte-identical with `units` an int, packet tools zero on approve, and **zero store vocabulary in any customer-facing text**. **Rollback: chat v14 `cdca14e3-5b0e-4675-9861-f5f22736362f`.** Voice `d28bbcb0` and fork `9ae7a0c3` untouched; app list still five.
- Phase 6 PLATFORM MECHANISM — **a widget tool call that carries a `textResponse` TERMINATES the model turn; anything instructed after it is NEVER called** (2026-08-13, plan 06-03, discovered by execution at the cost of two conversations). `claim_decision_card` is what delivers the decision sentence (the v9 `dataMapping` `FIELD_MAPPING` `"textResponse": "explanation"` mechanism), so it is not a step *before* the text — **it IS the text, and it closes the turn.** Wiring `record_claim` *after* the card, exactly as 06-03's plan specified, produced `record_claim` call count **0** on the escalated branch on `c2esc-6851866c` and again on `c3esc-f1a4ad37` after the instruction was strengthened with an explicit "on BOTH branches" three-tool sequence and a "never end this turn without calling it" clause. **Two rounds of stronger wording changed nothing, because the cause was never instruction strength.** It fired on the approve run `c1appr-9087deae` only because the model happened to issue card + `record_claim` in ONE parallel batch — luck, not design. Reordering to `resolve_claim` → `record_claim` → `claim_decision_card` fixed it on the first attempt on both branches, and the card stayed exactly where it has always been: last, delivering the decision text byte-identically, once. **GENERAL RULE for every future turn edit on this project: nothing may be instructed after a widget tool call. Put the work BEFORE the widget.** This sits alongside the two ordering lessons already recorded (the v11 `838b6d2b` gag-order shape, and o5l's "the last paragraph of a step wins"). **06-04 inherits it directly** — voice's decision turn has the same shape.
- Phase 6 MEASURED, for 06-04's latency decision (2026-08-13, plan 06-03): a full `record_claim` is 1 GET + 2 PUTs and measured **1.52 s / 1.55 s** end-to-end from the executor via `apps.executeTool`; inside a live conversation the platform ran `claim_decision_card` and `record_claim` in **one parallel batch** with both responses landing **0.31 s** apart. `ASYNCHRONOUS` was **deliberately NOT used on chat**: (1) the repoint gate needs `recorded: true` observable in a `toolResponse` on the decision turn and async removes it, (2) 06-02 locked `executionType: SYNCHRONOUS` on all four store tools and 06-05 re-checks port parity, so flipping chat alone manufactures the exact drift 06-05 hunts, (3) chat has no latency problem to buy anything with. **06-04 SHOULD re-open it, and for a sharper reason than "voice is slower": under the shipped ordering `record_claim` sits between the caller's last word and the SPOKEN decision** — invisible in a web widget, audible on a phone call, immediately before the demo's best moment. Options are `ASYNCHRONOUS`, `ces_requests.async_*`, or accepting ~1 s.
- Phase 6 BUILT (2026-08-13, plan 06-02): **the shared claim store is LIVE and both store tools exist on BOTH apps, invoked and verified, but deliberately UNWIRED.** Store: `gs://meridian-claim-store-500614`, prefix `claims/`, exactly the locked rung-2 mechanism (GCS over the S3-compatible XML API, AWS SigV4, GCS HMAC key in tool source). **No GCP service was enabled**; the accepted bucket and service account were neither recreated nor deleted. Unauthenticated `GET` → **403**; an authenticated `GET` of a key never written → **404** with a non-JSON body. Five claim records: fixtures `CLM-60201`/`CLM-60202` (`PDP-100294`) and `CLM-60203` (`PDP-100583`), plus `CLM-60210` written by CHAT `record_claim` and `CLM-60211` written by VOICE `record_claim` — **a cross-channel pair already in the store, so 06-05 can assert cross-channel retrieval without filing anything first.** Tool resource ids are `record_claim` and `lookup_claim` on both `a2f621e4` and `6e01e4a5`. `lookup_claim` is **byte-identical** across the two apps; `record_claim` differs on exactly ONE line (`channel: str = "CHAT"` vs `"VOICE"`) and hashes identically once normalised. **41/41 behavioural assertions on chat, 41/41 on voice, all against saved raw `toolResponse` objects on the SHIPPED source — invoked, not read back.** 71/71 isolation assertions: every pre-existing tool's whole-object hash, both apps' `claim_intake`/`claims_concierge` instruction hashes, voice's 33 `variableDeclarations`, `languageSettings` and `audioProcessingConfig` all unchanged; **no version cut, neither deployment repointed** (chat `d7bfbb93` → v14 `cdca14e3`, voice `d28bbcb0` → `5d9df25c`, both `updateTime`s identical at plan start and end). Zero CES conversations; `apps.executeTool` carried every proof and the throwaway app was deleted (GET → 404, app list back to 5). **T-06-03 is mitigated in the STRONG form: `lookup_claim` reads `context.state['policy_id']` and ignores the model-supplied argument entirely, and it never reads `claims/by-ref/` at all** — a cross-policy reference returns a response **byte-identical** to an unknown one, and no not-found response carries a single claim field. **06-03/06-04 wire against `06-02-STORE-CONTRACT.md` (`## STORE_SURFACE`, `## TOOL_CONTRACT`, `## PORT_PARITY`) verbatim and re-derive nothing.**
- Phase 6 ANTI-PATTERN CORRECTED IN NEW CODE (2026-08-13, plan 06-02): **`record_claim` and `lookup_claim` are built as the deliberate inverse of `resolve_claim`'s truthfulness defect.** Every success field starts `false`/`0` and is **derived at the end of the function from a status code already observed** — `recorded = all(s in (200,201) for s in [by_policy_status, by_ref_status])` — never pre-set; every branch is on `int(getattr(r,'status_code',0) or 0)` and **never on `.ok`**; an exception is caught and its type/message recorded in `error` with the step in `error_stage`, and `recorded` stays false; a read that is neither 200 nor 404 **aborts before any write** so an unreadable policy object is never clobbered; and `store_calls`, `read_status`, `by_policy_status`, `by_ref_status`, `status_code`, `error`, `error_stage` are all returned so a caller can tell a write that happened from one that did not, and where it stopped. **Proven by invocation**, with a wrong-key copy on a throwaway app: `{"recorded": false, "status_code": 403, "read_status": 403, "store_calls": 1, "error": "unexpected read status 403", "error_stage": "read"}`. A real defect during the build (bytes passed to `data=`) surfaced the same way unprompted — under the `resolve_claim` shape it would have returned success for a write that never happened. **06-03 and 06-04 MUST branch on `recorded`.**
- Phase 6 PLATFORM RULES for every future CES Python tool (2026-08-13, plan 06-02, each discovered by a failed create): (1) **CES registers EVERY module-level function name in the app-wide tool namespace and names the tool after the FIRST one** — the first create here landed with `displayName: _sigv4_headers` and the second failed `409 ALREADY_EXISTS`. **A tool must have exactly one module-level `def`**; fold every constant and helper inside the entry function. (2) **Every parameter needs an explicit type annotation, including helpers** (`400 ... has parameters without types`). (3) **A default may not be `None`** (`400 Invalid default value: ... Expected non-null value, received null`) — use `[]`, `""`, `0.0`, `False`. (4) **There is no `inputSchema` field and `description` is not top-level**: CES derives the parameter schema from the signature + type hints and **overwrites `pythonFunction.description` with the entry function's docstring**, silently discarding a supplied description — so the tool's "what this is NOT" prose and per-argument docs must live in the docstring. (5) **`ces_requests.put(..., data=...)` requires a STRING**; bytes raise `TypeError: Object of type bytes is not JSON serializable`. (6) `apps.executeTool` seeds session state via a **`variables`** field in the request body — which is what lets a tool's `context.state` read be proven rather than assumed.
- Phase 6 MECHANISM **LOCKED** by the user 2026-08-12, literal reply **`as-proven`** (plan 06-01 — see `06-01-SPIKE-FINDINGS.md`, five greppable sections; rungs 3 and 4 formally rejected, Cloud Run costed but never built; the bucket and service account are ACCEPTED as permanent and must NOT be deleted): **the shared claim store is Google Cloud Storage, reached over the S3-compatible XML API and signed with AWS SigV4 using a GCS HMAC key held in tool source.** Proven by execution, not doc-reading: `PUT` 200, signed `GET` 200 with `payload_byte_identical: true`, **unauthenticated `GET` 403**, `DELETE` 204, `GET`-after-delete 404, plus a second probe running the real access pattern (cold-start 404 → write → read-modify-write append → one-`GET` lookup by policy returning newest-first → one-`GET` lookup by ref → 404 for an unknown ref) — ten signed calls in **3.56 s**. **Rung 1 (Google-credentialed direct) is DEAD**: the metadata server returns `status_code: 0, reason: "Error fetching from URL"` on both `metadata.google.internal` and `169.254.169.254` while plain `http://example.com/` returns 200 in the same invocation, `google.auth`/`google.cloud.*` are `ModuleNotFoundError`, and all 12 env vars are non-credentials. **Egress is OPEN, not allowlisted to `api.resend.com`.** Rung 3 (Cloud Run) and rung 4 (third-party host) were costed and NOT built — neither is needed. **COST: no GCP service enablement (`run`/`artifactregistry`/`cloudbuild`/`firestore`/`secretmanager` all stay disabled), no component to operate, no data leaving the project.** Created: bucket `gs://meridian-claim-store-500614` (US, uniform access, public-access-prevention enforced, empty) and SA `claim-store-writer@insurance-agent-demo-500614.iam.gserviceaccount.com` with `roles/storage.objectAdmin` **on that bucket only**. The one new secret is a GCS HMAC key in tool source — same risk class as the Resend key already in `resolve_claim`; the spike's key was deactivated and deleted, and 06-02 mints its own inside a single process. **SCHEMA:** two objects per claim — `claims/by-policy/{POLICY_KEY}.json` (newest-first array) and `claims/by-ref/{CLAIM_KEY}.json` — so *both* lookups are one `GET` with no index and no LIST; keys normalised (`PDP-100294` → `PDP100294`, `CLM-24253` → `CLM24253`) so a spoken policy id survives speech-to-text, which is what makes criterion 4 reachable without anyone reading a claim reference aloud; no pre-composed prose field, the status sentence is composed at read time in Python. **`record_claim` CAN run asynchronously** (`ExecutionType` accepts `ASYNCHRONOUS`; `ces_requests.async_*` exists), so 06-04 may have no voice-latency problem at all.
- Phase 6 SANDBOX FACTS that change how every future CES tool is written (2026-08-13, plan 06-01): (1) **`import ces_requests` FAILS with `ModuleNotFoundError`** — it is an *injected global*, as are `requests`, `context`, `tools`, `async_tools`, `Part`, `Blob`, `StatusError`. Any tool guarding its HTTP call with `try: import ces_requests` silently does nothing (the 05-07 shape). (2) **`ExternalResponse.ok` is `True` even when `status_code` is `0`** — the metadata probe returned `ok: true, status_code: 0`; **never branch on `.ok`, branch on `.status_code`**, or a store write that never happened reads as a success. (3) `ces_requests` accepts `headers`, `params`, `data`, `json` and **rejects** `timeout`, `allow_redirects`, `verify` — the 05-07 `timeout=20` defect generalised. (4) **`context.state` is a plain writable `dict`** and `executeTool` echoes writes back in its `variables` block, so a lookup tool can read the session-authenticated policy id itself rather than trusting a model-supplied parameter (threat T-06-03's control). (5) `apps.executeTool` body is `{"tool": "<FULL resource name>", "args": {...}}` — a bare tool id returns 400; and a `:executeTool` URL passed to curl via a shell variable can mangle into `Could not resolve host: xecuteTool`, so pass it with `--url`.
- Phase 5 LIVE — the chat demo now serves **v14 `cdca14e3-5b0e-4675-9861-f5f22736362f`** (2026-08-13, quick task 260812-o5l): deployment `d7bfbb93` was repointed off v13 and read back, `updateTime 2026-08-13T01:35:50Z`. v14 = the photo the customer uploads is captured on the turn it arrives and **attached to the `[ASSESSOR] [CHAT]` packet** (`attached: true`, `photo_b64_len: 1530736`, Resend 200, live on `CLM-24253`); the spoken send-away line and the customer email both branch on whether a photo was assessed, so nobody is asked to re-send a photo they just sent; and a cross-sell regression introduced by that wording was found and fixed. Full canary set green on the deployment itself, not only the draft (`o5l-live-v14-a1`). **Rollback target: chat v13 `1eb3fd5c-5aff-46c8-b572-e3fe18bf966f`.** Voice `6e01e4a5` and fork `9ae7a0c3` were never called.
- Phase 5 MECHANISM — **a `beforeToolCallback` short-circuits a tool BEFORE its body runs** (2026-08-13, quick task 260812-o5l): the chat `assess_screen_crack` gate (`if not v.get("dx_outcome") -> DIAGNOSTIC_INCOMPLETE`) meant a photo volunteered mid-diagnostic could NEVER be captured — the tool was refused on the one turn the image bytes exist, and ran for real a turn later when they were gone. Reproduced three times (`o5l-esc-b1`, `-h1`, `-j1`: `photo_check: confirmed` but `attached: false`) **while the same-turn instruction rule was being obeyed**. Fixed by re-keying that gate on `issue_category`; `resolve_claim` is still gated on `dx_outcome`. **Any deterministic gate placed in front of a tool also disables that tool's side effects — check what else the tool does before gating it.**
- Phase 5/6 MECHANISM, THE SECOND MEMBER OF THE FAMILY — **a `beforeModelCallback` that returns an `LlmResponse` short-circuits THE MODEL, so an instruction change cannot affect that turn** (2026-08-13, quick task 260813-ui0; sits alongside the `beforeToolCallback` entry directly above, which is the same mechanism one layer down). **BOTH** apps' `claims_concierge` carried a byte-identical 915-char `beforeModelCallbacks[0]` (sha `10cf58b137b2b4fa…`) that hard-returned the opening greeting as a literal `LlmResponse` on `<event>session start</event>`. 260813-tgq rewrote the *instruction* to greet and ask *"How can I help you today?"*, its probes passed **truthfully**, and the phone and the widget both went on opening with the old identity demand — because on turn 0 the model never ran. Proven on the SERVING build, not the draft: live chat v17 `64be15eb` through `config.deployment` opened with *"…To pull up your policy, could I take your full name and your policy ID?"* **and then demanded identity a second time on turn 1**, which is the exact double-ask tgq existed to remove. **THE VERIFICATION LESSON IS THE REUSABLE PART, AND THE BRIEF'S PREMISE WAS WRONG: `runSession` DOES deliver session start.** The `event` arm of `SessionInput` is a *message* whose own field is also called `event`, so the phone's turn 0 is reproducible from a script as **`{"event": {"event": "session start"}}`** — a bare `{"event": "session start"}` is a 400, `{"text": "<event>session start</event>"}` also fires the branch, and **`{"event": {"name": "session start"}}` is a DIFFERENT, silently-plausible event that lets the model run and returns a perfectly good greeting from the wrong code path.** tgq's probes missed the bug not because the channel was blind but because they opened with `{"text": "Hi there."}`, an ordinary user turn the branch never matches. **Any future greeting, opener or turn-one behaviour must be probed with the `event`-in-`event` form and confirmed on the real channel; a probe that opens with a sentence verifies the instruction and tells you nothing about what a caller hears.** These probes short-circuit the model entirely, so they cost effectively no quota. **Also recorded, by construction unfixable by prompting: because branch 1 returns a hardcoded English literal, the session-start greeting is ALWAYS English** — a Spanish caller hears the English opener and the agent switches from their first real utterance (verified end to end, handoff included). Script the Spanish beat as *"the agent switches"*, never *"the agent opens in Spanish."*

- Phase 5 MECHANISM — **instruction position is load-bearing; the last paragraph of a step wins** (2026-08-13, quick task 260812-o5l): the same 528-char rule, byte-identical, produced opposite defects depending on where it sat in the `"Ask for photos by email"` step. Last in the step → approve path behaved but the HUMAN_REVIEW turn went **completely silent** (packet filed, not a word spoken — the chat v11 regression shape, `o5l-esc-g1`). Earlier in the step → the escalated line came back but the approve path called `end_session` on the send-away turn and killed the cross-sell (`o5l-appr-n1`, `-p1`). Fixed by **merging both rules into one paragraph** so the step still ends on *"the last thing they ever hear from you must not be the decision alone."* Both paths green after that (`o5l-appr-q1`, `o5l-esc-r1`). Do not add a competing paragraph to the end of a step that already ends on a rule you depend on.
- Phase 5 CAUTION — **`cover_offer_actions` CAN be broken by wording elsewhere** (2026-08-13, quick task 260812-o5l): this does NOT reverse the 260811-tgm entry below (the offer genuinely fires and both earlier voice negatives were false negatives), but it adds a second, real failure mode on CHAT. Changing the send-away line from an open loop (*"reply with photos"*) to a closed one (*"nothing further you need to send us"*) made the model read its own sentence as the end of the call and emit `end_session` — with the bogus args `session_escalated: true, reason: "escalation after errors"` on a cleanly approved claim. Proven by running the **identical script against the live v13 deployment**, where the offer fired, and against the draft, where it did not. **When you touch closing wording on chat, re-run the cross-sell canary — a control on the live deployment via `config.deployment` costs nothing and settles it.**
- Phase 5 OPEN — **the photo-contradiction path has still never been run live on any channel** (2026-08-13, quick task 260812-o5l): the total-loss branch never asks for a photo (`"See the damage"` triggers only on `REPAIRABLE` + cracked screen), so the scenario that actually shows the new attachment off — reported crack, no damage visible, `DL-5`, human review, photo attached for the assessor — remains unexercised. It needs a photo of an **undamaged** device, which the repo does not have. Everything shipped in v14 works; this is the demo beat nobody has watched.
- Phase 5 TECHNIQUE — **`config.deployment` on `runSession` runs a conversation against a deployed version without touching it** (2026-08-13, quick task 260812-o5l): this is how the v13-vs-draft control was run. Read-only; the deployment is not modified. Together with `apps.executeTool` (no conversation at all) it removes most of the reason to guess at whether a defect is new. Also: `excludeDiagnosticInfo: true` keeps a `runSession` response at ~300 bytes instead of 1.5 MB on an image turn, and `--diag` on the packet turn shows every tool call and result inline.
- Phase 5 added: Demo Build - Multimodal, Backend Reveal & Multilingual (build phase, not spec authoring)
- Phase 6 added (2026-08-12): Cross-Channel Claim Status — a claim filed on one channel is retrievable on the other, via a shared claim store both apps read and write. Build phase, like Phase 5. Narrows DEF-01 (cross-channel continuity, deferred at milestone close) from full session continuity to claim-reference lookup. **User decision: a REAL store, not seeded mock claims** — seeded lookups were rejected because a live-filed claim would not be findable, so the loop would only appear to close. Open technical question to spike first: what a CES Python tool can authenticate to (outbound HTTPS with a shared secret is proven by `resolve_claim`'s Resend call; a Google-credentialed token for Firestore/GCS is unproven, with a Cloud Run service fronting the store as the fallback).

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmap: Deliverable is a spec, not code — phase success criteria describe properties of spec text (unambiguous, buildable, internally consistent), not running-software behaviors.
- Roadmap: 4-phase structure adopted directly from research/ARCHITECTURE.md's validated authoring-order dependency chain (Foundations → Component Architecture → Use-Case Specs → Runbook & Synthesis).
- [Phase 01-foundations]: Gap closure (01-05): inserted D-03/D-05/D-07/D-08/D-12/D-13 citation tokens at heading/field-row anchors matching existing Section 2/4 citation convention; no spec content rewritten

### Pending Todos

None yet.

### Blockers/Concerns

- **DEMO-DAY — THE SPANISH PHONE CALL HAS NEVER BEEN HEARD (2026-08-13, quick task 260813-nnm). Blocking user check outstanding, and it is a short one.** Voice v14 `5d02f14c` is live on `d28bbcb0` and answers a Spanish opener in Spanish — **proven on the deployment itself, but only over the TEXT/API channel.** A conversation record cannot tell you what `es-US` TTS *sounds* like. **Dial the number and listen for four things:** (1) does the voice sound like a **Spanish voice**, or like an English voice reading Spanish words — the `es-US` `synthesizeSpeechConfigs` entry is `{}`, i.e. platform default, and nobody has heard it; (2) does **barge-in still cut in** mid-Spanish-sentence (`bargeInAwareness: true` is byte-unchanged, but untested in Spanish); (3) **the decision sentence will arrive in ENGLISH and then be repeated in Spanish** — that is a known, recorded defect awaiting 05-09, not a surprise, but it is what a customer would hear today; (4) whether the audio channel renders `$840` sanely in Spanish, given it is already unstable in English (*"420 dollars"* vs *"420"*, 05-06). **If any of it is unpresentable, rollback is one call:** `deployments.patch` `updateMask=appVersion` → `5d9df25c-3771-45bb-bd20-b28978cc5955`.
- **OPEN QUESTION RE-OPENED — the mid-call EN→ES switch is untested, not impossible (2026-08-13, quick task 260813-nnm).** 05-03 recorded it as a closed negative on a wrong attribution; that attribution is corrected. Voice v14's instruction explicitly tells the agent to change language part-way through, but **no conversation has exercised it**. **One 2-turn probe settles it** (open in English, switch to Spanish, score the reply). Until someone runs it, **do not script a mid-call switch as a demo beat** — and note the two-back-to-back-calls fallback the project has been relying on was never actually verified either, and would have failed for the same reason the switch did.
- **SECURITY — ROTATE THE RESEND KEY (2026-08-13, quick task 260812-trc). User action, urgent, unrelated to the email defect.** A Resend API key value was surfaced in the executor's own diagnostic output during this task: a structural probe meant to print only argument *shapes* printed the `headers=` token of voice `send_case_record_email`, and **that tool carries the key as an inline string literal rather than behind a named constant**, so the value came out with it. It was masked in every subsequent output and **is not written anywhere in this repository** (`grep -rE 're_[A-Za-z0-9_]{20,}' .planning/` → no match), but it existed in a transcript. **Rotate in the Resend dashboard, then update the FOUR places it lives — `resolve_claim` and `send_case_record_email` on BOTH apps** (confirmed to be the same single key in all four). **When rewriting, put the key behind a named constant in both tools**: `resolve_claim` already does (`f"Bearer {RESEND_API_KEY}"`), voice `send_case_record_email` does not, and that difference is the entire reason it leaked. Do this in the same pass as the `resolve_claim` truthfulness fix so the tools are patched once, not twice.
- **DEMO-DAY RISK — the customer email is UNMONITORED and its delivery is UNPROVEN (2026-08-13, quick task 260812-trc). Blocking user check outstanding.** `resolve_claim` demonstrably reaches Resend and gets **HTTP 200 with a message id** — proven by an instrumented byte-identical copy run in the sandbox, on the same code the live phone deployment serves. But **200 is acceptance, not delivery**, the sender is the shared unverified `onboarding@resend.dev`, the recipient is a corporate mailbox, and the live `CLM-24430` email did not arrive while `CLM-24464` (same code, 24 h earlier) did. **THE ONE QUESTION THAT SETTLES IT: of the 13 emails this task sent, which arrived?** Specifically search for **`CLM-24224`, `CLM-24614`, `CLM-24646`** — three real customer claim emails, each answered 200. All three arriving means an intermittent drop; none arriving while `TRC-MARK-A`/`B` did means selective content/sender filtering and is a demo-day blocker. Also: check junk for anything from `onboarding@resend.dev`, count how many of the six `TRC-*` markers landed (only two were reported), and **open the Resend dashboard** to read the delivery log for `2026-08-13T02:21–02:23Z` — the embedded key is **send-only** (`GET /api-keys`, `/domains`, `/emails/{id}` all return `401 restricted_api_key`), so the dashboard is the only way to see status. **Likely real fix: verify a sending domain (SPF + DKIM) in Resend instead of `onboarding@resend.dev`** — a dashboard action, not a code change. **Until settled: do NOT build a demo beat on the customer opening the email live.** Budget is NOT the constraint — ~21/100 daily and ~92/3,000 monthly used at call time, ~1 email per auto-approved claim and ~2 per escalated one.
- **EVERY "customer email verified via `resolve_claim`" CANARY IN THIS PROJECT IS VOID AS DELIVERY EVIDENCE (2026-08-13, quick task 260812-trc).** This qualifies, but does not reverse, the 2026-08-11 single-send entry below. **`resolve_claim` cannot report a failed send, on BOTH apps.** It writes `email_delivery = "live"` and `email_status = "queued"` on the branch *before* the request, wraps the POST and a `raise_for_status()` in `except Exception`, contains **zero** references to `status_code` and **zero** to `.ok`, and returns `email_queued: true` as an unconditional literal. A 401, a 422, a 429, a DNS failure, a `status_code: 0` and a clean 200 therefore produce the **identical** observable. The standing instruction to assert the customer-send count via `resolve_claim` rather than `send_claim_email` is **still right about which tool to count** — `send_claim_email` is a keyless reporter and counting it is meaningless — but **wrong about what the count proves**: it proves `resolve_claim` ran once, not that an email left. Those canaries (05-05, 05-07, 260812-hhi, 260812-o5l) remain valid as single-send assertions and are worthless as delivery assertions. **The only delivery evidence this project has ever had is the inbox**, on four occasions: `CLM-24464` (voice v11), `CLM-24314` and `CLM-O5LPHOTO` (chat, both via `send_case_record_email`, which DOES return a real status code), and today's `TRC-MARK-A`/`B`. **FIX SPECIFIED BUT HELD** (see `260812-trc-SUMMARY.md`): keep the `try`/`except` so a mail failure can never break a live call, but assign `ok = getattr(resp, "status_code", 0) in (200, 201)` — never `.ok`, per 06-01 — drive `email_status`/`email_delivery`/`email_queued` from it, and **add `email_status_code` and a truncated `email_error` to the returned dict** so the conversation record carries the evidence. Change nothing the customer hears. Held because the send returns 200 (so the fix would not have prevented this failure), because the key rotation touches the same four tools and should be one pass, and because it needs two version cuts, two gated repoints and both canary batteries. Rollback targets: voice `5d9df25c`, chat `cdca14e3`.
- **METHOD WARNING — `x-resend-daily-quota` is NOT a send detector (2026-08-13, quick task 260812-trc).** This task nearly shipped a confident, brief-confirming, **false** conclusion ("`resolve_claim` sent zero emails while reporting success") derived from bracketing an invocation with two marker sends and reading the quota-header delta. A positive control caught it: the same bracket around a send **already known to have returned 200 with a message id** also read a delta of 1. **The header lags by exactly one send** — the full sequence reconciles to 31 exactly, which is only possible if every send counted. The header is usable for *budget* ("how much of today is gone") and useless for *attribution* ("did that specific call send"). **Never infer a send count from a delta of one, and never assert on a counter without a positive control.**
- **Phase 6 — RESOLVED, NOT A BLOCKER (2026-08-12, plan 06-01 task 3): the store choice and claim schema are LOCKED.** The user's literal reply was **`as-proven`**. Accepted explicitly: (a) the store is GCS via the S3-compatible XML API with a **GCS HMAC key embedded in tool source**, same risk class as the Resend key already in `resolve_claim`; (b) bucket `gs://meridian-claim-store-500614` and service account `claim-store-writer@insurance-agent-demo-500614.iam.gserviceaccount.com` (`roles/storage.objectAdmin` on that bucket only) are **permanent — do NOT delete them**; (c) the record schema and two-object key layout as written. **Rungs 3 (Cloud Run — costed, never built) and 4 (third-party host) are formally rejected and must not be re-litigated.** **Nothing needs deploying, no GCP service needs enabling, and 06-02 has no external dependency outstanding.**
- Phase 1: Audio-to-audio translation language set for mid-demo voice language switching is press-sourced only, not confirmed in current CX Agent Studio technical docs — needs console/stakeholder confirmation before the narrative commits to specific language pairs (see CAP-02, DATA-03, UC-03).
- Phase 1: Transcript → specialist-summary → drafted-email mechanics has no confirmed off-the-shelf Gemini Enterprise pipeline — must be spec'd as a custom in-session sub-agent chain, not an assumed built-in feature (see CAP-02, UC-07).
- Phase 2: Exact Handoff Rule UI/syntax and Python-tool session-variable mechanics are MEDIUM-confidence (docs shift on a 6-month-old product) — flag for live-console verification early in Phase 2 (see DEC-02, ARCH work).
- GTM: Competitive framing vs. Microsoft Copilot/Nuance is LOW-MEDIUM confidence — treat as stakeholder input (Scott/Srini/Stephanie/Hallie) to validate, not asserted fact (see NARR-03).
- Demo build: the customer-facing "FNOL Demo Narrative - V2.docx" describes a build that does not exist — it promises chat intake, photo upload, an EN↔ES switch, a $100 deductible, a $3,500 limit, a $1,000 threshold and $450/$2,400 claim amounts. The delivered agent is voice-only with a diagnostic tree, $25/$3,000/50%-of-coverage and $840/$3,000. Reconcile before the document goes to any customer or implementation team (Phase 5 scope).
- Demo build: the implemented PoC diverges from the Phase 1 Mock Data Appendix (§4) — tariff-based pricing, a 50%-of-coverage auto-approve threshold, $25 excess and no photo-upload path. §4's $1,000 threshold, $100 deductible and $3,500 limit are stale against what a caller actually hears. Reconcile §4 (or mark it superseded) before the spec ships to any other implementation team.
- Phase 5 (RESOLVED 2026-08-05): the model's vision accuracy on a real photo is confirmed — a cracked-screen image was read correctly in the simulator ("I can see several cracks across the screen"), confirmed, and auto-approved at $840. Plan 05-02 is unblocked. Original concern: Every branch of `assess_screen_crack` is tested, but only by supplying the observation values directly — the API can be driven with text, not with a convincing photo of a cracked screen. If it misreads a demo photo, the confirm path silently becomes the contradiction path in front of an audience. One upload through the deployed widget (`d7bfbb93`) settles it; blocks plan 05-02.
- Phase 5 ACCEPTED LIMITATION (resolved 2026-08-06, quick task 260806-u21): photo assessment cannot verify WHICH device is in the image. History: v4 asked the model for the device category, v5 asked for physical features (keyboard visible, hinged lid) and inferred in code. Both failed live: against PDP100746 (iPhone) the model reported keyboard_visible=no, hinged_lid=no and "a black phone" for an unmistakable MacBook — see evidence-laptop-photo.png in the phase dir, decoded from the conversation record. Root cause: the model knows what the policy covers and conforms its report to that, not to the pixels. Damage reporting is accurate; only identity is contaminated. DECISION (user): option (b) — a guard that does not work is worse than none, because it reads as protection in both the code and the runbook. Option (a) (agent-as-tool vision isolation) was not attempted. Removed in chat v6 `56a8b22a`, now served by deployment `d7bfbb93`: the `keyboard_visible`/`hinged_lid` parameters, the device-inference block, the `WRONG_SUBJECT` branch and the `photo_device_seen` variable are all gone; `assess_screen_crack(crack_visible, what_you_see)` asks about damage only. STANDING DEMO CONSTRAINT: photo assessment confirms whether the reported damage is visible; it cannot verify that the photographed device is the insured device — run demos with matching device/policy pairs (PDP100294 = MacBook, use a laptop photo). Unchanged and still working: the contradiction path (crack reported, none visible → photo_contradiction, DL-5, human review, non-accusatory), the one-retry-then-human path for unusable photos, and deterministic tariff pricing. A junk or subject-less photo now lands in unclear → retry → human rather than a rejection, the same end state. The photo CONFIRM path is now verified live through the real widget on chat v7 `bb14cdcc` (2026-08-09, quick task 260809-nt7): a real cracked-MacBook photo uploaded through the browser, read correctly ("I can see the cracks on the screen there."), priced from the tariff at $840 and auto-approved as `CLM-24442`. The CONTRADICTION path remains NOT VERIFIED LIVE, proven offline in phototest2.py only, and still needs a photo of an undamaged device through the widget.
- Phase 5 VERIFIED LIVE (2026-08-09, quick tasks 260809-n1b diagnosis + fix, 260809-nt7 verification): widget rendering. ROOT CAUSE: the `claim_decision_card` tool declared seven flat claim fields (`issue_label`, `claim_amount`, `deductible`, ...) as its `parameters`, and a widget tool's parameters ARE its emitted payload. The deployed web-widget SDK's `order_summary` renderer reads only `productItem`, `costBreakdown`, `paymentMethod` and `actions`, found all four undefined, and drew nothing — so the raw payload is what appeared. NOT an SDK limitation, NOT an agent defect, and NOT the console-Preview limitation it was originally attributed to. CORRECTION to the previous entry's claim that "payload shape is confirmed correct": it was well-FORMED (the platform tags it `"type": "order_summary"`) which is NOT the same as matching the renderer's contract — that inference is what kept the real cause hidden for a week. FIXED in chat v7 `bb14cdcc-d723-4be1-85af-9f4451e22ed5`, now served by deployment `d7bfbb93`: the card is a single `productItem` row (title/subtitle/price/imageUri), with `costBreakdown` deliberately dropped (its hardcoded, unsuppressable "Sales tax" row would print $0.00 on an insurance claim) and `actions` deliberately dropped (its labels are live buttons wired to sendQuery, so a presenter tapping one would inject it into the conversation). The decision and the $1,500 cutoff moved into the subtitle. VERIFIED LIVE (n1b): the emitted payload is byte-identical to what `resolve_claim` computes in Python, with `units` as an int — the model supplies no figure. The full SDK contract is written down in 05-02-SUMMARY.md and is reusable for every future widget. VERIFICATION RECORD (2026-08-09, quick task 260809-nt7): the user drove a full claim through the real, console-generated chat-messenger SDK v1.16 widget embed at `http://localhost:3000` with a token broker, against deployment `d7bfbb93` serving chat v7 `bb14cdcc`, and the card RENDERED. It drew title `Apple MacBook Pro 16"` with right-aligned price `$840.00`, and subtitle `Screen replacement · CLM-24442 · $840 less $25 excess = $815 to you · Approved on the spot (on-the-spot limit $1,500)`. The two predicted cosmetic artifacts appeared (an empty grey image tile at left, two divider rules with nothing after them) and none of the warned-of failures did (no `$4,200.00`, no `$NaN`, no `$0.00` row, no broken-image glyph). The run was the photo CONFIRM path with a matching device/policy pair (Jordan Rivera / PDP100294 / MacBook). The SDK `order_summary` contract in 05-02-SUMMARY.md is now verified-by-observation, not only read-from-source.
- Phase 5 (RESOLVED 2026-08-09, quick task 260809-nt7): browser file upload through the web widget needs no Cloud Storage bucket configuration. The `enable-file-upload` flag on the console-generated v1.16 embed was sufficient on its own — a real photo uploaded, displayed inline in the conversation and reached the model, with no storage config anywhere. An image-upload error seen earlier in the same session was a race against the in-flight redeploy, not a defect. This was the first time the photo path had ever been exercised through the real embed rather than the API or the simulator.
- Phase 5 FIXED AND FIRED (2026-08-10, quick task 260810-ifr, chat v8 `3f85b1d8-4810-44eb-85e6-39adc42593c9` now served by `d7bfbb93`): `cover_offer_actions`. It WAS broken the same way as the decision card — it declared `{prompt, options[{label,value}]}` while the SDK's `quick_actions` builder reads `{actions: [{content, description?, utterance?}]}`, and `b.actions.map(...)` throws on undefined rather than degrading to a JSON blob, so nothing would have appeared at all. THE KEY FINDING, and it corrects a week-old assumption: **it had never fired because it had never been REACHED, not because it was miswired.** All three prerequisites were already correct — the tool was attached to `claim_intake`, `uninsured_device` was a declared variable written by `verify_identity`, and the instruction had an explicit Cross-sell step naming the tool. Zero of 14 conversations had ever taken a turn PAST the email confirmation on the auto-approve path, which is structurally what the offer requires; the one that got closest (the user's 2026-08-09 run) ended on the email turn. The payload defect was real but entirely latent. Three further instruction defects were fixed at the same time, because a correct payload alone would still have left the buttons dead: (1) `end_session` was instructed in the SAME turn as the widget, so the session would end before anyone could tap — a new `<step name="Close after the offer">` now owns `end_session` and runs only after the customer answers; (2) "the buttons ARE the question" was provably false — the sentence lived in the `prompt` parameter, which renders nowhere, so following the instruction produced two naked buttons with no question; the agent now says the offer aloud; (3) `textResponseConfig: NONE` (= "the LLM decides whether to emit text") let the sentence vanish, so it is now `LLM_GENERATED` ON THIS WIDGET ONLY — the decision card's `NONE` was deliberately left untouched as the visually-verified known-good baseline. Mechanism note: `LLM_GENERATED` adds a REQUIRED `textResponse` parameter to the tool's input schema, described by `textResponseInstruction` — that is what makes the sentence unskippable. VERIFIED LIVE on session `f9c3999f-2c0d-4908-8a1d-c22971547bb4` through deployment `d7bfbb93`: the tool fired for the first time ever, emitting exactly `{actions:[{content:"Add it",utterance:"Yes please, add it to my cover."},{content:"Not now",utterance:"Not right now, thanks."}]}` and nothing else, alongside the agent text *"One more thing while I have you: your Apple iPhone 16 Pro Max isn't on this policy. Would you like me to add it to your cover?"*, with NO same-turn `end_session`; sending the button's own utterance then produced a warm close plus `end_session` one turn later, with no price quoted anywhere. The decision card, tariff pricing and single-send email were all unregressed on the same run. STILL PENDING: the buttons' on-screen render is NOT confirmed by eye, and the conversation record shows the offer sentence emitted TWICE (once as the model's own text chunk, once as the widget's delivered `textResponse`) — whether that doubles up in the browser is not determinable from the record and needs a human at `http://localhost:3000`. If it does, the fix is to stop instructing the agent to say the offer separately and let the mandatory `textResponse` be the only source.
- Phase 5 STANDING WARNING (2026-08-10, re-stated after quick task 260810-k5x): the chat app `a2f621e4` was last modified by DIRECT `apps.tools.patch` / `apps.agents.patch`, not by export→import. **Every exported package on disk is stale**, including `chat-fresh-k5x.zip`, which was taken before the k5x patches landed. Any future export→edit→import task must take a fresh `exportApp` first or it will silently revert the `cover_offer_actions` schema, the `claim_decision_card` `textResponseConfig`/`dataMapping`, and the `claim_intake` cross-sell AND decision-announcement instructions. Current live version: chat v9 `160dc3b2-571c-480f-b901-e4dbe8947f70`.
- Phase 5 FIXED (2026-08-10, quick task 260810-k5x, chat v9 `160dc3b2-571c-480f-b901-e4dbe8947f70` now served by `d7bfbb93`, read back not inferred): **the silent decision turn.** Previously the agent drew the card and said nothing (chat v7) or said a figure-free paraphrase (chat v8) — non-deterministic, so a presenter could not depend on it, and the rule-and-why lived only in the card's small grey subtitle. TWO causes, both fixed together. (1) `claim_decision_card` had `textResponseConfig: NONE`, which per the CES discovery doc means "the LLM dynamically decides whether to generate a text response", NOT "no text" — the sentence went into the `summary` parameter, which renders nowhere. Now `LLM_GENERATED` with a `textResponseInstruction` pinning it to a word-for-word reproduction. (2) `claim_intake`'s decision-announcement block was self-contradictory — read the explanation WORD FOR WORD, then "do NOT repeat them in your own message - say the short human part only". **Resolved in favour of verbatim** per the user's locked decision; the contradicting clauses were DELETED and a greppable sentinel added: `THE CARD TEXT RESPONSE IS THE ONLY PLACE YOU SAY THE DECISION - do not also send a message of your own in this turn, or the customer hears it twice.` Instruction 18,310 → 18,496 chars, exactly one contiguous region changed. **THE MECHANISM THAT MATTERS:** the `dataMapping` `FIELD_MAPPING` entry `"textResponse": "explanation"` was **ACCEPTED by the API** — so the platform copies `resolve_claim`'s string into the widget's required text-response parameter and the model composes nothing. That is deterministic, not prompt-dependent. VERIFIED LIVE on session `dda81738-832f-455d-bc91-42f7abb3e308`, run against the DRAFT APP before any repoint (a `runSession` with no `config.deployment` is accepted, so the demo rig was never exposed to an unverified build): the spoken line was *"Good news - that keyboard replacement comes to $420, which is under the $1,500 I can approve on the spot, so I can approve that for you right now. Your excess is $25, and your reference is CLM-24005."* — **byte-identical** to `resolve_claim`'s `explanation`, and emitted **EXACTLY ONCE**. The `260810-ifr` duplication trap did NOT reproduce: the decision turn holds two text chunks total (the customer's own message, and one widget-delivered chunk), and the model emitted no text chunk of its own. The repoint was made CONDITIONAL on that count being 1. Unregressed on the same run: `productItem` byte-identical to `card_product_item` with `price.units` an int 420, `cover_offer_actions` byte-identical, tariff $420/$25/$1,500, `send_claim_email` in exactly one turn, 8 tools / 37 variables. STILL PENDING: nobody has watched the spoken line draw in a browser — specifically whether it prints once or doubles above the card. Also note the `summary` parameter is still filled and still renders nowhere; harmless.
- Phase 5 PROCESS NOTE (2026-08-10, quick task 260810-k5x): two executor sessions died from an API transport error while composing the 18,310-char `claim_intake` instruction **inline in tool-call arguments**. Volume of generated text was the cause, not the edit itself. The technique that worked: a Python script written to the scratchpad that GETs the agent, does an exact `str.replace()` of only the small contradictory region, asserts (unique anchors, six content assertions, ±1,200-char delta bound), PATCHes and reads back byte-for-byte — printing only lengths, booleans and short excerpts. **Anyone editing a large instruction on this project should do it by scripted replacement, never by re-typing the instruction inline.**
- Phase 5 (resolved): CES quota is the binding constraint on testing. `RunSession LLM tokens` defaults to 1,000/min per project/region/model while a single conversation costs 120k-150k input tokens (3.5k-10.8k per turn). Raise it in IAM → Quotas before any demo; also `Concurrent BidiRunSession Operations` (10 per 30 min) if more than one person will use the phone.
- Phase 5 DRAFT-ONLY FIX, LIVE DEMO STILL DEFECTIVE (2026-08-11, quick task 260811-suy): **the agent re-asks a question the customer has already answered.** Observed on the REAL GTP call (conversation `081cCNZtVwgSGqmfMpFSpbxMQ`, `channelType: AUDIO`, `source: LIVE`, deployment `d28bbcb0` on v11 `b17c9a26`): after the customer answered *"does it still switch on and respond normally?"* with *"It does."*, the agent called `resolve_claim` — guessing `diagnostic_outcome: "REPAIRABLE"` — instead of `run_diagnostic`. The tool correctly refused with `DIAGNOSTIC_INCOMPLETE` (**working hardening, deliberately NOT weakened**), and the agent recovered by re-interrogating the customer, costing one turn and ~11 s of a 132 s call immediately before the moment the demo is meant to shine. ROOT CAUSE: neither app's `claim_intake` contained the string `DIAGNOSTIC_INCOMPLETE` at all — there was no instruction for what to do when the guard fires. FIXED IN THE DRAFT of BOTH apps by scripted `str.replace()` + `apps.agents.patch` (read-verify-patch-verify, byte-for-byte read-back): one paragraph inserted after the unique anchor `stop asking and let the tool escalate.`, telling the agent to call `run_diagnostic` again immediately carrying every answer it already has and never to re-ask an answered question to recover. **+507 chars on both** — voice `6e01e4a5` agent `87551704` 14,140 → 14,647 (sha `628548c1…` → `02b1e6ae…`), chat `a2f621e4` same agent id 18,496 → 19,003 (sha `79b3c6bd…` → `acf62e78…`). VERIFIED NON-REGRESSIVE on one draft conversation (`suy-verify-563ed61c`): no repeated question, diagnostic terminal, `AUTO_APPROVE` $840/$25/$1,500 `CLM-24437`, and the `DECISION_SPEECH_EN` canary holds (one agent chunk, byte-identical to `resolve_claim.explanation`). **CAVEAT: the recovery branch itself was NOT exercised** — the model took the correct path, so the guard never fired; the premature call is non-deterministic and cannot be forced from the client. **NO VERSION WAS CUT AND NEITHER DEPLOYMENT WAS REPOINTED** (per instruction — 05-07 will cut voice v12 and can carry this). Voice `d28bbcb0` still serves v11 `b17c9a26`; chat `d7bfbb93` still serves v9 `160dc3b2`. **The live phone demo and the live chat demo still exhibit this defect.** Repoint decision left to the user.
- Phase 5 RESOLVED — NOT A DEFECT (2026-08-11, quick task 260811-tgm): **the voice cross-sell FIRES.** This entry supersedes and reverses the 260811-suy entry immediately below, which asserted a "probable genuine gap"; that conclusion was wrong and must not be carried into 05-07 or 05-09. EVIDENCE: conversation **`103r6XO3ZXjS3S97D-AtLK1ag`** on voice app `6e01e4a5-42a8-5213-b3da-c9053ff8ea52`, `2026-08-12T02:06:37Z` → `02:08:35Z` (1m58s), `channelType: AUDIO`, `source: LIVE`, deployment `d28bbcb0` serving v11 `b17c9a26`, policy PDP100583, 9 turns — the same configuration as the suy call, placed 29 minutes later. At turn 8 the agent said, verbatim: *"Please reply to that email with photos of the damage. Once those are in, allow three to seven business days for a representative to confirm everything. **Also, I see you have an HP Pavilion 15 that isn't covered yet – would you like to add that to your policy?**"* The customer said *"Uh sure."*, the agent replied *"Great, I'll have someone send over the options for that."* and closed with `end_session`. **BOTH PRIOR NEGATIVES WERE FALSE NEGATIVES — neither conversation ever reached the beat.** Re-reading `081cCNZtVwgSGqmfMpFSpbxMQ`'s own record: it delivered the send-away line at its turn 8, the caller said *"¿Qué?"* at turn 9, and the agent went straight to the sign-off — the call ENDED before the offer's slot rather than skipping it. The `runSession` observation failed the same way on an early *"No thanks"*. This is exactly the failure the runbook's own presenter note warns about (*"Don't say 'no thanks' early"*), so 05-06's explanation (1) — presenter script, not capability — was right, and explanation (2), the "v11 capability gap", is disproven on v11 itself. TWO SHAPE FACTS for the runbook and 05-07: (a) the offer is appended to the send-away instructions **inside the same agent turn**, not delivered as a turn of its own — the runbook's "three separate turns" phrasing is inaccurate and no separate beat should be scripted or waited for; (b) it fired on the turn **immediately after a barge-in**, so interruption does not suppress it. **No retest outstanding. Not a blocker. The "cost centre → profit centre" moment works on live voice v11.**
- Phase 5 VERIFIED — BARGE-IN WORKS (2026-08-11, quick task 260811-tgm): open item 4 of `05-06-VOICE-BASELINE.md ## PHONE_CHECK` ("does talking over it interrupt?") is **CLOSED, PASS**. In conversation `103r6XO3ZXjS3S97D-AtLK1ag` (`2026-08-12T02:06:37Z`, AUDIO/LIVE, deployment `d28bbcb0`, v11 `b17c9a26`) the caller talked over the agent mid-sentence and the platform recorded the marker verbatim on the next user turn: `<context>agent speaking was interrupted. user only heard 'Since we've' in the last agent response.</context>`. The interrupted agent text was *"Since we've approved that, an email is already on its way to the address we have on…"* — cut off exactly where the marker says. TWO results, and the second is the interesting one: (1) barge-in is live and effective, so `bargeInConfig` in `## GTP_SURFACE` is doing its job; (2) the agent knows **how much** the caller heard and **recovered by re-delivering the missed content** — the following turn restated the reply-with-photos instruction and the 3–7 business-day expectation before appending the cross-sell, without the caller having to ask. DEMO IMPLICATION: interrupting the agent is safe and is itself a credible wow beat; nothing is lost when a presenter cuts in.
- Phase 5 — 05-09 MUST READ (2026-08-11, quick task 260811-tgm): **`spoken_equals_explanation: true` holds on the TEXT/API channel only, NOT as a byte comparison on AUDIO.** The relayed decision text is normalised for speech on the audio channel, and the normalisation is **not stable between runs of the same version**: the pinned `DECISION_SPEECH_EN` / `resolve_claim.explanation` string carries `$420`; live audio call `081cCNZtVwgSGqmfMpFSpbxMQ` (`2026-08-12T01:37:41Z`) spoke *"420 dollars"*; live audio call `103r6XO3ZXjS3S97D-AtLK1ag` (`2026-08-12T02:06:37Z`) spoke *"420"* — same app, same deployment `d28bbcb0`, same version v11 `b17c9a26`, 29 minutes apart. The `$` sigil and the `CLM-` hyphen are likewise dropped on audio (`CLM-24464` → `CLM 24464`). **CONSEQUENCE: 05-09's Spanish byte-identity assertions MUST run against the text/API channel; an audio-channel byte comparison WILL fail spuriously** and will look like a paraphrase/self-narration regression when nothing is wrong. If an audio-channel check is needed, assert on semantic content (the four substantive fields present, in order), never on bytes. This does not weaken the underlying determinism result — `relay_mismatch: false` on the tool response, and the audio wording tracks the tool string clause-for-clause; the variance is TTS-side text normalisation, not the model rewriting.
- Phase 5 CLOSED — the "possible double email" question is RESOLVED, single-send HOLDS (2026-08-11, closed during plan 05-05; raised by quick task 260811-tgm): the user checked the inbox and **exactly ONE email arrived for `CLM-24464`**, despite `send_claim_email` being called on two turns (7 and 8) of conversation `103r6XO3ZXjS3S97D-AtLK1ag`. Both calls were **idempotent reports of the one decision-time send**, exactly as the record suggested — the invariant was holding, not breaking. **The v1→v11 single-send guarantee is intact and is now confirmed by eye, not only inferred.** THE MECHANISM, which is the part that matters going forward: **`resolve_claim` sends the customer email; `send_claim_email` does not.** `send_claim_email` holds no Resend key, makes no HTTP call, and is a ~1,705-char pure reporter that replies *"Email already sent when the claim was decided."* Established from 15 chat conversation records in 05-04 and reconfirmed on voice. **CONSEQUENCE for every future plan: assert the customer-send count via `resolve_claim` (one invocation, `email_queued: true`), NEVER via `send_claim_email`'s call count** — the latter is decorative and can legitimately appear zero, one or two times without the customer's mail count changing. Re-verified on both of 05-05's live runs: `resolve_claim` fired exactly once with `email_queued: true` on the escalated run (`CLM-24413`) and on the auto-approve run (`CLM-24774`). Folds in and closes the older "how many mails actually landed" open item.
- Phase 5 NUANCE — the repeated diagnostic question is INTERMITTENT, not systematic (2026-08-11, quick task 260811-tgm): conversation `103r6XO3ZXjS3S97D-AtLK1ag` ran on **live v11 `b17c9a26`, which does NOT carry the 260811-suy draft fix**, and the diagnostic completed cleanly — `q2` supplied at T4 → tool asks `q1` → `q1=screen` at T5 → tool asks `q3` → `q3=works_normally` at T6 → `terminal REPAIRABLE`, `questions_asked: 3`, `repeat_of_previous_question: false` on every response, no `DIAGNOSTIC_INCOMPLETE`, no wasted turn. TWO CONSEQUENCES: (a) **live-demo risk is lower than the 260811-suy "DRAFT-ONLY FIX, LIVE DEMO STILL DEFECTIVE" entry above implies** — the failure depends on whether the model jumps to `resolve_claim` before the diagnostic reaches terminal, and is a coin-flip across runs of the same version, not a certainty; (b) **the 260811-suy draft fix CANNOT be validated by absence of the symptom**, because a clean run already occurs pre-fix — validating it requires asserting on recovery behaviour under an induced `DIAGNOSTIC_INCOMPLETE`, not on "we called and it didn't repeat."
- ~~Phase 5 (2026-08-11, quick task 260811-suy)~~ **SUPERSEDED 2026-08-11 by the 260811-tgm entry above — the conclusion in this entry is WRONG and is retained only so the reversal is visible:** the voice cross-sell gap is now **probable, on two independent observations** — but still one scripted retest short of proven. The real GTP call reproduced `cross_sell_fired: false` on a LIVE AUDIO call with `uninsured_device` populated (`HP Pavilion 15`), the claim auto-approved, and a neutral *"Okay."* from the customer after the email confirmation — the natural opening, and NOT the `"No thanks"` refusal that 05-06's explanation (1) blamed. Zero cross-sell prose hits across every agent chunk. This substantially weakens the "artifact of how a text `runSession` drives turns" hedge, since a live call CAN volunteer an unprompted turn. NOT yet a proven defect: the caller said *"Okay."*, not the scripted *"That's everything, thanks"*, and did not hold the 10 seconds of silence. The 3-minute retest that would settle it is unchanged and still outstanding. This matters commercially — the cross-sell is the "cost centre → profit centre" wow moment, and it has now failed to appear on both channels' voice path twice.
- Phase 5 SHIPPED — chat v10 `658472a0-05be-4a28-9d9f-9774ebe0dd05` is LIVE on `d7bfbb93` (2026-08-11, plan 05-05, read back not inferred): **the assessor briefing packet.** An escalated chat claim now composes a six-section packet (`SUMMARY` / `ACTION` / `CLAIM` / `DIAGNOSTIC` / `RULES FIRED` / `FLAGS`) from session state via the `case_summary` agent-as-tool, and delivers it as a **second, separate email** — subject `[ASSESSOR] CLM-24413 - Jordan Rivera`, to `akash.vinayak@nerdery.com`, `from` unchanged at `onboarding@resend.dev` (per the `same-mailbox` decision; both emails land in one inbox, told apart by the subject prefix). Resend returned **HTTP 200**. `claim_intake` 19,003 → 20,505 chars (+1,502, exactly one contiguous region), 8 → 10 tools, patched by scripted `str.replace()` with byte-for-byte read-back — **the plan's asserted pre-edit length of 18,496 was stale; 19,003 is correct** because the 260811-suy `DIAGNOSTIC_INCOMPLETE` fix is in the draft. PROVEN LIVE on the DRAFT before any repoint, session `esc-25e925f8`, all nine assertions passing: `HUMAN_REVIEW`/$3,000/`DL-3`+`DL-2`; `generate_case_summary` once; six headings present; **zero `{placeholder}` braces**; `send_case_record_email` once with `packet_text` **byte-identical** (520 == 520) to the composer's response; `sent: true` / `200` / `[ASSESSOR]` prefix; one customer send via `resolve_claim`; **zero leakage** — no agent text chunk anywhere mentions the packet, an assessor or any heading, and the packet turn emits no customer-facing text at all; card `productItem` byte-identical to `card_product_item`; no cross-sell on the escalation path. **⚠ TURN-BOUNDARY FINDING, matters for the demo and for 05-07:** the packet is filed on the turn **AFTER** the email confirmation. The first attempt stopped at the email turn and filed nothing. This is a **pre-existing** property of `claim_intake`'s email step (*"This is its own turn and ends with the last word of it"*) which already stranded `escalate_to_human`/`end_session` behind the same boundary — not a new defect. Fixed by giving it one more customer turn, NOT by weakening the verified clause; recorded in bold in the runbook. Also shipped in v10, both asserted present in the snapshot and both proven non-regressive to English on a live run: the **260811-suy repeated-question fix** (so the live CHAT demo no longer has it; the live PHONE demo still does, voice `d28bbcb0` still serves v11 `b17c9a26`) and **05-03's `supportedLanguageCodes: ["es-US"]` + `enableMultilingualSupport: true`** (English run: 0 Spanish markers, 24 English stopwords, `languageCode` `en-US` — consistent with the `LOCKS_AT_FIRST_UTTERANCE` verdict). ENGLISH AUTO-APPROVE CANARY on session `appr-0c70bb34`, run before the repoint and the repoint gated on it, ALL PASS: $420/$25/$1,500 `CLM-24774`, card `productItem` byte-identical with `units` int, spoken decision byte-identical to `resolve_claim.explanation` and emitted **exactly once**, `cover_offer_actions` fired with the correct `{actions:[…]}` shape, and **the packet tools fired ZERO times on the approve path** with no `NO_PACKET_REQUIRED` anywhere — the instruction-level gate stopped it before `case_summary`'s own fallback was needed. Rollback is chat v9 `160dc3b2-571c-480f-b901-e4dbe8947f70`; the `curl` and a chat version table are now in DEMO-RUNBOOK.md under *If something goes wrong (chat)*. **STILL UNCONFIRMED BY EYE: neither email has been seen in the inbox, and nobody has watched v10 in a browser** — the three on-screen items 05-02 left open (spoken line once vs twice, card render, cross-sell buttons) remain open, all byte-correct server-side and unchanged since v9.
- Phase 5 FIXED AND SHIPPED — chat v12 `26c3aebd-d72b-4ec5-861d-8a9fabb140cf` is LIVE on `d7bfbb93` (2026-08-12, quick task 260812-hhi, repointed `658472a0` → `26c3aebd` at `2026-08-12T18:38:25Z`, read back not inferred): **the assessor packet now files on the SAME turn as the email confirmation, on BOTH apps.** THE DEFECT: chat conversation `dfMessenger-ca0d435b` (v10 `658472a0`, WEB_UI, PDP100583, 4 turns) escalated correctly — `TOTAL_LOSS`, `DL-3`+`DL-2`, `CLM-24625`, `$1,500` — and filed **no packet at all**; neither `generate_case_summary` nor `send_case_record_email` appears anywhere in the record. Cause: 05-05 wired the packet to the turn AFTER the email confirmation, and the customer said "ok", got the send-away line, and stopped — the natural end of the conversation. **The deeper problem was that the turn the email lands on is not fixed:** in 05-05's passing test the email landed on turn 2 and the packet on turn 3; in the wild run the email landed on turn 3 and the packet needed a turn 4. It fired only when the conversation happened to continue — the *unlikely* case for a demo — and failed silently. THE FIX: file it on the email-confirmation turn, which on an escalated claim ALWAYS happens, and where the agent is already speaking a send-away line so the two tool calls land while the customer is being talked to rather than waiting in silence. Two alternatives rejected and recorded: keeping it on a later turn (proven fragile above), and filing it on the decision turn (05-07's voice design — it puts two tool calls in front of the customer hearing their outcome, which on a phone call is dead air at the worst moment). PROVEN LIVE on session `esc4-68e580e1` against the DRAFT, **3 customer turns and NOTHING further** — the conversation ends exactly where the failing one did: `send_claim_email` → `generate_case_summary` → `send_case_record_email` → `escalate_to_human` → `end_session`, all on turn 3; `HUMAN_REVIEW`/$3,000/`DL-3`+`DL-2`/`CLM-24041`; six headings each exactly once; zero `{placeholder}` braces; `packet_text` **byte-identical** (477 == 477) to the composer's response; Resend **HTTP 200**; subject `[ASSESSOR] CLM-24041 - Jordan Rivera`; one customer send via `resolve_claim`; **zero packet vocabulary in any agent text**. The user independently confirmed the mailbox on the earlier session `esc-94a67cc8` (`CLM-24314`): the `[ASSESSOR]` email arrived with all six sections and the **REAL customer name** — the conversation record redacts it, so the inbox was the only possible proof. ENGLISH AUTO-APPROVE CANARY (`appr-25ea0080`, 4 turns, run before the repoint and the repoint gated on it) ALL PASS: $420/$25/$1,500 `CLM-24347`, card `productItem` byte-identical to `card_product_item` with `units` int, spoken decision byte-identical to `resolve_claim.explanation` and emitted **exactly once**, `cover_offer_actions` fired with the correct `{actions:[…]}` shape, and the packet tools fired **ZERO** times with no `NO_PACKET_REQUIRED` anywhere. `claim_intake` 20,505 → 22,148 chars (+1,643), tools unchanged at 10. Rollback is chat v10 `658472a0`; the runbook's version table and rollback `curl` now name it.
- Phase 5 BONUS FINDING, and it is more serious than the packet defect (2026-08-12, quick task 260812-hhi): **`escalate_to_human` and `end_session` were stranded behind the SAME turn boundary, so the human handoff never actually fired on an escalated chat claim.** They sit at the end of the same instruction chain (*"only once the case record is filed call `escalate_to_human` and immediately `end_session`"*), so every escalated conversation that ended at the email line — including the wild `dfMessenger-ca0d435b` run — signalled **no** escalation and **never closed the session**. The claim looked escalated to the customer, who heard the specialist handoff, but no `session_escalated` signal was ever emitted. 05-04 had found a HUMAN_REVIEW conversation where `send_claim_email` was never called at all, and 05-05 recorded the pair as "stranded", but neither treated it as a live defect. **It was one.** Chat v12 fixes it as a side effect: on `esc4-68e580e1` both fire on turn 3 and the conversation closes cleanly with no further customer input.
- Phase 5 REGRESSION CAUGHT BEFORE SHIPPING — chat v11 `838b6d2b-1e9f-44f8-b319-941c3e4ea10b` WAS CUT AND MUST NEVER BE DEPLOYED (2026-08-12, quick task 260812-hhi): moving the packet onto the email turn initially **silenced the entire turn**. Sessions `esc-94a67cc8` and `esc2-6df302c0` both filed the packet on the correct turn and emitted **no customer-facing text at all** — the send-away line vanished, so the customer's last words from the agent would have been the decision alone. ROOT CAUSE: the Case record step's sentence *"The only thing they hear on this path is the specialist handoff you have already given them, unchanged"* was written when the step owned its own turn; sharing the email turn, it reads as an instruction to say nothing in that turn. FIXED by deleting that sentence and adding an explicit ordering rule — *"the message to the customer comes FIRST … before you call a single tool in this turn"* — reinforced in the `Price and decide` HUMAN_REVIEW bullet. Verified on `esc4-68e580e1`: the send-away line is spoken, once, naming the 3-to-7-business-day expectation. **GENERAL LESSON for any future turn-merge on this project: a silence clause written for a solo turn becomes a gag order when that step is merged into a speaking turn — re-read every "the only thing they hear" style sentence before merging steps.** `d7bfbb93` was never pointed at v11; the runbook's version table marks it *"Never deployed. Do not roll onto it."*
- Phase 5 VOICE WIRED BUT DELIBERATELY UNDEPLOYED — 05-07 OWNS THE CUT (2026-08-12, quick task 260812-hhi): voice `6e01e4a5` `claim_intake` now has the `Case record` step for the first time, using the same-turn design above rather than 05-07's planned decision-turn splice. **Instruction 14,647 → 18,215 chars (+3,568), tools 5 → 7** — `generate_case_summary` and `send_case_record_email` **attached**, which was required: `updateMask=instruction` alone leaves the step unreachable (chat needed the identical 8 → 10 in 05-05). Voice's step carries a stronger silence posture than chat's (*"none of it may reach text to speech"*, plus no-narration and no-holding-phrase clauses) because on a call a leak is audible. Verified by read-back only — 30 assertions, all pass: step order, same-turn trigger, message-first ordering, packet sentinel, no photo vocabulary, `DIAGNOSTIC_INCOMPLETE` intact, escalation chain intact, all 33 `variableDeclarations` and every `case_summary` `{token}` resolving on the VOICE app. **NO VERSION WAS CUT AND `d28bbcb0` WAS NOT REPOINTED — it still serves v11 `b17c9a26`, `updateTime` still `2026-08-05T17:00:57Z`, and the newest voice version is still v11.** Plan 05-07 owns the voice version cut, the live-call verification and the repoint. **05-07's Task 2 splice design is superseded by this task** — the step is already written; 05-07 should verify and ship it, not re-author it.
- Phase 5 PLATFORM TRAP (2026-08-12, quick task 260812-hhi): **the voice `claim_intake` instruction uses CRLF line endings (176 CRLF + 7 bare LF); the chat one is pure LF.** Two consequences that have to be carried into every future instruction edit. (1) **Anchor strings cannot be copied between the two apps** — a chat anchor will never match in voice, and vice versa. Derive anchors by slicing them out of the target app's own bytes. (2) **A text-mode file round-trip silently mangles voice's instruction**: writing it to a `.txt` and reading it back collapses `\r\n` to `\n` and loses 176 characters, which then fails every length and hash gate for reasons that look like server drift. All voice instruction handling in this task went through JSON, never a text file.
- Phase 5 KNOWN AND NOT FIXED — TWO PACKET DEFECTS, 05-07 OWNS BOTH ON BOTH APPS (2026-08-12, quick task 260812-hhi, found by the user in the delivered email): (1) **currency renders as a raw float** — the packet reads `Amount: 3000.0; Excess: 25.0`, with no `$` and no thousands separator; it should read `$3,000` and `$25`. Cosmetic but it is the artifact the "backend claims-processing reveal" puts on screen. (2) **no channel token in the chat subject** — `[ASSESSOR] CLM-24314 - Jordan Rivera` carries no channel marker, so once voice ships its packet the two are indistinguishable in the shared mailbox, which is precisely what the `same-mailbox` decision requires them not to be. **Voice's `send_case_record_email` already composes `[ASSESSOR] [PHONE] {claim_ref} - {customer_name}`; chat has no matching marker.** Both are recorded in `DEMO-RUNBOOK.md` under the packet section as presenter warnings.
- Phase 5 SHIPPED — voice v13 `5d9df25c-3771-45bb-bd20-b28978cc5955` is LIVE on `d28bbcb0` and chat v13 `1eb3fd5c-5aff-46c8-b572-e3fe18bf966f` is LIVE on `d7bfbb93` (2026-08-12, plan 05-07, both read back not inferred; voice repointed `b17c9a26` → `5d9df25c` at `2026-08-12T20:51:46Z`, chat repointed `26c3aebd` → `1eb3fd5c` at `2026-08-12T20:51:48Z`, `channelProfile` byte-identical on both): **the assessor briefing packet now works on the PHONE, and both channels' packets are presentable.** This is the **first change to the voice app's live deployment since 2026-08-05**. THE DEFECT THIS PLAN FOUND BY RUNNING IT, and it is the important part: **the voice packet mailer had never delivered a single email.** `send_case_record_email` on voice passed `timeout=20` to `ces_requests.post(...)`; the platform's HTTP shim does not accept that kwarg, so every send raised and the tool's own `except` branch returned `{"sent": false, "status_code": 0, "error": "Requests.request() got an unexpected keyword argument 'timeout'"}` — **silently, without breaking the call**, which is exactly the behaviour it was designed for and exactly what made it invisible. Chat's equivalent tool (05-04) has no such kwarg and has returned 200 since. 05-07's Task 1 said *"Send no test email in this task"*; quick task `260812-hhi` verified the voice wiring by **read-back only** (30 assertions, all passing, none of which execute the tool). **The first thing that could have caught it was calling it, and 05-07 is the first thing that did.** GENERAL LESSON: a tool that swallows its own exceptions cannot be verified by reading it — any future change to `send_case_record_email` must be proven by one live invocation. Fixed by an anchored `str.replace()` removing the kwarg (−24 chars, one contiguous region, `compile()`-checked, key count still 1, byte-identical read-back). **Voice v12 `9227210b-e46b-41bd-af7d-59db48abb3a6` was cut BEFORE that fix and must NEVER be deployed** — it carries the broken mailer; v13 supersedes it, and the runbook's phone version table marks it *"Never deployed. Do not roll onto it."* ALSO SHIPPED, the two presentation defects the user found in a delivered packet: (1) **currency** — the packet read `Amount: 3000.0; Excess: 25.0` and now reads **`$3,000`** and **`$25`**, via a `MONEY FORMATTING` rule added to the `case_summary` instruction on BOTH apps (+697 chars each, anchored on each app's own CLAIM line, one contiguous region, byte-identical read-back); (2) **channel token** — the packet subject is now **`[ASSESSOR] [VOICE] CLM-24xxx - Jordan Rivera`** and **`[ASSESSOR] [CHAT] CLM-24xxx - Jordan Rivera`**. CHOICE AND REASONING: a second bracketed word rather than the suggested `[ASSESSOR·VOICE]`, because it keeps the `[ASSESSOR]` prefix **byte-identical** so every existing assertion, runbook line and mailbox filter still works; it is pure ASCII (`·` is not, and is not greppable from a terminal without care); `VOICE`/`CHAT` are the two words this project already uses for the two apps everywhere, whereas voice's pre-existing `[PHONE]` was a third word for the same thing; and the two equal-length tokens keep the claim ref at a near-constant column so a mailbox list stays scannable. Voice's `[PHONE]` → `[VOICE]` was a delta-0 in-place swap. LIVE PROOF, voice session `s07v-esc-a2` (3 customer turns, DRAFT, ending exactly where a real caller would stop), **28/28 gated assertions**: `HUMAN_REVIEW`/`$3,000`/`DL-3`+`DL-2`/`CLM-24257`; `generate_case_summary` once; six headings each once; **zero `{placeholder}` braces**; `packet_text` **byte-identical** (434 == 434); Resend **HTTP 200**; subject `[ASSESSOR] [VOICE] CLM-24257`; **exactly one customer send asserted via `resolve_claim`** (`email_queued: true`, never via `send_claim_email`); **zero packet vocabulary in any agent-spoken text** — no heading, no "assessor", no "case record", no "packet". Chat session `s07c-esc-d1` likewise **28/28** with `$3,000`/`$25`, `[ASSESSOR] [CHAT] CLM-24858`, Resend **200**, 518 == 518. **THE v11-REGRESSION CANARY HELD: the send-away line is still spoken** — 250 chars on voice, 224 on chat, and the conversation record shows the TEXT chunk emitted **before any tool call** on that turn, which is the message-first ordering rule doing its job. **WHAT HAPPENS IF A CALLER HANGS UP AFTER THE EMAIL CONFIRMATION: nothing is lost.** The packet, `escalate_to_human` and `end_session` all complete inside that same turn, server-side, before the caller could react to the silence — proven on a 3-turn conversation that ends exactly there, with no further customer input of any kind.
- Phase 5 CANARIES AND MEASURED LATENCY for the voice repoint (2026-08-12, plan 05-07): **the cross-sell FIRES on voice v13** — session `s07v-appr-b1`, 13/13 gated canaries, *"While I have you, I see you also have an Apple iPhone 16 Pro Max - would you like to add that to your cover?"* This is the first time the beat has been observed on the voice **text/API** channel; it arrived as its own turn after a neutral *"That's everything, thanks"*, whereas live audio (`103r6XO3ZXjS3S97D-AtLK1ag`) appended it to the send-away line in the same turn — both shapes are the beat working, and both confirm the runbook's *"don't say 'no thanks' early"* note is the real constraint. Also unregressed on that run: `AUTO_APPROVE`, deterministic tariff **840/25/1500** with `rules_fired == ["DL-1"]`, `resolve_claim` once with `email_queued: true`, `send_claim_email` in exactly one turn, and the **`DECISION_SPEECH_EN` canary byte-identical on the TEXT/API channel** — one agent chunk, 197 chars, `spoken == resolve_claim.explanation` exactly, matching the 05-06 baseline string modulo the per-run `CLM-` digits (asserted on text only, per the 2026-08-11 audio-normalisation warning). The packet tools fired **ZERO** times on the approve path with no `NO_PACKET_REQUIRED` anywhere — the instruction-level gate stopped them before `case_summary`'s own fallback was needed. **MEASURED ESCALATION-TURN LATENCY: 3.30 s on v11 → 9.53 s on v13, +6.2 s.** `05-06-VOICE-BASELINE.md` records no per-turn wall clock, so the baseline was taken by running the identical Scenario B script against the **LIVE v11 deployment** `d28bbcb0` (conversation `s07v-v11base-c1`, `appVersion b17c9a26`) 40 minutes before the repoint — a true like-for-like control rather than an undated number. v11's escalation turn calls `send_claim_email`+`escalate_to_human`+`end_session` and emits 198 chars; v13 adds `generate_case_summary`+`send_case_record_email` and emits 250 chars. **Whether that +6.2 s is audible is NOT settled by this measurement** — the agent speaks the send-away line before it calls any tool, and 250 characters is roughly 16 s of speech, so the tool work should run under the caller's ear; but that is an inference about the GTP audio pipeline, not an observation. **A human on the phone is the judge, and v11 `b17c9a26` is the named one-call rollback.**
- Phase 5 CLOSED — the chat cross-sell BUTTONS RENDER, confirmed by the user on chat v13 (2026-08-12): conversation `dfMessenger-05b6446b-d6c4-4faf-9ac3-3e0b29d7a0cd` (`2026-08-12T22:12:34Z`, MULTIMODAL, PDP100583, 9 turns) drove the full happy path through the real widget — photo uploaded → `assess_screen_crack` → `CRACK_CONFIRMED` → **$420** under **$750** → decision card → single email → cross-sell — and **the user tapped the `Add it` button**: turn 8 is `"Yes please, add it to my cover."`, byte-identical to the configured `utterance`. A tap is only possible if the button drew, so **05-02's last open on-screen item is CLOSED**; the user separately confirmed *"the card renders fine."* On the same run **the send-away line was still spoken**, so the chat v11 `838b6d2b` turn-silencing regression did **not** recur on v13. (v11 `838b6d2b` remains NEVER-DEPLOY.)
- Phase 5 FIXED IN THE DRAFT, NOT SHIPPED — the photo attachment and the photo-redundancy fix (2026-08-12, quick task 260812-o5l). **`d7bfbb93` still serves chat v13 `1eb3fd5c`; no version was cut and nothing about the live demo changed.** THE SPIKE VERDICT, which settles a question this project had never answered: **a Python tool CAN reach an uploaded image's bytes at runtime** — `context.user_content.parts[].inline_data` is a `Blob` whose `.data` is **already base64**, exactly what Resend's `attachments[].content` wants — **but ONLY on the turn the image arrives.** Proven live: by the next turn `context.parts()` and `context.events` retain that turn's *text* and have dropped the image entirely. Ruled out with evidence, not by reading docs: there is no GCS/URI path at all (the CES `Part` model's fields are exactly `{function_call, function_response, inline_data, text}` — no `file_data`, no `file_uri`, and the API's `Chunk` schema matches), and a self-referential `conversations.get` is dead because the sandbox has no `google.auth`, no `requests` and 12 env vars none of which is a credential. MECHANISM BUILT: `assess_screen_crack` captures the base64 on the upload turn and slices it into session variables; `send_case_record_email` reassembles and attaches. **HARD PLATFORM LIMIT, found by hitting it: one session variable may not exceed 262,144 bytes and an oversize write returns HTTP 400 that KILLS THE CUSTOMER'S TURN** — not a soft error — so the guard sits before the write, slicing at 250,000 and refusing above 4,000,000 base64 chars. Session variables were proven to hold **5 MB across 20 keys**, surviving turn boundaries exactly, with no cost on later turns. DELIVERY PROVEN by `executeTool` against the real tool: Resend **200** with a **1,530,736-char** attachment (`attached: true`), and the no-photo call **200** with `attached: false` and no `attachments` key — packet body and `[ASSESSOR] [CHAT]` subject byte-unchanged either way. ALSO IN THE DRAFT, the photo-redundancy fix (the entry below, now addressed): `claim_intake` +832 and `resolve_claim` branched on `photo_check in ("confirmed","contradicted")`, so on chat the customer email subject now reads `Claim CLM-xxxxx - photo received, nothing needed`, the body says the photo is already attached, and the spoken send-away line says the same — observed live on both an escalated (`CLM-24366`) and an approved (`CLM-24537`) run. **Voice untouched, deliberately** — it has no image path so asking by email is right there. **WHY NO REPOINT: two canaries did not pass.** (1) **`cover_offer_actions` did not fire** on the approve run — the agent went straight to `end_session` after the send-away line; the Cross-sell step is byte-unchanged by this task and this looks like the known "never reached" mode, **but byte-unchanged is not the same as unaffected** and the edit sits immediately before that step, so it needs one more approve run with a neutral closing turn. (2) The live escalated packet went out with **`attached: false`**, because the image arrived in a turn with **no tool call at all** and `assess_screen_crack` did not run until two turns later — the designed degradation working (packet still delivered, 200), but proof that **capture only exists if a Python tool runs on the upload turn.** A +491-char same-turn rule was added to `claim_intake` and worked on the next run; it is **one observation old**. STRUCTURAL LIMITATION worth knowing: on the total-loss branch the agent never asks for a photo (the "See the damage" subtask triggers only on REPAIRABLE + cracked screen), so an escalated claim carries a photo only when the customer volunteers one or on the contradiction path — **the photo-contradiction path is the scenario that shows this feature off, and it has still never been run live.** Everything else byte-identical: 10 tools, 37 variables, the other 7 tools and 2 agents unchanged by whole-object sha256, `resolve_claim` key count 1 before and after. Rollback target if anyone repoints later: chat v13 `1eb3fd5c`. **UNCONFIRMED BY EYE: nobody has opened the attachment.** A Resend 200 proves acceptance, not delivery — two test emails (`CLM-O5LPHOTO`, with `CLM-O5LPHOTO-damage.png`, and `CLM-O5LNOPHOTO`) are in the inbox to settle it. PLATFORM FINDING WITH BROAD REACH: **`apps.executeTool` runs a Python tool with no conversation and no quota** (`tool` + `args` + `variables` + `context`), and **`SessionInput.image`** pushes an image through `runSession` exactly as the widget does — between them the entire spike cost zero demo-app quota, on a throwaway app since deleted. AND A TRAP: **tool code changes are not effective immediately** — a conversation started seconds after a `tools.patch` ran the PREVIOUS code while a byte-for-byte read-back said the new code was live; wait ~25 s after patching a tool.
- ~~Phase 5 KNOWN DEFECT, NOT FIXED~~ **ADDRESSED IN THE DRAFT 2026-08-12 by quick task 260812-o5l (entry above); still not shipped, since no version was cut** — the chat send-away line and email subject are voice-shaped (2026-08-12, found on the chat v13 widget run): on the **chat** channel the agent still asks the customer to *"reply to the email with photos of the damage"* and the email subject is still `Claim CLM-24832 - please reply with photos` — **even when a photo was already uploaded, assessed and confirmed in the conversation**. The template is correct on **voice**, which has no image path and genuinely needs the customer to mail photos in; on chat it asks for something the customer has already given and directly undercuts the multimodal beat the demo just landed. FIX (a separate task owns it): branch the send-away sentence and the email subject on whether a photo was already assessed this session. **Voice must keep the current wording unchanged.**
- Demo build (2026-08-11, quick task 260811-suy): `DEMO-RUNBOOK.md` does not mention that the voice claim email **asks the customer to reply with photos** (subject `Claim CLM-xxxxx - please reply with photos`; the body says so in capitals; the agent voices it at the email turn). This is the voice channel compensating for having no photo-upload path, and it is a good answer to the obvious *"how do you see the damage over the phone?"* question — but a presenter meeting it cold will fumble it. Two lines are enough: flag the beat, and flag the contrast with chat's live in-session photo assessment as the segue between channels. Also still blank in the runbook: the `☎ ____` phone number (~line 20), which is not API-retrievable.
- Demo build: the claim email can only reach ONE mailbox. Resend's shared `onboarding@resend.dev` sender delivers only to the address owning the Resend account (`akash.vinayak@nerdery.com`) — confirmed by a 403 from Resend when tested against another address. The phone deployment is pinned to v11 `b17c9a26`, which mails that address; both paths verified live. Sending to a second recipient requires verifying a domain at resend.com/domains and changing the `from` address.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260805-aze | Create presenter demo runbook for deployed Meridian voice claim agent | 2026-08-05 | (see log) | [260805-aze-create-presenter-demo-runbook-for-deploy](./quick/260805-aze-create-presenter-demo-runbook-for-deploy/) |
| 260806-u21 | Remove wrong-subject photo guard from chat app; document the accepted limitation | 2026-08-06 | (see log) | [260806-u21-remove-wrong-subject-photo-guard-from-ch](./quick/260806-u21-remove-wrong-subject-photo-guard-from-ch/) |
| 260809-n1b | Reshape claim_decision_card to the SDK order_summary contract (chat v7) | 2026-08-09 | (see log) | [260809-n1b-reshape-claim-decision-card-to-the-sdk-o](./quick/260809-n1b-reshape-claim-decision-card-to-the-sdk-o/) |
| 260809-nt7 | Record live verification of decision-card rendering and photo upload through the real web widget | 2026-08-09 | (see log) | [260809-nt7-record-live-verification-of-decision-car](./quick/260809-nt7-record-live-verification-of-decision-car/) |
| 260810-ifr | Fix cover_offer_actions to match the SDK quick_actions contract; cross-sell fires for the first time (chat v8) | 2026-08-10 | (see log) | [260810-ifr-fix-cover-offer-actions-to-match-the-sdk](./quick/260810-ifr-fix-cover-offer-actions-to-match-the-sdk/) |
| 260810-k5x | Make the agent speak the decision explanation verbatim, exactly once (chat v9) | 2026-08-10 | (see log) | [260810-k5x-make-the-agent-speak-the-decision-explan](./quick/260810-k5x-make-the-agent-speak-the-decision-explan/) |
| 260811-suy | Record the real phone check into the voice baseline; fix the repeated diagnostic question in both drafts (no version cut) | 2026-08-11 | (see log) | [260811-suy-record-phone-check-and-fix-the-repeated-](./quick/260811-suy-record-phone-check-and-fix-the-repeated-/) |
| 260811-tgm | Correct the record: the voice cross-sell FIRES (reverses 260811-suy); barge-in confirmed; repeated question is intermittent; audio-channel normalisation warning for 05-09 | 2026-08-11 | (not committed) | [260811-tgm-correct-the-cross-sell-record-voice-cros](./quick/260811-tgm-correct-the-cross-sell-record-voice-cros/) |
| 260812-hhi | File the assessor packet on the email-confirmation turn (chat v12 `26c3aebd` live; voice wired but undeployed; human handoff un-stranded) | 2026-08-12 | (not committed) | [260812-hhi-file-the-assessor-packet-on-the-decision](./quick/260812-hhi-file-the-assessor-packet-on-the-decision/) |
| 260812-o5l | SPIKE: FEASIBLE — a Python tool can reach an uploaded image, but only on its own turn. Photo now attached to the chat assessor packet + photo-redundancy fix + cross-sell regression found and fixed. **SHIPPED: chat v14 `cdca14e3-5b0e-4675-9861-f5f22736362f`, `d7bfbb93` repointed 2026-08-13** | 2026-08-12 | (not committed) | [260812-o5l-attach-the-assessed-photo-to-the-chat-as](./quick/260812-o5l-attach-the-assessed-photo-to-the-chat-as/) |

- Phase 5 SHIPPED (2026-08-13, quick task 260813-olv): **voice v15 `17b2e438-b132-49f1-8b32-190b132225ae` is LIVE** on `d28bbcb0`, repointed from v14 `5d02f14c` at `2026-08-13T23:42:50Z` on the user's explicit instruction, after 260813-olv deliberately held the gate for a human decision. Read back from the API, not inferred; `channelType: GOOGLE_TELEPHONY_PLATFORM` unchanged. **Rollback target: v14 `5d02f14c-8cba-4bf4-aa3a-b9caf57ffddc`.** What it carries: the language now tracks the LATEST user turn and survives the `claims_concierge` → `claim_intake` handoff (probe 3, the exact live-call failure, goes from a call-ending defect on v14 to a pass). Probes 1-3 PASS; **probe 4 — switching language AFTER the handoff — still FAILS both directions**, but has never worked on any build, so it is 05-03's original finding rather than a regression. Canaries 42/42 plus 18/18 isolation, including `DECISION_SPEECH_EN` byte-identical, the send-away line spoken, and the cross-sell firing. STILL OPEN: T-05-12 — the model translated the decision on the decision turn, so `spoken != explanation` and the figures were model-composed; 05-09 owns it. Presenter guidance: **switch language within the first two turns**, before the claim begins.

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| v2 | DEF-01 Cross-channel context continuity (chat→voice resume) | Deferred | Requirements definition |
| v2 | DEF-02 Proactive/catastrophe-triggered outbound outreach | Deferred | Requirements definition |
| v2 | DEF-03 Agent Assist | Deferred | Requirements definition |
| v2 | DEF-04 Speed/efficiency live metric overlay | Deferred | Requirements definition |

## Session Continuity

Last session: 2026-07-09T23:03:54.209Z
Stopped at: Phase 2 context gathered
Resume file: .planning/phases/02-component-architecture/02-CONTEXT.md
