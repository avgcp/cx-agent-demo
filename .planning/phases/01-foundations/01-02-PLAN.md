---
phase: 01-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/spec/section-2-platform-capability-map.md]
autonomous: true
requirements: [CAP-01, CAP-02]

must_haves:
  truths:
    - "Every wow moment has exactly one capability-map row naming a specific CX Agent Studio primitive"
    - "Every row is tagged [BUILT-IN], [CUSTOM TOOL], or [MOCK DATA]"
    - "Every platform-capability claim cites a Google Cloud documentation URL from CLAUDE.md §Sources"
    - "The two unverified/press-only claims (audio-to-audio EN↔ES support; transcript→summary→email mechanics) are explicitly flagged as open questions, not asserted as fact"
  artifacts:
    - path: ".planning/spec/section-2-platform-capability-map.md"
      provides: "One-row-per-wow-moment mapping of narrative moments to CX Agent Studio primitives with tags, citations, and open-question flags"
      contains: "## Platform Capability Map"
  key_links:
    - from: ".planning/spec/section-2-platform-capability-map.md"
      to: "CLAUDE.md §Sources (Google Cloud doc URLs)"
      via: "per-row citation column"
      pattern: "docs.cloud.google.com"
---

<objective>
Author the **Platform Capability Map (Section 2)** — a table mapping every wow moment in the demo to a specific CX Agent Studio primitive, each tagged `[BUILT-IN]` / `[CUSTOM TOOL]` / `[MOCK DATA]` and cited to current Google Cloud documentation, with every unverified/press-only claim explicitly flagged as an open question.

Purpose: Satisfies CAP-01 (map every wow moment to a primitive with a tag) and CAP-02 (cite docs; flag unverified claims). This is where the research in CLAUDE.md / STACK.md gets translated into spec form, and it is the guardrail against Pitfall 9 (over-promising platform integration).
Output: `.planning/spec/section-2-platform-capability-map.md`

This is a spec-authoring task. It produces Markdown spec text, not code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@CLAUDE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capability-map table — one row per wow moment, tagged + cited</name>
  <files>.planning/spec/section-2-platform-capability-map.md</files>
  <read_first>
    - CLAUDE.md — the full platform-capability tables AND the §Sources list of Google Cloud doc URLs (this file is the citation source for every row; use its exact URLs)
    - .planning/research/STACK.md — BUILT-IN / CUSTOM TOOL / MOCK DATA tagging taxonomy and the 28-variant voice list
    - .planning/phases/01-foundations/01-CONTEXT.md — the locked wow-moment set: laptop chat/voice FNOL intake, multimodal photo damage assessment, coverage/deductible lookup, $1,000 decision branch, total-loss/data-loss always-escalate flag (D-05), human handoff, backend claims-processing reveal, voice channel, EN↔ES mid-demo language switch (D-10), cross-sell/personalization (D-08), empathy/tone adaptation (D-06/ADD-01)
    - .planning/research/PITFALLS.md — Pitfall 9 (over-promising platform integration)
  </read_first>
  <action>
    Create `.planning/spec/section-2-platform-capability-map.md` with heading `## Platform Capability Map` and a short intro noting every row is cited to CLAUDE.md §Sources and that press-only/unverified claims are flagged in the Open Questions subsection (Task 2).

    Author a single table with columns: `Wow Moment | CX Agent Studio Primitive | Tag | Google Cloud Doc Citation | Confidence`. Include exactly one row per wow moment. Use `[BUILT-IN]` / `[CUSTOM TOOL]` / `[MOCK DATA]` tags (a row may carry more than one tag where a moment combines a native capability with a custom tool). Author these rows verbatim in intent:

    1. Chat FNOL intake (V1 baseline) → Root "Claims Concierge" agent + Intake sub-agent, natural-language instructions → `[BUILT-IN]` → cite Agents + Instructions docs → HIGH
    2. Voice channel intake → Google Telephony Platform native voice channel → `[BUILT-IN]` → cite Google Telephony Platform deploy doc → HIGH
    3. Mid-demo language switch (EN↔ES US) → automatic language detect + response matching (chat) / audio-to-audio voice translation (voice) → `[BUILT-IN]` → cite Agents (auto language detect) + Languages reference (28 voice variants) → HIGH for chat/28-variant list, MEDIUM for voice audio-to-audio (flag in Task 2)
    4. Multimodal photo damage assessment → native Gemini image understanding + a Python tool that turns the read into a structured severity/decision → `[BUILT-IN]` + `[CUSTOM TOOL]` → cite web-widget `enable-file-upload` doc + image-understanding doc → HIGH
    5. Coverage / deductible lookup → Python code tool returning seeded mock JSON → `[CUSTOM TOOL]` + `[MOCK DATA]` → cite Python code tools doc → HIGH
    6. Decision threshold branch ($1,000 auto-approve vs human) → deterministic Python tool writes a session variable + Handoff Rule routes on it → `[CUSTOM TOOL]` + `[BUILT-IN]` → cite Python code tools + Handoff rules docs → HIGH
    7. Total-loss / data-loss always-escalate flag (D-05) → same Python-tool-computed session variable + Handoff Rule → `[CUSTOM TOOL]` + `[BUILT-IN]` → cite Handoff rules doc → HIGH
    8. Human handoff / escalation → `end_session` system tool (session_escalated=true) → `[BUILT-IN]` → cite System tools / handoff doc → HIGH
    9. Backend claims-processing reveal (transcript → specialist summary → drafted customer email / assessor briefing) → in-session Backend Claims-Processing sub-agent chain + Python draft-email tool (NOT an off-the-shelf Gemini Enterprise ingestion pipeline) → `[CUSTOM TOOL]` + `[MOCK DATA]` → cite Best-practices (sub-agents/callbacks) + Gemini Enterprise apps/data-stores doc → MEDIUM (flag in Task 2)
    10. Cross-sell / upsell + personalization (D-08/ADD-04) → Upsell sub-agent (or agent-as-tool) + coverage/asset lookup Python tool reading the seeded uninsured-device field → `[CUSTOM TOOL]` + `[MOCK DATA]` → cite Python code tools + Agent-as-tool doc → HIGH
    11. Sentiment / empathy tone adaptation (D-06/ADD-01) → agent instruction/persona tuning (native generative capability) → `[BUILT-IN]` → cite Instructions doc → HIGH

    Every citation cell must contain a real `https://docs.cloud.google.com/...` (or `cloud.google.com`) URL copied from CLAUDE.md §Sources.
  </action>
  <acceptance_criteria>
    - File contains heading `## Platform Capability Map`
    - Contains a Markdown table with column headers including `Wow Moment`, `Tag`, and a citation column
    - Contains all three tag tokens `[BUILT-IN]`, `[CUSTOM TOOL]`, `[MOCK DATA]` at least once each
    - Contains at least 11 wow-moment rows (grep for row count of the primitive/tag pattern)
    - Every wow-moment row includes a `docs.cloud.google.com` or `cloud.google.com` URL
    - Rows for the decision branch and the total-loss flag both name a `Handoff Rule` and a `Python` tool (never RAG)
    - grep confirms: `grep -c 'docs.cloud.google.com\|cloud.google.com' section-2-platform-capability-map.md` returns ≥ 8
  </acceptance_criteria>
  <verify>
    grep -q "## Platform Capability Map" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "\[BUILT-IN\]" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "\[CUSTOM TOOL\]" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "\[MOCK DATA\]" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "Handoff Rule" .planning/spec/section-2-platform-capability-map.md &&
    [ "$(grep -c 'google.com' .planning/spec/section-2-platform-capability-map.md)" -ge 8 ]
  </verify>
  <done>One tagged, cited row per wow moment (≥11 rows) present; all three tag types used; decision-branch and total-loss rows name Python tool + Handoff Rule; every capability claim carries a Google Cloud doc URL.</done>
</task>

<task type="auto">
  <name>Task 2: Open-Questions / unverified-claims flags</name>
  <files>.planning/spec/section-2-platform-capability-map.md</files>
  <read_first>
    - CLAUDE.md — the two "MEDIUM confidence / press-only" capability notes: audio-to-audio real-time voice translation (not found in current CX Agent Studio technical docs) and the transcript→summary→drafted-email pipeline (no confirmed off-the-shelf Gemini Enterprise feature)
    - .planning/phases/01-foundations/01-CONTEXT.md — D-11 (audio-to-audio EN↔ES flagged as in-console confirmation item; captioned-text fallback)
    - .planning/research/SUMMARY.md — the four flagged platform uncertainties
    - .planning/spec/section-2-platform-capability-map.md — the table authored in Task 1 (append; do not duplicate)
  </read_first>
  <action>
    Append an `### Open Questions — Verify Against Live Console / Current Docs (CAP-02)` subsection to `.planning/spec/section-2-platform-capability-map.md`. It must explicitly list, as flagged open questions (NOT asserted facts):

    1. **Audio-to-audio voice translation for EN↔ES (US), mid-call switching** — press-confirmed (Jan 2026 announcement coverage) but NOT located in current CX Agent Studio technical docs; the confirmed fallback is that the agent responds in whichever supported voice language the caller speaks (28 GA/Preview voice variants) WITHOUT live audio-to-audio translation, with captioned-text fallback for anything outside the confirmed set (per D-11). Action: confirm the exact audio-to-audio language set in-console / with the Google account team before scripting this as a live voice moment.
    2. **Transcript → specialist summary → auto-drafted email mechanics** — Gemini Enterprise data stores / NotebookLM / meeting-transcript action-item extraction are adjacent but NOT confirmed for this exact CX-conversation-to-email workflow. Action: spec the reveal as a custom in-session sub-agent + Python tool chain (a built simulated pipeline), never marketed as an off-the-shelf Gemini Enterprise feature.

    Add a one-line rule: any capability claim used in customer-facing sales language must be independently re-verified against current Google Cloud docs at build time. Cross-reference: these two items must reappear in the final Open Questions/Risks section (Phase 4, AC-03) — note that here.
  </action>
  <acceptance_criteria>
    - Contains heading `### Open Questions` (verify/console flag subsection)
    - Contains an explicit flag for `audio-to-audio` translation naming EN↔ES / Spanish and the captioned-text fallback
    - Contains an explicit flag for the `transcript` → summary → email mechanics naming it as a custom in-session chain, not off-the-shelf
    - Contains a statement that claims must be re-verified against current docs at build time
    - grep confirms both `audio-to-audio` and `transcript` appear in the Open Questions subsection
  </acceptance_criteria>
  <verify>
    grep -q "### Open Questions" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "audio-to-audio" .planning/spec/section-2-platform-capability-map.md &&
    grep -q "transcript" .planning/spec/section-2-platform-capability-map.md &&
    grep -qi "captioned" .planning/spec/section-2-platform-capability-map.md
  </verify>
  <done>Open-Questions subsection present; both unverified/press-only claims (audio-to-audio EN↔ES; transcript→summary→email) explicitly flagged as console-verify items with the confirmed fallbacks stated; build-time re-verification rule stated.</done>
</task>

</tasks>

<threat_model>
N/A — spec-authoring phase, no runtime/attack surface. This section documents platform capabilities and citations only; there is no code or live service to threat-model.
</threat_model>

<verification>
- Both tasks' grep checks pass against `.planning/spec/section-2-platform-capability-map.md`
- Every wow moment from CONTEXT's locked set appears as exactly one table row
- The two press-only claims are flagged, not asserted, and their confirmed fallbacks are stated
</verification>

<success_criteria>
The Platform Capability Map contains one tagged, cited row per wow moment and explicitly flags every unverified/press-only claim as an open question — satisfying CAP-01, CAP-02, and ROADMAP Phase 1 success criterion #3.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-02-SUMMARY.md`
</output>
