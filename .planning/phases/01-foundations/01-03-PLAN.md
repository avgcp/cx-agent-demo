---
phase: 01-foundations
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified: [.planning/spec/section-3-decision-logic.md]
autonomous: true
requirements: [DEC-01, DEC-02, DEC-03]

must_haves:
  truths:
    - "All routing rules are expressed as literal if/then rows with no prose ambiguity"
    - "The spec mandates routing via a deterministic Python tool + Handoff Rule on a session variable, explicitly NOT RAG or instruction-only LLM judgment"
    - "The $1,000 threshold has a stated, configurable, illustrative rationale ('cost of a manual adjuster touch')"
    - "Auto-approval is framed as a human-authorized, configurable, auditable rule set with a visible audit-trail artifact — never 'the AI decides'"
    - "Each rule row references the Mock Data (Section 4) record ID(s) that exercise it"
  artifacts:
    - path: ".planning/spec/section-3-decision-logic.md"
      provides: "Literal if/then business-rules table + determinism mandate + audit-trail/configurable-rationale framing"
      contains: "## Decision Logic"
  key_links:
    - from: ".planning/spec/section-3-decision-logic.md"
      to: ".planning/spec/section-4-mock-data-appendix.md"
      via: "rule rows cite CLM-24001 / CLM-24002 / CLM-24003 and named fields"
      pattern: "CLM-2400"
---

<objective>
Author the **Decision Logic Spec (Section 3)** — the explicit, unambiguous business-rules table governing the $1,000 auto-approve/human-assessor threshold, the total-loss/data-loss always-escalate flag, and the cross-sell trigger, plus the determinism mandate and the audit-trail/configurable-rationale framing.

Purpose: Satisfies DEC-01 (literal if/then rules), DEC-02 (deterministic Python tool + Handoff Rule, never RAG/LLM), DEC-03 (human-authorized, configurable, auditable framing with audit-trail artifact and stated $1,000 rationale). Pre-empts Pitfalls 2, 6, 7 (non-determinism, non-credible threshold, regulatory optics).
Output: `.planning/spec/section-3-decision-logic.md`

This is a spec-authoring task. It produces Markdown spec text, not code. It references the canonical Mock Data Appendix (Section 4) authored in plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/spec/section-4-mock-data-appendix.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Literal if/then rules table + determinism mandate</name>
  <files>.planning/spec/section-3-decision-logic.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-12 (literal $1,000 ceiling + rationale), D-13 (the four literal if/then rows, routed by deterministic Python tool + Handoff Rule on a session variable)
    - .planning/spec/section-4-mock-data-appendix.md — the canonical records (CLM-24001 $450, CLM-24002 $2,400 total_loss_flag=true, CLM-24003 $950 negative, uninsured_device field) so each rule row can cite the record that exercises it
    - .planning/research/ARCHITECTURE.md — Pattern 1 (deterministic decision fork via Handoff Rule + Python tool) and Anti-Pattern 1 (do NOT use a Data Store/RAG for the threshold)
    - .planning/research/PITFALLS.md — Pitfalls 2 and 6
    - .planning/REQUIREMENTS.md — DEC-01, DEC-02
  </read_first>
  <action>
    Create `.planning/spec/section-3-decision-logic.md` with heading `## Decision Logic` and a short intro stating these rules are the single source of truth for routing and that every threshold references a named field in the Mock Data Appendix (Section 4).

    Author a **Business-Rules Table** with columns: `Rule ID | Condition (literal if/then) | Outcome | Exercised by (Section 4 record)`. Author these exact rows (copy the literal conditions verbatim from D-13):
    - DL-1: `claim_amount ≤ $1,000 AND coverage_valid = true AND total_loss_flag = false` → **auto-approve, issue claim number** → exercised by CLM-24001 ($450) and CLM-24003 ($950, over-fire negative — must auto-approve, must NOT escalate)
    - DL-2: `claim_amount > $1,000` → **route to human assessor** → exercised by CLM-24002 ($2,400)
    - DL-3: `total_loss_flag = true` → **route to human (regardless of amount)** → exercised by CLM-24002 (total_loss_flag=true, data_loss_flag=true)
    - DL-4: `claim resolved AND profile has uninsured_device` → **fire cross-sell** → exercised by primary claimant Jordan Rivera (uninsured_device present); must NOT fire for Sam Okafor (no uninsured_device)

    Then author a **### Determinism Mandate (DEC-02)** subsection stating explicitly: the comparison is computed by a deterministic Python code tool (`evaluate_claim`) that writes a boolean/enum session variable (e.g. `escalation_required`), and a **Handoff Rule** on that session variable performs the routing — this is `[CUSTOM TOOL] + [BUILT-IN]`, and it is explicitly **NOT** a RAG/Data-Store lookup and **NOT** instruction-only LLM judgment (name Anti-Pattern 1: "is $1,200 > $1,000" must never be a probabilistic retrieval). State that this is what makes the branch reliably repeatable live (N/N).
  </action>
  <acceptance_criteria>
    - File contains heading `## Decision Logic`
    - Contains a rules table with all four rows DL-1..DL-4
    - Contains the literal condition string `claim_amount ≤ $1,000` (or `<= $1,000`) and `claim_amount > $1,000`
    - Contains `total_loss_flag = true` as a standalone always-escalate condition
    - Each rule row references a Section 4 record ID (`CLM-24001`, `CLM-24002`, `CLM-24003` all appear)
    - Determinism Mandate names both `Python` tool and `Handoff Rule` and explicitly says NOT RAG / NOT LLM judgment
    - grep confirms: `grep -c 'DL-1\|DL-2\|DL-3\|DL-4' section-3-decision-logic.md` returns ≥ 4
  </acceptance_criteria>
  <verify>
    grep -q "## Decision Logic" .planning/spec/section-3-decision-logic.md &&
    grep -q "DL-1" .planning/spec/section-3-decision-logic.md && grep -q "DL-4" .planning/spec/section-3-decision-logic.md &&
    grep -q "1,000" .planning/spec/section-3-decision-logic.md &&
    grep -q "total_loss_flag" .planning/spec/section-3-decision-logic.md &&
    grep -q "Handoff Rule" .planning/spec/section-3-decision-logic.md &&
    grep -qi "not.*RAG\|never.*RAG\|not a RAG" .planning/spec/section-3-decision-logic.md &&
    grep -q "CLM-24002" .planning/spec/section-3-decision-logic.md
  </verify>
  <done>All four literal if/then rules present as a table, each referencing the Section 4 record that exercises it; determinism mandate names Python tool + Handoff Rule and explicitly excludes RAG/LLM judgment.</done>
</task>

<task type="auto">
  <name>Task 2: Audit-trail + configurable-rationale framing (DEC-03)</name>
  <files>.planning/spec/section-3-decision-logic.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-12 ($1,000 rationale = "roughly the cost of a manual adjuster touch"; configurable per carrier), D-13 (human-authorized/auditable framing)
    - .planning/research/PITFALLS.md — Pitfall 7 (regulatory optics of AI "deciding" claims) and the recovery/audit-trail guidance
    - .planning/spec/section-3-decision-logic.md — the rules + determinism mandate authored in Task 1 (append; do not duplicate)
    - .planning/REQUIREMENTS.md — DEC-03
  </read_first>
  <action>
    Append a `### Governance, Configurability & Audit Trail (DEC-03)` subsection to `.planning/spec/section-3-decision-logic.md` containing:

    1. **Configurable rationale for the $1,000 threshold** (verbatim intent from D-12): the ceiling is set at roughly the cost of a manual adjuster touch — claims that cost more to hand-process than to simply pay are auto-settled; each carrier tunes the threshold to their own cost-to-serve. State explicitly that $1,000 is an *illustrative, configurable* business rule, not a hardcoded universal truth.
    2. **Human-authorized framing** — the rule set is authored and owned by the carrier's claims leadership; the system *pre-approves within a human-authorized policy/rule set*. Explicitly forbid "the AI decides" language anywhere in the demo narration; the correct framing is "the system applied the carrier's configured rule."
    3. **Visible audit-trail artifact** — every auto-approve is paired with a reviewable artifact: a logged rule reference (which Rule ID fired, which session variable value drove it) plus the reviewable transcript. Describe this as a concrete on-screen artifact the presenter can show ("look, it logs and explains which rule it applied") so auditability becomes a wow moment, not a landmine. Note this same audit artifact doubles as the answer to "what happens if it's wrong / who's accountable."
    4. **Secondary factors beyond dollar amount** — note that `total_loss_flag` (and, in production, fraud/prior-claims flags) are additional routing inputs so the branch is not a single naive number.
  </action>
  <acceptance_criteria>
    - Contains heading `### Governance, Configurability & Audit Trail`
    - Contains the phrase `cost of a manual adjuster touch` (or "manual adjuster touch")
    - States the $1,000 figure is `configurable` / `illustrative` (not hardcoded universal)
    - Explicitly forbids "the AI decides" framing (contains a "never 'the AI decides'" or equivalent statement)
    - Describes a visible audit-trail artifact (logged rule reference + reviewable transcript)
    - grep confirms both `audit` and `configurable` appear
  </acceptance_criteria>
  <verify>
    grep -q "### Governance, Configurability & Audit Trail" .planning/spec/section-3-decision-logic.md &&
    grep -qi "manual adjuster touch" .planning/spec/section-3-decision-logic.md &&
    grep -qi "configurable" .planning/spec/section-3-decision-logic.md &&
    grep -qi "audit" .planning/spec/section-3-decision-logic.md &&
    grep -qi "AI decides" .planning/spec/section-3-decision-logic.md
  </verify>
  <done>Governance subsection present: $1,000 rationale stated as configurable/illustrative ("cost of a manual adjuster touch"); human-authorized framing with explicit ban on "the AI decides"; visible audit-trail artifact described; secondary factors noted.</done>
</task>

</tasks>

<threat_model>
N/A — spec-authoring phase, no runtime/attack surface. The relevant governance control (auditability, human-authorized rule set, no autonomous "AI decides" framing) is authored as spec content in Task 2, not a runtime security measure.
</threat_model>

<verification>
- Both tasks' grep checks pass against `.planning/spec/section-3-decision-logic.md`
- Every threshold/condition in the rules table references a named Section 4 field/record (cross-section traceability)
- Determinism mandate and audit-trail framing are both present and unambiguous
</verification>

<success_criteria>
The Decision Logic spec expresses every routing rule as a literal if/then row, mandates deterministic Python-tool + Handoff-Rule routing (never RAG/LLM), and frames auto-approval as human-authorized, configurable, and auditable with a stated $1,000 rationale — satisfying DEC-01..03 and ROADMAP Phase 1 success criterion #4.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-03-SUMMARY.md`
</output>
