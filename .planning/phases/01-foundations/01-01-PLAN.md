---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/spec/section-4-mock-data-appendix.md]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]

must_haves:
  truths:
    - "A reader can find a named seed record + named field for every dollar figure and coverage term the demo will speak on stage"
    - "The two anchor laptop claims land on opposite sides of the literal $1,000 threshold (one under, one over)"
    - "At least one 'negative' record exists per branch that must NOT trigger escalation or cross-sell (over-fire check)"
    - "All names, policy IDs, contacts, emails are obviously synthetic (fictitious namespace, invalid-format identifiers, .test/example domains)"
    - "A pre-validated damage-image library with an expected-analysis output per image is listed"
    - "English + Spanish (US) mid-demo language-switch trigger phrases are given verbatim"
    - "Traceability to locked CONTEXT decisions: D-01: laptop FNOL anchor scenario drives the seed records; D-02: insurance framing supplies the coverage/deductible vocabulary in the mock data; D-04: the two anchor claims straddle both sides of the $1,000 threshold; D-05: total-loss/data-loss flag is a seeded intake field; D-08: a seeded uninsured device enables the device cross-sell; D-10: EN↔ES (US) multilingual test phrases are provided verbatim"
  artifacts:
    - path: ".planning/spec/section-4-mock-data-appendix.md"
      provides: "Canonical versioned seed dataset — single source of truth for all dollar figures, coverage terms, records, images, phrases"
      contains: "## Mock Data Appendix"
  key_links:
    - from: ".planning/spec/section-4-mock-data-appendix.md"
      to: "Decision Logic (Section 3) and Demo Narrative (Section 1)"
      via: "named-field registry (claim_amount, deductible, coverage_limit, total_loss_flag, data_loss_flag, uninsured_device, policy_id, claim_id)"
      pattern: "claim_amount"
---

<objective>
Author the **Mock Data Appendix (Section 4)** — the canonical, versioned seed dataset that is the single source of truth for every dollar figure, coverage term, claim record, damage image, and multilingual phrase used anywhere else in the spec.

Purpose: Success criterion #5 requires that *every* dollar figure/coverage term spoken in the demo traces to a named mock-data field, engineered to land on both sides of every threshold, with over-fire "negative" records, synthetic PII only. This section is authored FIRST because Decision Logic (Section 3) and the Demo Narrative (Section 1) must reference its record IDs and field names rather than inventing values.
Output: `.planning/spec/section-4-mock-data-appendix.md`

This is a spec-authoring task. It produces Markdown spec text, not code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical seed records + synthetic-PII standard</name>
  <files>.planning/spec/section-4-mock-data-appendix.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-01/D-02/D-03/D-04 (laptop FNOL anchor, insurance framing, generic white-label carrier, two back-to-back claims), D-05 (total-loss/data-loss flag), D-08 (uninsured-device cross-sell), D-12/D-13 (literal $1,000 ceiling, routing variables)
    - .planning/research/PITFALLS.md — Pitfalls 1, 4, 6, 8 (hallucinated dollar figures, mock data must reliably trigger branches, credible threshold, synthetic-PII standard)
    - .planning/REQUIREMENTS.md — DATA-01, DATA-04
  </read_first>
  <action>
    Create `.planning/spec/section-4-mock-data-appendix.md` starting with a top heading `## Mock Data Appendix` and a version line `**Dataset version:** v1.0` and a **Synthetic-Data Standard** subsection stating: all names, policy numbers, phone numbers, emails, and addresses are synthetic and drawn from a fictitious namespace — no valid-format SSNs, phone numbers use the reserved 555-01xx range, emails use `.test`/`example-*` domains — and that presenters must NEVER improvise real personal details live (all inputs come from this canonical dataset).

    Then author the canonical seed records as Markdown tables/records. Use these exact literal values (dollar figures within the small/large split are authored here as the source of truth):

    **Carrier (white-label, generic — per D-03):** `Meridian Device Protection` (generic personal-lines / gadget insurer; note it is a swappable placeholder, not a real brand).

    **Primary claimant profile (drives the demo run + cross-sell + personalization per D-08/ADD-04):**
    - `claimant_name`: Jordan Rivera
    - `policy_id`: PDP-100294
    - `email`: jordan.rivera@example-mail.test
    - `phone`: 555-0142
    - `tenure`: policyholder since 2023
    - `uninsured_device`: recently purchased smartphone (purchased 2026-06, NOT on any policy) — this is the seeded cross-sell/personalization hook

    **Policy & coverage (insurance framing per D-02):**
    - `coverage_type`: personal device / gadget insurance
    - `deductible`: $100
    - `coverage_limit`: $3,500 per device
    - `coverage_valid`: true

    **Two anchor laptop claims, run back-to-back (per D-04), engineered to land on opposite sides of the literal $1,000 threshold:**
    - Small claim (auto-approve side): `claim_id` CLM-24001, `loss_type` accidental cracked screen / cosmetic laptop damage, `claim_amount` $450, `total_loss_flag` false, `data_loss_flag` false, `coverage_valid` true, `damage_image_ref` IMG-CRACK-01
    - Large claim (HITL side, per D-05 always-escalate): `claim_id` CLM-24002, `loss_type` liquid-damaged high-end laptop (destroyed), `claim_amount` $2,400, `total_loss_flag` true, `data_loss_flag` true, `coverage_valid` true, `damage_image_ref` IMG-LIQUID-01

    Present each record as a labeled field list or table so every field name is greppable. State at the end of the task section that these field names are the canonical tokens Sections 1 and 3 must reference.
  </action>
  <acceptance_criteria>
    - File `.planning/spec/section-4-mock-data-appendix.md` exists and contains the heading `## Mock Data Appendix`
    - Contains `**Dataset version:** v1.0`
    - Contains a Synthetic-Data Standard statement including the string `555-01` and a `.test` domain
    - Contains `claim_amount` value `$450` associated with `CLM-24001` and `$2,400` associated with `CLM-24002`
    - `$450` is below and `$2,400` is above `$1,000`
    - Contains `total_loss_flag` set to `true` for CLM-24002 and `false` for CLM-24001
    - Contains `deductible` `$100`, `coverage_limit` `$3,500`, and `uninsured_device`
    - grep confirms: `grep -c 'CLM-24001\|CLM-24002' section-4-mock-data-appendix.md` returns ≥ 2
  </acceptance_criteria>
  <verify>
    grep -q "## Mock Data Appendix" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "CLM-24001" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "CLM-24002" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "total_loss_flag" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "555-01" .planning/spec/section-4-mock-data-appendix.md
  </verify>
  <done>All canonical seed records (carrier, claimant, policy/coverage, both anchor claims) present with literal named fields; synthetic-PII standard stated; small ($450) and large ($2,400) claims straddle the $1,000 threshold; total-loss/data-loss flags set.</done>
</task>

<task type="auto">
  <name>Task 2: Negative/over-fire records, damage-image library, multilingual phrases, and traceability field registry</name>
  <files>.planning/spec/section-4-mock-data-appendix.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-04 (both sides of threshold), D-05 (total-loss flag), D-08 (uninsured-device cross-sell), D-10/D-11 (English↔Spanish (US) switch, captioned fallback)
    - .planning/research/PITFALLS.md — Pitfalls 4, 5, 8, 10 and the "Looks Done But Isn't" checklist (negative records, pre-validated seed images, synthetic PII, exact trigger phrases)
    - .planning/REQUIREMENTS.md — DATA-02, DATA-03, DATA-04
    - .planning/spec/section-4-mock-data-appendix.md — the records authored in Task 1 (do not duplicate; append)
  </read_first>
  <action>
    Append to `.planning/spec/section-4-mock-data-appendix.md` the following subsections:

    **### Negative / Over-Fire Records (DATA-02)** — records that must NOT trigger a branch, to catch over-firing:
    - `claim_id` CLM-24003, near-threshold cracked-screen claim, `claim_amount` $950, `total_loss_flag` false, `data_loss_flag` false → expected: AUTO-APPROVE (must NOT escalate despite being close to the ceiling). This is the escalation over-fire check.
    - Second claimant `claimant_name` Sam Okafor, `policy_id` PDP-100871, fully insured, `uninsured_device`: none → expected: cross-sell must NOT fire. This is the cross-sell over-fire check.

    **### Pre-Validated Damage-Image Library (DATA-03)** — a table of 3–5 named seed images per damage scenario with the expected damage-analysis output per image (forbid ad-hoc/live photos in the standard path):
    - IMG-CRACK-01, IMG-CRACK-02, IMG-CRACK-03 — cracked/cosmetic screen — expected read: "moderate cosmetic screen damage, repairable, LOW severity, within auto-approve range"
    - IMG-LIQUID-01, IMG-LIQUID-02 — liquid-damaged/destroyed high-end laptop — expected read: "severe internal liquid damage, likely total loss, HIGH severity, route to human"
    State these are placeholder image references the implementation team must attach + re-validate before the live demo.

    **### Multilingual Test Phrases (DATA-03)** — English ↔ Spanish (US) per D-10, with exact trigger phrases (Pitfall 10 requires exact phrasing):
    - English FNOL open: "Hi, I dropped my laptop and the screen is cracked."
    - Language-switch trigger (customer): "¿Podemos continuar en español?"
    - Spanish FNOL phrase: "Se me cayó la laptop y la pantalla está rota."
    - Note: EN↔ES (US) are both GA voice variants; any language outside the confirmed audio-to-audio set uses the captioned-text fallback (per D-11).

    **### Traceability Field Registry (DATA-04)** — a table mapping every on-stage dollar figure / coverage term to its canonical named field and owning record, e.g. rows for `deductible → $100 (policy)`, `coverage_limit → $3,500 (policy)`, `claim_amount → $450 (CLM-24001)`, `claim_amount → $2,400 (CLM-24002)`, `claim_amount → $950 (CLM-24003)`. State the rule: nothing is free-generated on stage; if a figure is not in this registry, it must not be spoken.
  </action>
  <acceptance_criteria>
    - Contains heading `### Negative / Over-Fire Records` with `CLM-24003` and `$950` and an explicit "must NOT escalate" / auto-approve expectation
    - Contains a second claimant (`Sam Okafor`) whose cross-sell must NOT fire
    - Contains heading `### Pre-Validated Damage-Image Library` with `IMG-CRACK-01` and `IMG-LIQUID-01` and an expected-output per image
    - Contains heading `### Multilingual Test Phrases` including the verbatim string `¿Podemos continuar en español?`
    - Contains heading `### Traceability Field Registry` mapping `$450`, `$2,400`, `$950`, `$100`, `$3,500` each to a named field/record
    - grep confirms both `IMG-CRACK-01` and `IMG-LIQUID-01` present
  </acceptance_criteria>
  <verify>
    grep -q "### Negative / Over-Fire Records" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "CLM-24003" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "IMG-CRACK-01" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "IMG-LIQUID-01" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "continuar en español" .planning/spec/section-4-mock-data-appendix.md &&
    grep -q "### Traceability Field Registry" .planning/spec/section-4-mock-data-appendix.md
  </verify>
  <done>Negative/over-fire records for both branches present; damage-image library with expected outputs listed; EN↔ES trigger phrases verbatim; traceability registry maps every dollar figure to a named field/record.</done>
</task>

</tasks>

<threat_model>
N/A — spec-authoring phase, no runtime/attack surface. This section produces Markdown seed data with synthetic PII only; there is no code, no service, and no live data path to threat-model. The synthetic-PII standard (Task 1) is the relevant data-hygiene control.
</threat_model>

<verification>
- Both tasks' grep checks pass against `.planning/spec/section-4-mock-data-appendix.md`
- Every dollar figure appearing in the file also appears as a row in the Traceability Field Registry
- One record straddles each threshold: `$450`/`$950` (auto-approve) vs `$2,400` (escalate); `total_loss_flag=true` on CLM-24002; one no-cross-sell claimant
</verification>

<success_criteria>
The Mock Data Appendix is a versioned, literal, synthetic-PII-only canonical dataset in which every dollar figure/coverage term maps to a named field, records straddle every threshold, and over-fire negatives exist — satisfying DATA-01..04 and ROADMAP Phase 1 success criterion #5.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-01-SUMMARY.md`
</output>
