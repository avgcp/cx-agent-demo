---
phase: 01-foundations
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified: [.planning/spec/section-1-demo-narrative.md]
autonomous: true
requirements: [NARR-01, NARR-02, NARR-03, NARR-04]

must_haves:
  truths:
    - "A single ≤10–15 min beat-by-beat script sequences every wow moment into one coherent white-label FNOL story"
    - "The script runs both decision branches back-to-back (auto-approve small claim, then route-to-human large claim)"
    - "The narrative states which insurance sectors it generalizes to (white-label reusability)"
    - "The narrative explicitly states what V2 adds over the V1 chat-happy-path baseline"
    - "Competitive framing vs. Microsoft Copilot + Nuance is present and every competitive claim is tagged [VALIDATE — Scott/Google]"
    - "Every dollar figure spoken in the narrative traces to a named Mock Data (Section 4) field"
  artifacts:
    - path: ".planning/spec/section-1-demo-narrative.md"
      provides: "The single coherent end-to-end demo storyline with sequenced wow moments, V1→V2 delta, sector generalization, and flagged competitive framing"
      contains: "## Demo Narrative"
  key_links:
    - from: ".planning/spec/section-1-demo-narrative.md"
      to: ".planning/spec/section-4-mock-data-appendix.md and section-3-decision-logic.md"
      via: "narrative dollar figures cite Section 4 fields; decision-branch beats cite Section 3 rules DL-1..DL-4"
      pattern: "CLM-2400\\|DL-"
---

<objective>
Author the **Demo Narrative / Storyline (Section 1)** — the single coherent ≤10–15 min beat-by-beat script that stitches every locked wow moment into one white-label FNOL story, states the V2-over-V1 delta and sector generalization, and includes a light, clearly-flagged competitive framing vs. Microsoft Copilot + Nuance.

Purpose: Satisfies NARR-01 (single coherent ≤10–15 min narrative sequencing every wow moment), NARR-02 (white-label + sector generalization), NARR-03 (competitive framing, all claims flagged for validation), NARR-04 (explicit V1→V2 delta). Pre-empts Pitfalls 11, 12, 13 (deck-not-workflow, no differentiation, too-custom).
Output: `.planning/spec/section-1-demo-narrative.md`

This is the integrative section — authored LAST so it references the locked Decision Logic rules (Section 3) and the canonical Mock Data fields (Section 4). This is a spec-authoring task; it produces Markdown spec text, not code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/spec/section-4-mock-data-appendix.md
@.planning/spec/section-3-decision-logic.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beat-by-beat storyline + V1→V2 delta + sector generalization</name>
  <files>.planning/spec/section-1-demo-narrative.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-01/D-04 (laptop FNOL anchor, two back-to-back claims), D-05/D-06 (total-loss/data-loss always-escalate + empathy/tone adaptation rides this moment), D-08 (uninsured-device cross-sell), D-10/D-11 (EN↔ES mid-demo voice switch, captioned fallback)
    - .planning/spec/section-4-mock-data-appendix.md — canonical figures: CLM-24001 ($450 cracked screen), CLM-24002 ($2,400 liquid damage, total_loss_flag=true), deductible $100, coverage_limit $3,500, uninsured_device; the multilingual trigger phrases
    - .planning/spec/section-3-decision-logic.md — rules DL-1..DL-4 the branch beats must reference by ID
    - .planning/research/FEATURES.md — V1 baseline definition (chat: greet → name+policy → coverage/deductible → photo damage analysis → claim number) and Scott's 5 V2 must-haves
    - .planning/research/PITFALLS.md — Pitfall 11 (distinguish deterministic must-land beats from flex/curveball beats), Pitfall 13 (pattern vs. example)
  </read_first>
  <action>
    Create `.planning/spec/section-1-demo-narrative.md` with heading `## Demo Narrative / Storyline` and a one-line framing that it is a ≤10–15 minute, single-sitting, single-line-of-business (device/gadget insurance) white-label run.

    Author a **### Beat-by-Beat Script** as a numbered list of beats. Every dollar figure MUST cite its Section 4 field/record inline (e.g., "$450 (CLM-24001.claim_amount)"). Sequence all locked wow moments into one coherent story (order is Claude's discretion per CONTEXT, but all must appear):
    1. Chat FNOL intake opens (carrier Meridian Device Protection, claimant Jordan Rivera / PDP-100294) — greet, verify policy — [reuses V1]
    2. Coverage/deductible lookup — deductible $100, coverage_limit $3,500 (cite Section 4) — [reuses V1]
    3. Claim 1 (small): photo of cracked screen (IMG-CRACK-01) → multimodal damage assessment → estimate $450 (CLM-24001)
    4. **Decision branch — autonomous:** apply Rule DL-1 → auto-approve, issue claim number; agent visibly states which rule/threshold it applied (Pitfall UX — make the decision visible)
    5. **Voice + mid-demo language switch (EN↔ES US):** customer says "¿Podemos continuar en español?" → agent continues in Spanish (per D-10; captioned-text fallback noted per D-11)
    6. Claim 2 (large), back-to-back: liquid-damaged high-end laptop (IMG-LIQUID-01) → estimate $2,400 (CLM-24002), total_loss_flag=true, data_loss_flag=true
    7. **Empathy/tone adaptation (ADD-01, D-06):** agent detects the customer's genuine distress about lost work/data and visibly adapts tone
    8. **Decision branch — HITL:** apply Rules DL-2 and DL-3 → route to human assessor (visibly); prove HITL is a warm, context-rich handoff, not a dead end
    9. **Backend claims-processing reveal:** transcript → specialist summary → auto-drafted artifact (customer email for the autonomous claim; assessor briefing packet for the HITL claim) — the "screen behind the screen"
    10. **Cross-sell / upsell close (D-08, ADD-04):** system notes Jordan Rivera's seeded uninsured_device (recently bought phone) → offers device-bundle protection — the "cost center → profit center" closing beat, made to feel earned by personalization
    Mark each beat as **[DETERMINISTIC — must land]** or **[FLEX — presenter may improvise]**, and include at least one scripted **[CURVEBALL]** out-of-scope moment to prove generative flexibility (Pitfall 11).

    Then author two subsections:
    **### What V2 Adds Over V1 (NARR-04):** an explicit list stating the V1 baseline = chat-only single-language FNOL happy path (greet → name+policy → coverage/deductible → photo damage analysis → claim number), and what V2 layers on: voice channel + mid-demo EN↔ES language switch, the visible auto-approve-vs-human decision branch shown BOTH ways, the empathy/tone adaptation, the backend claims-processing reveal, and the cross-sell close.
    **### White-Label & Sector Generalization (NARR-02):** state the carrier and product are generic/swappable placeholders; the run generalizes to any personal-lines / device / gadget / contents insurer and the pattern generalizes to broader P&C FNOL (auto, home, property). Distinguish structural/reusable patterns (decision-branch, escalation, cross-sell, backend-reveal) from swappable examples (laptop, device-bundle) per Pitfall 13. State the demo stays single-sector per run (no multi-sector branching on stage).
  </action>
  <acceptance_criteria>
    - File contains heading `## Demo Narrative / Storyline`
    - Contains `### Beat-by-Beat Script` with numbered beats covering: chat intake, coverage lookup, both claims, voice + EN↔ES switch, empathy, decision branch BOTH ways, backend reveal, cross-sell
    - Contains the verbatim Spanish trigger `¿Podemos continuar en español?`
    - Every dollar figure appears with its Section 4 source: both `CLM-24001` and `CLM-24002` are cited inline, and `$450` + `$2,400` appear
    - Decision-branch beats cite rule IDs (`DL-1` and at least one of `DL-2`/`DL-3`)
    - Contains `[DETERMINISTIC` and `[FLEX` (or `[CURVEBALL`) beat markers
    - Contains heading `### What V2 Adds Over V1` explicitly naming the V1 baseline and the V2 additions
    - Contains heading `### White-Label & Sector Generalization` naming ≥2 generalizable sectors
    - States a ≤10–15 minute ceiling
  </acceptance_criteria>
  <verify>
    grep -q "## Demo Narrative / Storyline" .planning/spec/section-1-demo-narrative.md &&
    grep -q "### Beat-by-Beat Script" .planning/spec/section-1-demo-narrative.md &&
    grep -q "continuar en español" .planning/spec/section-1-demo-narrative.md &&
    grep -q "CLM-24001" .planning/spec/section-1-demo-narrative.md && grep -q "CLM-24002" .planning/spec/section-1-demo-narrative.md &&
    grep -q "DL-1" .planning/spec/section-1-demo-narrative.md &&
    grep -q "### What V2 Adds Over V1" .planning/spec/section-1-demo-narrative.md &&
    grep -q "### White-Label & Sector Generalization" .planning/spec/section-1-demo-narrative.md &&
    grep -qi "10.15 min\|10–15\|10-15" .planning/spec/section-1-demo-narrative.md
  </verify>
  <done>Single ≤10–15 min beat-by-beat script sequencing every wow moment (both branches back-to-back, voice+ES switch, empathy, backend reveal, cross-sell) with every dollar figure traced to Section 4 and branch beats citing DL rules; V1→V2 delta and sector generalization subsections present.</done>
</task>

<task type="auto">
  <name>Task 2: Competitive framing (flagged) + validation-item callouts</name>
  <files>.planning/spec/section-1-demo-narrative.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md — D-14 (light, clearly-flagged competitive framing vs. Copilot + Nuance; every claim tagged [VALIDATE — Scott/Google]; NOT a full head-to-head table), D-09 (device-bundle-vs-boat flagged as Scott/Google validation item), D-11 (audio-to-audio EN↔ES flagged as in-console confirmation item)
    - .planning/research/FEATURES.md — the Competitor Feature Analysis table (voice/language reach, backend reveal, flow-free vs flow-based) for the 1–2 demonstrable contrasts
    - .planning/research/PITFALLS.md — Pitfall 12 (differentiation must be demonstrable + flagged, LOW-MEDIUM confidence) and Pitfall 9 (over-promising)
    - .planning/spec/section-1-demo-narrative.md — the storyline authored in Task 1 (append; do not duplicate)
  </read_first>
  <action>
    Append two subsections to `.planning/spec/section-1-demo-narrative.md`:

    **### Competitive Framing — vs. Microsoft Copilot + Nuance (NARR-03):** a SHORT callout (prose, not a full head-to-head table per D-14) positioning 1–2 *demonstrable* Google advantages that map to wow moments already in the script: (a) native audio-to-audio voice + live mid-call language switching vs. Copilot's region-locked real-time voice; (b) the backend claims-processing reveal (auto-drafted, ready-to-send customer email) as a claims-ops transformation vs. transcript-summary note-taking. EVERY competitive claim MUST carry the inline tag `[VALIDATE — Scott/Google]`. State that this is GTM/positioning input for Scott/Srini/Stephanie/Hallie to confirm, not asserted fact.

    **### Flagged Validation Items:** a short list of demo-content choices flagged for stakeholder/console confirmation (so nothing is a silent assumption): (1) device-bundle cross-sell chosen over Scott's original boat example — `[VALIDATE — Scott/Google]`, boat retained as a restorable option (per D-09); (2) audio-to-audio support for the EN↔ES (US) pair — in-console confirmation item, captioned-text fallback if unconfirmed (per D-11); (3) transcript→summary→email backend reveal is a custom in-session chain, not an off-the-shelf pipeline (cross-reference Section 2 Open Questions). Note these roll up into the Phase 4 Open Questions/Risks section (AC-03).
  </action>
  <acceptance_criteria>
    - Contains heading `### Competitive Framing` naming both `Copilot` and `Nuance`
    - Every competitive claim carries the tag `[VALIDATE — Scott/Google]` (tag appears at least twice)
    - Competitive framing is prose/callout, NOT a full head-to-head comparison table (no multi-row Microsoft-vs-Google feature matrix)
    - Contains heading `### Flagged Validation Items` including the boat-vs-device-bundle flag and the audio-to-audio EN↔ES flag
    - grep confirms `[VALIDATE` appears ≥ 2 times and both `Copilot` and `Nuance` appear
  </acceptance_criteria>
  <verify>
    grep -q "### Competitive Framing" .planning/spec/section-1-demo-narrative.md &&
    grep -q "Copilot" .planning/spec/section-1-demo-narrative.md && grep -q "Nuance" .planning/spec/section-1-demo-narrative.md &&
    [ "$(grep -c 'VALIDATE' .planning/spec/section-1-demo-narrative.md)" -ge 2 ] &&
    grep -q "### Flagged Validation Items" .planning/spec/section-1-demo-narrative.md &&
    grep -qi "boat" .planning/spec/section-1-demo-narrative.md &&
    grep -q "audio-to-audio" .planning/spec/section-1-demo-narrative.md
  </verify>
  <done>Competitive framing subsection present as a short flagged callout (Copilot + Nuance, every claim tagged [VALIDATE — Scott/Google], not a full table); flagged-validation-items list covers boat-vs-device-bundle, audio-to-audio EN↔ES, and the backend-reveal custom-chain caveat.</done>
</task>

</tasks>

<threat_model>
N/A — spec-authoring phase, no runtime/attack surface. This section is narrative prose; there is no code or live service to threat-model. The regulatory/optics guardrail lives in Section 3 (Decision Logic governance framing), cross-referenced here.
</threat_model>

<verification>
- Both tasks' grep checks pass against `.planning/spec/section-1-demo-narrative.md`
- Every dollar figure in the narrative also appears in Section 4's Traceability Field Registry (cross-section consistency check for success criterion #5)
- Both decision branches are demoed back-to-back and cite Section 3 rule IDs
- Every competitive claim is flagged; no unflagged competitive assertion exists
</verification>

<success_criteria>
The Demo Narrative is a single coherent ≤10–15 min beat-by-beat white-label script sequencing every wow moment, states the V1→V2 delta and sector generalization, traces every dollar figure to Section 4, and carries flagged-only competitive framing — satisfying NARR-01..04 and ROADMAP Phase 1 success criteria #1 and #2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-04-SUMMARY.md`
</output>
