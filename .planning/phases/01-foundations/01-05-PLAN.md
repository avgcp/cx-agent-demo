---
phase: 01-foundations
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/spec/section-3-decision-logic.md
  - .planning/spec/section-4-mock-data-appendix.md
  - .planning/spec/section-1-demo-narrative.md
autonomous: true
requirements: [DEC-01, DEC-02, DEC-03, DATA-01, DATA-04, NARR-02]

must_haves:
  truths:
    - "section-3-decision-logic.md cites D-05 (DL-3 total-loss always-escalate), D-12 ($1,000 rationale), and D-13 (literal if/then rows via Python tool + Handoff Rule) literally"
    - "section-4-mock-data-appendix.md cites D-08 literally next to the uninsured_device cross-sell field"
    - "section-1-demo-narrative.md cites D-03 (sector generalization) and D-07 (fraud out of escalation scope) literally, and explicitly states fraud-signal surfacing is out of the escalation beat's scope and rides the ADD-02 backend reveal"
  artifacts:
    - path: ".planning/spec/section-3-decision-logic.md"
      provides: "D-05/D-12/D-13 traceability citations"
      contains: "D-13"
    - path: ".planning/spec/section-4-mock-data-appendix.md"
      provides: "D-08 traceability citation"
      contains: "D-08"
    - path: ".planning/spec/section-1-demo-narrative.md"
      provides: "D-03/D-07 traceability citations + fraud-out-of-scope statement"
      contains: "D-07"
  key_links:
    - from: "01-CONTEXT.md D-05/D-12/D-13"
      to: "section-3-decision-logic.md"
      via: "inline (per D-NN) citation next to matching content"
      pattern: "D-(05|12|13)"
    - from: "01-CONTEXT.md D-08"
      to: "section-4-mock-data-appendix.md"
      via: "inline (per D-08) citation next to uninsured_device"
      pattern: "D-08"
    - from: "01-CONTEXT.md D-03/D-07"
      to: "section-1-demo-narrative.md"
      via: "inline (per D-NN) citation + explicit fraud-out-of-scope sentence"
      pattern: "D-(03|07)"
---

<objective>
Close the three decision-ID traceability gaps found in 01-VERIFICATION.md (verified 6/9). The four Phase-1 spec sections are substantively complete, internally consistent, and free of contradicting values — the ONLY failures are missing literal `D-NN` citation tokens (a citation-format/traceability gap, not a content gap). This project's decision-coverage gate requires literal `D-NN` citations, so these are treated as genuine gaps.

Purpose: Restore consistent decision-ID traceability across all four Phase-1 sections so Phase 1 can pass fully. Every other Phase-1 section already follows this convention; three sections applied it incompletely.

Output: Three spec files edited with inline `(per D-NN)` citations (and one added D-07 fraud-out-of-scope sentence). No content rewrites — citation insertions only.

**Scope discipline:** Do NOT rewrite, restructure, or re-argue any spec content. Do NOT touch dollar figures, rule rows, claim IDs, or values. Insert only the citation tokens and the single D-07 sentence specified below. The substance each citation points to is already present and correct (except D-07's substance, which requires one added sentence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations/01-VERIFICATION.md
@.planning/phases/01-foundations/01-CONTEXT.md

Threat model / UI blocks: **N/A** — this is a Markdown citation-token edit in a specification-authoring project (no code, no attack surface, no UI).

Decision text the citations must land next to (from 01-CONTEXT.md — read the full decisions before citing):
- **D-03:** White-label carrier is generic/unbranded; narrative states it generalizes to any personal-lines / device / gadget / contents insurer (pattern generalizes to broader P&C FNOL). No per-account branding.
- **D-05:** Deterministic "total-loss of a business-critical device with data loss" flag that ALWAYS routes to a human regardless of dollar amount (becomes DL-3).
- **D-07:** Fraud stays a rider on the backend-reveal specialist summary (ADD-02), NOT the escalation trigger.
- **D-08:** Seeded uninsured device (phone/tablet/home electronics) enables the device cross-sell; makes ADD-04 personalization land.
- **D-12:** Literal $1,000 auto-approve ceiling; rationale = roughly the cost of a manual adjuster touch; illustrative/configurable/auditable; never "the AI decides."
- **D-13:** Decision rows are literal if/then, all routed by a deterministic Python tool + Handoff Rule on a session variable (never RAG or instruction-only LLM judgment).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-05/D-12/D-13 citations to Section 3 (Decision Logic)</name>
  <files>.planning/spec/section-3-decision-logic.md</files>
  <read_first>
    - .planning/spec/section-3-decision-logic.md (the full file — currently zero D-NN tokens)
    - .planning/phases/01-foundations/01-CONTEXT.md (read D-05, D-12, D-13 decision text so each citation lands next to matching content)
  </read_first>
  <action>
    Insert three inline `(per D-NN)` citations. Do NOT change any rule, value, or prose beyond adding the parenthetical tokens.

    1. **D-13 → `### Business-Rules Table` heading.** D-13 = "decision rows are literal if/then, all routed by a deterministic Python tool + Handoff Rule." The Business-Rules Table (DL-1..DL-4) IS those literal if/then rows. Change the heading from `### Business-Rules Table` to `### Business-Rules Table (per D-13)`. (Alternatively, if you prefer the intro paragraph on line 3 that begins "This section is the **single source of truth for routing**", you may instead append `(per D-13)` to the end of the first sentence — but the heading is the cleaner anchor. Pick ONE; do not duplicate.)

    2. **D-12 → the $1,000 rationale paragraph in the Governance subsection.** The anchor is the bolded lead-in `**1. Configurable rationale for the $1,000 threshold.**` in `### Governance, Configurability & Audit Trail (DEC-03)`. Append the citation so it reads: `**1. Configurable rationale for the $1,000 threshold (per D-12).**`

    3. **D-05 → the DL-3 total-loss-always-escalate rule row (or its note).** D-05 = the total-loss/data-loss flag that always routes to a human regardless of amount. The cleanest anchor is the first "Notes on precedence and negatives" bullet, which begins "DL-3 is evaluated independently of DL-1/DL-2 and always wins". Append `(per D-05)` to that bullet, e.g. end it with "...the two are not mutually exclusive. (per D-05)". (Alternatively append `(per D-05)` inside the DL-3 row's Outcome or Exercised-by cell — pick ONE anchor, do not duplicate.)
  </action>
  <verify>
    <automated>grep -oE 'D-(05|12|13)' ".planning/spec/section-3-decision-logic.md" | sort -u | tr '\n' ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE 'D-(05|12|13)' section-3-decision-logic.md | sort -u` returns all three of D-05, D-12, D-13
    - D-13 token sits on/next to the Business-Rules Table heading or its intro sentence
    - D-12 token sits next to the $1,000 configurable-rationale paragraph in the Governance subsection
    - D-05 token sits next to the DL-3 total-loss-always-escalate row or its precedence note
    - No rule row, dollar value, or existing prose changed other than the three inserted `(per D-NN)` tokens
  </acceptance_criteria>
  <done>Section 3 contains literal D-05, D-12, and D-13 citations, each adjacent to the content that already implements that decision; no content rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-08 citation to Section 4 (Mock Data Appendix)</name>
  <files>.planning/spec/section-4-mock-data-appendix.md</files>
  <read_first>
    - .planning/spec/section-4-mock-data-appendix.md (Primary Claimant Profile section ~lines 25-36 and the surrounding cross-sell prose)
    - .planning/phases/01-foundations/01-CONTEXT.md (read D-08 decision text so the citation lands next to the uninsured_device seed)
  </read_first>
  <action>
    Insert ONE inline `(per D-08)` citation. The `uninsured_device` field is already correctly seeded (Jordan Rivera has one, Sam Okafor does not) — only the citation token is missing. Do NOT change the seed values.

    Anchor option A (preferred): the `uninsured_device` row in the Primary Claimant Profile table (~line 36). Append the citation to the field's description cell so it reads: `Recently purchased smartphone (purchased 2026-06, NOT currently on any policy) — seeded cross-sell/personalization hook (per D-08)`.

    Anchor option B (acceptable alternative): the profile intro sentence on line 27 ("Drives the demo run, the cross-sell moment, and the personalization 'wow' (seeded uninsured device makes the offer feel earned rather than generic).") — append `(per D-08)` to that sentence.

    Pick ONE anchor; do not duplicate. The D-08 token must sit next to `uninsured_device` content.
  </action>
  <verify>
    <automated>grep -c 'D-08' ".planning/spec/section-4-mock-data-appendix.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-08' section-4-mock-data-appendix.md` returns >= 1
    - The D-08 token appears next to the `uninsured_device` field (Primary Claimant Profile table row or its surrounding prose)
    - No seed value changed — Jordan Rivera still has an uninsured smartphone, Sam Okafor still has none
    - Existing citations (D-01/D-02/D-04/D-05/D-10/D-11) remain intact
  </acceptance_criteria>
  <done>Section 4 contains a literal D-08 citation adjacent to the uninsured_device cross-sell seed; no seed values changed.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-03 citation and D-07 citation+sentence to Section 1 (Demo Narrative)</name>
  <files>.planning/spec/section-1-demo-narrative.md</files>
  <read_first>
    - .planning/spec/section-1-demo-narrative.md (Beats 7-8 empathy/escalation ~lines 19-21, White-Label & Sector Generalization ~lines 44-50, Flagged Validation Items ~lines 61-67)
    - .planning/phases/01-foundations/01-CONTEXT.md (read D-03 and D-07 decision text; note D-07's substance — fraud is an ADD-02 rider, NOT the escalation trigger)
  </read_first>
  <action>
    Two edits. D-03 is a citation-only insertion (substance already present). D-07 requires a citation AND one added affirmative sentence (its substance is currently absent — the escalation beats never mention fraud at all).

    1. **D-03 → White-Label & Sector Generalization (citation only).** Anchor: the `### White-Label & Sector Generalization` heading (line 44) OR its first sentence (line 46, "The carrier (Meridian Device Protection) and the specific product... are **generic, unbranded, swappable placeholders**..."). Append `(per D-03)` to the heading — `### White-Label & Sector Generalization (per D-03)` — OR to the end of the first sentence. Pick ONE; do not duplicate. (Note: D-01 is already cited on line 50 of this subsection; leave it.)

    2. **D-07 → explicit fraud-out-of-escalation-scope statement (citation + new sentence).** D-07's substance is missing, so add ONE brief affirmative sentence. Place it at the END of Beat 8 (the HITL escalation beat, line 21) OR as a new item in the Flagged Validation Items list (after line 67). Preferred: append to Beat 8. Add this sentence (adjust wording lightly to match surrounding voice, but keep the tokens `ADD-02` and `D-07`):

       "Note: fraud-signal surfacing is deliberately OUT of scope for this escalation beat — fraud stays a rider on the backend-reveal specialist summary (ADD-02), not the escalation trigger (per D-07)."

    Do NOT alter the routing logic, rule citations, or dollar figures in Beat 8; only append the sentence.
  </action>
  <verify>
    <automated>grep -oE 'D-(03|07)' ".planning/spec/section-1-demo-narrative.md" | sort -u | tr '\n' ' '; grep -c 'ADD-02' ".planning/spec/section-1-demo-narrative.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -oE 'D-(03|07)' section-1-demo-narrative.md | sort -u` returns both D-03 and D-07
    - D-03 token sits on/next to the White-Label & Sector Generalization heading or its first sentence
    - An explicit sentence near the empathy/escalation beat (or the Flagged Validation Items list) states that fraud-signal surfacing is out of the escalation beat's scope and rides the ADD-02 backend reveal, referencing D-07
    - The added sentence references `ADD-02` literally (grep 'ADD-02' still matches, now including the new sentence)
    - No existing beat logic, rule ID, or dollar figure changed; existing citations (D-01/D-06/D-08/D-09/D-11/D-14) remain intact
  </acceptance_criteria>
  <done>Section 1 contains literal D-03 and D-07 citations, and an explicit affirmative sentence stating fraud is out of the escalation beat's scope (rides ADD-02); no beat content rewritten.</done>
</task>

</tasks>

<verification>
Run all three grep gates and confirm expected tokens:

```bash
cd .planning/spec
grep -oE 'D-(05|12|13)' section-3-decision-logic.md | sort -u   # expect D-05 D-12 D-13
grep -c 'D-08' section-4-mock-data-appendix.md                    # expect >= 1
grep -oE 'D-(03|07)' section-1-demo-narrative.md | sort -u        # expect D-03 D-07
grep -c 'ADD-02' section-1-demo-narrative.md                      # expect >= 2 (existing Beat 9 + new D-07 sentence)
```

Confirm no substantive drift: `git diff --stat` should show only small additive line changes to the three files (no value/rule/beat deletions).
</verification>

<success_criteria>
- Section 3 cites D-05, D-12, D-13 literally, each next to matching content
- Section 4 cites D-08 literally next to `uninsured_device`
- Section 1 cites D-03 and D-07 literally, with an explicit fraud-out-of-escalation-scope sentence referencing ADD-02
- Zero content rewrites — only `(per D-NN)` token insertions plus one D-07 sentence
- All 9 Phase-1 verification truths now satisfiable (previously 6/9)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-05-SUMMARY.md` documenting which citation tokens were inserted at which anchors, and confirming the four grep gates pass.
</output>
