---
phase: 05-demo-build-multimodal-backend-reveal-multilingual
plan: 04
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - "app:a2f621e4/agents/case_summary (new)"
  - "app:a2f621e4/tools/generate_case_summary (new)"
  - "app:a2f621e4/tools/send_case_record_email (new)"
autonomous: true
covers_success_criteria: [3, 6]
requirements: []

must_haves:
  truths:
    - "The chat app can compose a six-section assessor briefing packet from session state alone, without inventing a figure."
    - "The chat app can deliver an arbitrary text body to the packet recipient as a real email, without touching the tool that sends the customer confirmation."
  artifacts:
    - path: "app:a2f621e4/agents/case_summary"
      provides: "SUMMARY / ACTION / CLAIM / DIAGNOSTIC / RULES FIRED / FLAGS packet composition on the HUMAN_REVIEW branch"
      contains: "RULES FIRED"
    - path: "app:a2f621e4/tools/generate_case_summary"
      provides: "agentTool exposing case_summary as a SYNCHRONOUS callable tool"
      contains: "agentTool"
    - path: "app:a2f621e4/tools/send_case_record_email"
      provides: "Resend delivery of a supplied packet body to the decided recipient with the decided subject prefix"
      exports: ["packet_text", "claim_ref"]
  key_links:
    - from: "app:a2f621e4/tools/generate_case_summary"
      to: "app:a2f621e4/agents/case_summary"
      via: "agentTool.agent resource name"
      pattern: "agents/[0-9a-f-]{36}"
    - from: "app:a2f621e4/agents/case_summary"
      to: "app:a2f621e4 variableDeclarations"
      via: "every {variable} the instruction interpolates is a declared session variable"
---

<objective>
Build the three pieces the chat app needs to produce and deliver an assessor briefing packet.
Build only — nothing is wired into `claim_intake` and no version is cut in this plan.

Purpose: 05-RESEARCH.md Pitfall 4 is precise — porting `case_summary` alone does not satisfy
success criterion 3, because the fork's own instruction says the packet "is for the file" and
sends it nowhere. Composition and delivery are two separate builds, and delivery must not put a
scratch on `send_claim_email`, which carries the single-send guarantee from the v1→v11 hardening.

Output: `case_summary` agent, `generate_case_summary` agentTool, and `send_case_record_email`
Python tool — all present on the chat draft app, all schema-verified, with every pre-existing
resource byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-RESEARCH.md
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-03-SPIKE-FINDINGS.md
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md
@.planning/spec/DEMO-RUNBOOK.md
</context>

<environment>
| | |
|---|---|
| API base | `https://ces.googleapis.com/v1beta` |
| Project / location | `insurance-agent-demo-500614` / `us` |
| Chat app (target) | `a2f621e4-9faf-505a-b804-22471f022366` |
| Reference fork (READ ONLY) | `9ae7a0c3-6511-413c-8cdb-0efe9e90d2b9` |
| Source `case_summary` agent in the fork | `projects/insurance-agent-demo-500614/locations/us/apps/9ae7a0c3-6511-413c-8cdb-0efe9e90d2b9/agents/eeb8edfd-97d3-42c7-ac84-dc5e89cd8000` |
| Chat baseline before this plan | 8 tools, 37 `variableDeclarations`, `claim_intake` instruction 18,496 chars, version v9 `160dc3b2` |
</environment>

<hard_constraints>
1. **Never `importApp`.** Use `apps.tools.create` / `apps.agents.create` and `apps.*.patch` with
   `updateMask`. Read back after every write. Every exported package on disk is STALE — do not
   read or reuse any zip.
2. **Never read, echo, print or copy the source of `resolve_claim` or `send_claim_email`.** The
   one permitted interaction is the scripted key transplant in Task 3, which extracts a single
   regex match and prints only a boolean and an integer length.
3. **Write only to `a2f621e4`.** Gate every mutating URL on that literal in-script; refuse
   `6e01e4a5` and `9ae7a0c3` for any non-GET. The fork is read-only source material.
4. **No version cut, no deployment repoint, no live conversation in this plan.** Deployment
   `d7bfbb93` must still serve `160dc3b2` when the plan ends, asserted by re-read. This plan
   spends **zero** CES conversation quota.
5. **Large instruction text lives in files**, never inline in tool-call arguments — two prior
   executor sessions died composing an 18k-char instruction inline. Print lengths, booleans and
   short excerpts only.
6. **Regression canaries, asserted before and after:** `claim_decision_card` and
   `cover_offer_actions` `widgetTool` definitions byte-identical; `claim_intake` instruction
   byte-identical at 18,496 chars; `variableDeclarations` count 37; the 8 pre-existing tool
   `displayName`s unchanged.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create the case_summary agent on the chat app, briefing-packet branch only</name>

  <read_first>
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-RESEARCH.md` — "Pattern: agent-as-tool, confirmed live", which names the eleven session variables the source agent reads and its `<constraints>` posture
    - The live fork agent itself: `GET .../apps/9ae7a0c3-.../agents/eeb8edfd-97d3-42c7-ac84-dc5e89cd8000` (read-only; it is an instruction, it holds no key)
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md` — the `photo_status` / photo-gate vocabulary the chat app actually uses today
  </read_first>

  <files>app:a2f621e4/agents/case_summary (new); scratchpad: case_summary-source.txt, case_summary-adapted.txt, varcheck.json</files>

  <action>
`GET` the fork's `case_summary` agent and write its instruction to a scratchpad file. Do not print
it. Print its length and the list of `{variable}` tokens it interpolates.

Adapt it into a new instruction file, making exactly these changes and no others:

1. **Delete the `AUTO_APPROVE` branch entirely.** The chat app already sends the customer
   confirmation email via `send_claim_email`, which is part of the single-send guarantee. A second
   customer-email composer would put that invariant at risk for no gain. The adapted agent
   composes the six-section briefing packet only: `SUMMARY`, `ACTION`, `CLAIM`, `DIAGNOSTIC`,
   `RULES FIRED`, `FLAGS` — the exact six the roadmap's success criterion 3 names.
2. **Add an explicit guard at the top of the taskflow:** if `decision` is not `HUMAN_REVIEW`,
   return the single token `NO_PACKET_REQUIRED` and nothing else. This makes the agent safe to
   call unconditionally and makes a wrong-branch invocation detectable in the conversation record.
3. **Preserve the `<constraints>` anti-invention posture verbatim** — the source forbids inventing
   any figure, date or reference, which is the same posture `resolve_claim` holds and the reason
   this agent is safe to add.
4. **Delete the fork's closing directive** *"Do not read it out to the customer — it is for the
   file. Carry on and close warmly."* That sentence is the reason the fork's packet goes nowhere;
   the equivalent instruction now lives in `claim_intake` (plan 05-05) and says something
   different. Replace it with: the packet is returned as this tool's response and is never spoken
   to the customer.
5. **Keep the packet in English regardless of `preferred_language`.** The packet's reader is an
   internal assessor, not the customer, so it is out of scope for success criterion 5's
   "every customer-facing string". State this explicitly in the instruction so a later Spanish plan
   does not translate it by reflex.

Then `POST` the agent to `a2f621e4` with `displayName: case_summary`, the adapted instruction read
from the file, and a tool list containing nothing (it composes text and returns; it calls no tool).
Read the created agent back and byte-compare its instruction against the file.

Finally, run the variable audit that decides whether the packet renders or prints raw braces:
extract every `{token}` from the adapted instruction, `GET` the chat app's `variableDeclarations`,
and assert every extracted token is a declared variable name. Print the token list and any
unmatched token. An unmatched token is a task failure — fix by renaming the token to the chat
app's actual variable name (the chat app's vocabulary wins over the fork's), never by adding a new
declaration in this task.
  </action>

  <acceptance_criteria>
    - `GET apps/a2f621e4/agents` returns an agent with `displayName == "case_summary"`; its `name` is captured for Task 2.
    - The created agent's instruction, read back from the API, is byte-identical to the adapted scratchpad file.
    - The instruction contains all six literal section headings `SUMMARY`, `ACTION`, `CLAIM`, `DIAGNOSTIC`, `RULES FIRED`, `FLAGS`, each exactly once as a heading.
    - The instruction contains the literal `NO_PACKET_REQUIRED` and the literal `HUMAN_REVIEW`.
    - The instruction contains no occurrence of `AUTO_APPROVE` as a branch head, and no occurrence of the string `for the file`.
    - Every `{token}` interpolated by the instruction matches a name in `GET apps/a2f621e4` `variableDeclarations`; the unmatched-token list is empty.
    - `GET apps/a2f621e4` still reports 37 `variableDeclarations` and the 8 pre-existing tool `displayName`s; `claim_intake`'s instruction is still 18,496 chars.
  </acceptance_criteria>

  <verify>
    <automated>Script: `GET` the new agent, byte-compare instruction to file, assert the six headings / the two literal tokens / the two absences, run the token-vs-`variableDeclarations` set difference and assert it is empty, and assert the 37/8/18,496 canaries.</automated>
  </verify>

  <done>`case_summary` exists on the chat app, composes only the briefing packet, refuses to compose on the auto-approve branch, and interpolates only variables that actually exist.</done>
</task>

<task type="auto">
  <name>Task 2: Expose case_summary as the generate_case_summary agentTool</name>

  <read_first>
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-RESEARCH.md` — the confirmed `agentTool` structure read directly from the fork (`{displayName, executionType: SYNCHRONOUS, agentTool: {name, agent}}`)
    - The agent resource name captured in Task 1
  </read_first>

  <files>app:a2f621e4/tools/generate_case_summary (new); scratchpad: tool-create body + retrieveToolSchema output</files>

  <action>
`POST` a tool to `a2f621e4` with `displayName: generate_case_summary`, `executionType: SYNCHRONOUS`,
and `agentTool: {name: "generate_case_summary", agent: "<the case_summary resource name from Task 1>"}`.

Do not copy the fork's `agent` value — it points at the fork's app and would make the chat app call
into a pre-hardening build. Assert in-script that the `agent` field contains the literal
`apps/a2f621e4-9faf-505a-b804-22471f022366/` and does **not** contain `9ae7a0c3`.

Read the tool back. Then `POST` to `{app}:retrieveToolSchema` with the new tool's resource name and
record the schema the platform reports, so 05-05 knows exactly what `claim_intake` must supply when
it calls the tool (an agentTool's input surface is not obvious from its definition).
  </action>

  <acceptance_criteria>
    - `GET apps/a2f621e4/tools` returns a tool with `displayName == "generate_case_summary"`, `executionType == "SYNCHRONOUS"`, and an `agentTool.agent` value containing `apps/a2f621e4-9faf-505a-b804-22471f022366/agents/`.
    - `agentTool.agent` does not contain the string `9ae7a0c3`.
    - `{app}:retrieveToolSchema` returns a schema for the tool without error, and the schema is saved to the scratchpad for 05-05.
    - The tool count on `a2f621e4` is now 9; the 8 pre-existing `displayName`s are all still present.
    - `claim_decision_card` and `cover_offer_actions` `widgetTool` objects are byte-identical to the baselines captured at the start of this plan.
  </acceptance_criteria>

  <verify>
    <automated>Script: `GET` the tool list, assert the 9-tool set and the two `widgetTool` byte-comparisons; assert the `agentTool.agent` substring conditions; `POST retrieveToolSchema` and assert a non-error response.</automated>
  </verify>

  <done>`generate_case_summary` is callable on the chat app and points at the chat app's own `case_summary`, not the fork's.</done>
</task>

<task type="auto">
  <name>Task 3: Build send_case_record_email without reading the tool that already sends mail</name>

  <read_first>
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-03-SPIKE-FINDINGS.md` — `## PACKET_RECIPIENT`: the selected option, the literal recipient address, and the two subject-line prefixes
    - `.planning/spec/DEMO-RUNBOOK.md` — the "Only one mailbox can receive" note and the sender `onboarding@resend.dev` (public information, not a secret)
    - Resend's public HTTP API reference for `POST /emails` — write the call from the public docs, not from the existing tool
  </read_first>

  <files>app:a2f621e4/tools/send_case_record_email (new); scratchpad: tool source file, key-transplant script (never printed)</files>

  <action>
Build a new Python code tool. It is a new tool rather than a second mode on `send_claim_email`
deliberately: `send_claim_email` carries the single-send-email guarantee from the v1→v11 hardening
and is the regression canary on every run in this phase. It stays byte-identical.

**Signature:** `send_case_record_email(packet_text: str, claim_ref: str, customer_name: str)`.
`packet_text` is the full six-section packet returned by `generate_case_summary`. Declare
`packet_text` required, with a description stating that it must be reproduced word for word from
`generate_case_summary`'s response and that no figure may be altered — the same
reproduce-verbatim posture that made the decision explanation deterministic in `260810-k5x`.

**Body:** a single `ces_requests` POST to `https://api.resend.com/emails` with header
`Authorization: Bearer <key>` and JSON body `{from, to, subject, text}`, where:
  - `from` is `onboarding@resend.dev`
  - `to` is the literal recipient address recorded in `## PACKET_RECIPIENT`
  - `subject` is the packet subject-line prefix from `## PACKET_RECIPIENT`, followed by
    `claim_ref` and `customer_name`, so the packet is unmistakably distinct in the inbox from the
    customer confirmation
  - `text` is `packet_text` unmodified
Return a small dict: `{"sent": bool, "status_code": int, "subject": str}`. On any non-2xx, return
`sent: false` with the status code and do **not** raise — a mail failure must never break the
conversation, which is the behaviour the runbook already promises for the customer email.
Constrain the send to exactly one call per invocation; do not retry inside the tool.

**The key transplant — the only permitted interaction with guarded source.** Write a script that,
in a single process: `GET`s the `send_claim_email` tool by filtering the tool list on
`displayName == "send_claim_email"`, searches its source for the regex `re_[A-Za-z0-9_]{20,}`,
asserts **exactly one** match, and substitutes that match into a `__RESEND_KEY__` placeholder in
the new tool's source file, then `POST`s the new tool. The script prints only
`key_found: <bool>` and `key_len: <int>` — never the value, never any surrounding source line,
never the tool list. If the match count is not exactly 1, the script must abort before printing
anything else and the task fails; do not fall back to printing source to investigate.

After creating the tool, assert the placeholder is gone by checking the new tool's own source for
the literal `__RESEND_KEY__` in-script (boolean only), and assert that no file written under
`.planning/` in this plan matches the key regex.

**Do not send a test email in this plan.** Delivery is proven on the live run in 05-05, where it
can be asserted against a real escalated claim.
  </action>

  <acceptance_criteria>
    - `GET apps/a2f621e4/tools` returns a tool with `displayName == "send_case_record_email"`; tool count is now 10; all 8 original `displayName`s plus `generate_case_summary` are still present.
    - `{app}:retrieveToolSchema` on the new tool reports required parameters exactly `packet_text`, `claim_ref`, `customer_name` — no extra declared parameter.
    - An in-script boolean check reports the literal `__RESEND_KEY__` is absent from the created tool's source, and `key_found: true` with `key_len` a positive integer.
    - `grep -rE 're_[A-Za-z0-9_]{20,}' .planning/` returns no match.
    - The `send_claim_email` tool object is byte-identical before and after this plan.
    - `GET apps/a2f621e4/deployments/d7bfbb93-8cee-43fe-9095-bc5775f353bd` still serves `appVersion` ending `160dc3b2`.
    - No `runSession` call was made in this plan.
  </acceptance_criteria>

  <verify>
    <automated>Script: assert the 10-tool set; `retrieveToolSchema` parameter-name equality; the placeholder-absent boolean; `grep -rE 're_[A-Za-z0-9_]{20,}' .planning/` exits non-zero; byte-compare `send_claim_email` against its start-of-plan snapshot; re-`GET` the deployment and assert `160dc3b2`.</automated>
  </verify>

  <done>The chat app can deliver an arbitrary packet body to the decided recipient with a distinguishable subject, `send_claim_email` is untouched, and no key entered the transcript or the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor session → guarded tool source | `send_claim_email` holds a live Resend key; the key must move between tools without entering the transcript |
| new tool → Resend HTTP API | Claim data (synthetic PII) leaves the platform |
| model → `packet_text` parameter | The packet text crosses a model turn between two tool calls and could be paraphrased |
| draft app → deployment `d7bfbb93` | Unverified build must not reach the demo rig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | Resend key transplanted into `send_case_record_email` | mitigate | Single-process scripted regex transplant; prints only `key_found` boolean and `key_len` integer; abort-before-print on any match count ≠ 1; `grep -rE 're_[A-Za-z0-9_]{20,}' .planning/` asserted empty; key already baked into every version snapshot from v4 onward, so the runbook's post-demo revocation step covers the new tool too — add it there in 05-05 |
| T-05-02 | Information disclosure | Assessor packet content emailed externally | mitigate | `case_summary` interpolates only declared session variables sourced from the seeded synthetic policy table; Task 1's token audit proves no field comes from anywhere else; recipient fixed by the 05-03 user decision |
| T-05-08 | Tampering | `packet_text` paraphrased by the model between `generate_case_summary` and `send_case_record_email` | accept, then verify | Parameter description pins word-for-word reproduction; byte-identity is asserted on the live run in 05-05. The packet is an internal artifact, so a paraphrase degrades fidelity rather than breaking a customer-facing determinism guarantee. Pre-decided fallback if it fails: add a WORD-FOR-WORD sentinel to the `claim_intake` step, the technique proven in `260810-k5x` |
| T-05-03 | Tampering | Existing chat resources reverted by `importApp` or a wide `updateMask` | mitigate | `importApp` forbidden; only `create` calls in this plan, no `patch` of an existing resource; both `widgetTool`s and `send_claim_email` byte-compared before and after |
| T-05-05 | Tampering | Demo rig served an unverified build | mitigate | No version cut, no repoint; `d7bfbb93` re-read and asserted at `160dc3b2` |
| T-05-04 | Denial of service (self-inflicted) | CES quota | mitigate | Zero conversations in this plan by construction; no `runSession` call permitted |
| T-05-09 | Spoofing | New tool calls into the pre-hardening fork's agent | mitigate | Task 2 asserts `agentTool.agent` contains `apps/a2f621e4-…` and does not contain `9ae7a0c3` |
| T-05-SC | Tampering | Package installs | n/a | No npm/pip/cargo install in this phase |

No `high`-severity threat is left unmitigated.
</threat_model>

<verification>
- `GET apps/a2f621e4/tools` → 10 tools, including `generate_case_summary` and `send_case_record_email`.
- `GET apps/a2f621e4/agents` → includes `case_summary`.
- 37 `variableDeclarations`, `claim_intake` 18,496 chars, both `widgetTool`s and `send_claim_email` byte-identical.
- `d7bfbb93` still serves `160dc3b2`; zero conversations spent.
</verification>

<success_criteria>
- The chat app can compose a briefing packet and deliver a packet body by email.
- Nothing that already worked changed by a single byte.
- No Resend key appears in the transcript or anywhere under `.planning/`.
</success_criteria>

<output>
Create `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-04-SUMMARY.md` when done.
</output>
