---
phase: 05-demo-build-multimodal-backend-reveal-multilingual
plan: 05
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - "app:a2f621e4/agents/claim_intake"
  - "app:a2f621e4/versions (new: chat v10)"
  - "app:a2f621e4/deployments/d7bfbb93"
  - ".planning/spec/DEMO-RUNBOOK.md"
autonomous: false
covers_success_criteria: [3, 6]
requirements: []

must_haves:
  truths:
    - "An escalated chat claim produces a six-section assessor briefing packet and delivers it as a real email."
    - "The customer never sees or hears the packet."
    - "The customer still receives exactly one confirmation email on the same claim."
    - "Every v1→v11 hardening item and every 05-01/05-02 fix still holds on the same run."
  artifacts:
    - path: "app:a2f621e4/agents/claim_intake"
      provides: "the Case record step that calls generate_case_summary then send_case_record_email on the HUMAN_REVIEW branch only"
      contains: "generate_case_summary"
    - path: "app:a2f621e4/versions chat v10"
      provides: "a cut version carrying the packet path, served by d7bfbb93 only after every assertion passes"
    - path: ".planning/spec/DEMO-RUNBOOK.md"
      provides: "presenter instructions for the packet beat on Scenario D"
      contains: "briefing packet"
  key_links:
    - from: "app:a2f621e4/agents/claim_intake"
      to: "app:a2f621e4/tools/generate_case_summary"
      via: "an explicit taskflow step gated on decision == HUMAN_REVIEW"
      pattern: "generate_case_summary"
    - from: "app:a2f621e4/tools/generate_case_summary response"
      to: "app:a2f621e4/tools/send_case_record_email packet_text"
      via: "word-for-word reproduction across one model turn, byte-asserted on the live run"
---

<objective>
Wire the packet into the chat conversation, prove it on one live escalated claim, and ship it to
the demo rig only if every assertion passes.

Purpose: this is the task that turns success criterion 3 from "composed" into "delivered as a real
message". It is also the highest-regression-risk edit in the phase, because it touches
`claim_intake` — the 18,496-char instruction that carries the decision sentinel, the cross-sell
step and the photo gate, and the file that killed two executor sessions when edited inline.

Output: chat v10 cut and serving on `d7bfbb93`, or chat v9 still serving and a recorded failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-04-SUMMARY.md
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-03-SPIKE-FINDINGS.md
@.planning/quick/260810-k5x-make-the-agent-speak-the-decision-explan/260810-k5x-SUMMARY.md
@.planning/quick/260810-ifr-fix-cover-offer-actions-to-match-the-sdk/260810-ifr-SUMMARY.md
@.planning/spec/DEMO-RUNBOOK.md
</context>

<environment>
| | |
|---|---|
| API base | `https://ces.googleapis.com/v1beta` |
| Chat app | `a2f621e4-9faf-505a-b804-22471f022366` |
| Deployment | `d7bfbb93-8cee-43fe-9095-bc5775f353bd` |
| Rollback version | chat v9 `160dc3b2-571c-480f-b901-e4dbe8947f70` |
| Widget rig | `http://localhost:3000`, chat-messenger SDK v1.16, token broker — **already running, do not rebuild** |
| `POST /versions` | returns the version resource directly, **not** an LRO — read the id off the response, do not poll |
</environment>

<hard_constraints>
1. **The instruction is edited by scripted `str.replace()` only.** Write the replacement block to a
   file by heredoc; run a script that GETs the agent, asserts anchor uniqueness, replaces exactly
   one contiguous region, asserts content and a bounded length delta, PATCHes, and reads back
   byte-for-byte. Print only lengths, booleans and short excerpts. **Never compose the instruction
   inline in tool-call arguments** — two executor sessions died doing exactly that.
2. **Never `importApp`.** Direct `apps.agents.patch` with `updateMask=instruction`. Every export on
   disk is STALE.
3. **Never read, echo or copy `resolve_claim` / `send_claim_email` / `send_case_record_email`
   source.** Pin tool outputs from the conversation record's `toolResponse`.
4. **Write only to `a2f621e4`.** Refuse `6e01e4a5` and `9ae7a0c3` for any non-GET.
5. **The live run goes against the DRAFT app** (`runSession` with no `config.deployment`). The
   deployment stays on v9 until every assertion in Task 2 passes.
6. **The repoint is conditional.** If any assertion fails, do not repoint; leave `d7bfbb93` on
   `160dc3b2` and record the failure. There is no partial ship.
7. **Quota budget: one conversation, at most five turns**, paced ≥55 s. At most one spaced retry of
   a single quota-failed turn. No retry loops.
8. **Regression canaries that must hold on the same run:** `claim_decision_card` renders with a
   `productItem` byte-identical to `resolve_claim`'s `card_product_item`; `send_claim_email` fires
   in exactly one turn; the deterministic tariff figures are unchanged; `cover_offer_actions`'s
   `widgetTool` is byte-identical in the version snapshot.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the Case record step to claim_intake by scripted replacement</name>

  <read_first>
    - `.planning/quick/260810-k5x-make-the-agent-speak-the-decision-explan/260810-k5x-SUMMARY.md` — the exact scripted-replacement technique, the anchor-uniqueness gate and the six-assertion pattern that worked
    - `.planning/quick/260810-ifr-fix-cover-offer-actions-to-match-the-sdk/260810-ifr-SUMMARY.md` — the `<step name="Cross-sell">` / `Close after the offer` structure this edit must not disturb
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-04-SUMMARY.md` — the `generate_case_summary` schema from `retrieveToolSchema` and `send_case_record_email`'s three parameters
    - The live `claim_intake` instruction, GETed to a file and never printed whole
  </read_first>

  <files>app:a2f621e4/agents/claim_intake; scratchpad: claim_intake-live.txt, claim_intake-new.txt, replacement.txt, splice + patch scripts</files>

  <action>
GET `claim_intake` to a file. Assert its length is exactly 18,496 chars — the value `260810-k5x`
recorded. A different length means something changed underneath this plan; stop and report rather
than proceeding.

Author a replacement block in a file introducing a new `<step name="Case record">`, placed on the
escalation path **after** the specialist-handoff line the customer hears and **before** the step
that owns `end_session`. It must say, in the instruction's existing voice:

- **Trigger:** `decision` is `HUMAN_REVIEW`. On any other value, skip the step entirely — this is a
  second gate on top of the agent's own `NO_PACKET_REQUIRED` guard, so a misfire is impossible in
  two independent places.
- **Action 1:** call `{@TOOL: generate_case_summary}`.
- **Action 2:** call `{@TOOL: send_case_record_email}`, passing the tool's response **word for
  word** as `packet_text`, plus `claim_ref` and `customer_name`. Include a greppable sentinel in
  the same register as the decision sentinel already in this instruction:
  `THE PACKET IS REPRODUCED WORD FOR WORD - if you change a figure, a heading or a line break, the case record is wrong.`
- **Silence:** the packet is never spoken, summarised, quoted or referred to in front of the
  customer, and the customer is never told an email went to an assessor. The customer-facing line
  on this path is unchanged: the existing non-accusatory specialist handoff.
- **Ordering:** this step never delays, replaces or duplicates `{@TOOL: send_claim_email}`. The
  customer still gets exactly one confirmation email on this claim.

Run the splice script. It must assert, before PATCHing:
  1. the START and END anchors each occur **exactly once** in the live instruction;
  2. exactly one contiguous region changed;
  3. length delta within **+600 … +1,800** chars;
  4. the decision sentinel `THE CARD TEXT RESPONSE IS THE ONLY PLACE YOU SAY THE DECISION` survives;
  5. `<step name="Cross-sell">` occurs exactly once, and `Close after the offer`,
     `Skip this step entirely on the HUMAN_REVIEW path`, `Do NOT end the session in this turn`,
     `WORD FOR WORD`, `assess_screen_crack`, `claim_decision_card`, `cover_offer_actions`,
     `send_claim_email`, `end_session` and `</taskflow>` are all still present;
  6. `<step name="Case record">` occurs exactly once and `generate_case_summary` /
     `send_case_record_email` each occur at least once.

PATCH with `updateMask=instruction`, read the agent back and byte-compare against the file sent.
Assert `validationErrors` is absent, the tool list is still 10, `childAgents` and `transferRules`
are unchanged, and the `variableDeclarations` count is still 37.
  </action>

  <acceptance_criteria>
    - The pre-edit live instruction measured exactly 18,496 chars.
    - Anchor uniqueness: both anchors counted exactly 1 before replacement.
    - The post-PATCH instruction read back from the API is byte-identical to the file that was sent.
    - Length delta is within +600…+1,800 chars and exactly one contiguous region differs from the pre-edit text.
    - All ten preserved literals from assertion 5 are present in the post-edit instruction.
    - `<step name="Case record">` occurs exactly once.
    - `GET apps/a2f621e4/agents/claim_intake` reports no `validationErrors`; 10 tools; `childAgents` and `transferRules` byte-identical to pre-edit.
    - `GET apps/a2f621e4/deployments/d7bfbb93…` still serves `160dc3b2`.
    - The instruction body was never printed in full; only lengths, booleans and excerpts under 200 chars appear in the transcript.
  </acceptance_criteria>

  <verify>
    <automated>The splice script's own assertion battery, plus a post-PATCH `GET` byte-comparison and a re-`GET` of the deployment asserting `160dc3b2`.</automated>
  </verify>

  <done>`claim_intake` calls the packet path on the escalation branch only, with a word-for-word sentinel, and nothing else in the instruction moved.</done>
</task>

<task type="auto">
  <name>Task 2: Cut chat v10, prove the packet on one live escalated claim, repoint only if it passes</name>

  <read_first>
    - `.planning/spec/DEMO-RUNBOOK.md` — Scenario D and Scenario B turn scripts, and the seeded policy table
    - `.planning/quick/260810-k5x-make-the-agent-speak-the-decision-explan/260810-k5x-SUMMARY.md` — the conditional-repoint pattern and the `POST /versions` returns-a-version-not-an-LRO deviation
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-03-SPIKE-FINDINGS.md` — `## PACKET_RECIPIENT`, for the subject line to assert against
  </read_first>

  <files>app:a2f621e4/versions (new); app:a2f621e4/deployments/d7bfbb93; .planning/spec/DEMO-RUNBOOK.md; scratchpad: version snapshot, per-turn JSON, conversation record, assertion script</files>

  <action>
**Cut the version first, run against the draft app second.** `POST /versions` with a `displayName`
naming what changed (`chat v10 - assessor briefing packet composed by case_summary and delivered by send_case_record_email on the HUMAN_REVIEW branch`).
Read the version id straight off the POST response — it returns the version resource, not an LRO;
do not poll for `done`. `GET` the version and audit its snapshot: both `widgetTool`s present and
byte-identical to the v9 definitions, `case_summary` agent present, `generate_case_summary` and
`send_case_record_email` present, 10 tools, 37 variables, the `Case record` step present in the
snapshotted `claim_intake`.

**Assert the deployment is still on `160dc3b2` before the conversation starts.**

Then one conversation, against the DRAFT app (`runSession`, no `config.deployment`), turns paced
≥55 s. Use the liquid total-loss escalation — it reaches `HUMAN_REVIEW` in the fewest turns and
needs no photo:
  - `Hi, my name is Jordan Rivera and my policy is PDP100294`
  - `I spilled a full glass of water on my MacBook and now it won't turn on at all`
  - `Okay, thanks`

Assert from the conversation record — every one of these, all from `toolCall` / `toolResponse`
objects, never from transcript prose:

  1. `resolve_claim` returned `decision == "HUMAN_REVIEW"`, `claim_amount == 3000`, rules include
     `DL-3` and `DL-2`.
  2. `generate_case_summary` fired **exactly once**, and its response contains all six headings
     `SUMMARY`, `ACTION`, `CLAIM`, `DIAGNOSTIC`, `RULES FIRED`, `FLAGS`.
  3. Its response contains no `{` `}` brace pair — i.e. no unresolved variable token leaked into
     the packet.
  4. `send_case_record_email` fired **exactly once**, and the `packet_text` argument is
     **byte-identical** to `generate_case_summary`'s response.
  5. `send_case_record_email` returned `sent == true` with a 2xx `status_code`, and its `subject`
     starts with the packet subject prefix from `## PACKET_RECIPIENT`.
  6. `send_claim_email` fired in **exactly one** turn — the single-send guarantee.
  7. **No customer-facing agent text chunk anywhere in the conversation contains any of the six
     packet headings**, nor the string `assessor`, nor the packet subject prefix. The customer must
     not be shown the packet or told about it.
  8. `claim_decision_card` fired with a `productItem` byte-identical to `resolve_claim`'s
     `card_product_item`, `price.units` a JSON int, `nanos` 0, `currencyCode` `USD`.
  9. `cover_offer_actions` did **not** fire — there is deliberately no cross-sell on the escalation
     path.

**Repoint `d7bfbb93` to chat v10 only if assertions 1–9 all pass.** Read the deployment back and
assert `appVersion` ends with the new version id, `channelType` is `WEB_UI`, and `webWidgetConfig`
is byte-unchanged. If any assertion fails: do not repoint, leave `160dc3b2` serving, write the
failing assertion and the evidence to the summary, and stop.

Finally, update `.planning/spec/DEMO-RUNBOOK.md`: add the briefing-packet beat under Scenario D
(what arrives, in which mailbox, with which subject prefix, and how it differs from the customer
confirmation), and extend the "After the demo — revoke the Resend API key" note to name
`send_case_record_email` alongside `send_claim_email` / `resolve_claim`. Mark the inbox arrival as
**not yet confirmed by eye** until Task 3 closes it.
  </action>

  <acceptance_criteria>
    - A new version exists on `a2f621e4` whose snapshot contains `case_summary`, `generate_case_summary`, `send_case_record_email`, 10 tools, 37 `variableDeclarations`, and the literal `<step name="Case record">`.
    - The conversation record satisfies all nine numbered assertions above, each evidenced from a `toolCall` or `toolResponse` object.
    - Assertion 4 is an exact byte comparison, not a normalised or truncated one.
    - Exactly one conversation was run; total turns ≤ 5.
    - If all nine pass: `GET apps/a2f621e4/deployments/d7bfbb93…` returns `appVersion` ending in the new version id, `channelType == "WEB_UI"`, `webWidgetConfig` byte-identical to its pre-repoint value.
    - If any failed: `GET apps/a2f621e4/deployments/d7bfbb93…` returns `appVersion` ending `160dc3b2`, and the summary names the failing assertion number.
    - `.planning/spec/DEMO-RUNBOOK.md` contains the packet beat under Scenario D and names `send_case_record_email` in the key-revocation note.
    - No API call of any kind was issued to `6e01e4a5` or `9ae7a0c3`.
  </acceptance_criteria>

  <verify>
    <automated>The nine-assertion script against the saved conversation record, plus a deployment re-`GET` asserting the correct `appVersion` for whichever branch was taken, plus `grep -c 'briefing packet' .planning/spec/DEMO-RUNBOOK.md` returning ≥1.</automated>
  </verify>

  <done>Either chat v10 is live on `d7bfbb93` with all nine assertions passing, or v9 is still live and the exact failure is recorded. No middle state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the packet actually arrived, and that nothing on screen regressed</name>

  <read_first>
    - `.planning/spec/DEMO-RUNBOOK.md` — Scenario D, and the "Expected, not a bug" card artifacts
    - `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-03-SPIKE-FINDINGS.md` — `## PACKET_RECIPIENT`, for the mailbox and the two subject prefixes
  </read_first>

  <what-built>
An escalated chat claim now composes a six-section assessor briefing packet from session state and
sends it as a real email, alongside the customer confirmation that already shipped. The packet is
never shown to the customer. Chat v10 is serving on `d7bfbb93`, and the live run proved every
server-side assertion — but a server log cannot prove an email landed, and it cannot prove the
browser still draws the card and the buttons.
  </what-built>

  <how-to-verify>
The widget rig is already running at `http://localhost:3000` — **do not rebuild it. Reload the
page** so the widget picks up chat v10.

**1. Check the mailbox.** Open the recipient inbox recorded in `## PACKET_RECIPIENT`. From the run
this plan just made, you should find **two** emails for the same claim:
  - the customer confirmation, with its existing subject
  - the briefing packet, with the packet subject prefix followed by the claim reference and the
    customer name

Open the packet. It should contain six headings — `SUMMARY`, `ACTION`, `CLAIM`, `DIAGNOSTIC`,
`RULES FIRED`, `FLAGS` — with real values, not `{placeholders}`, and with `DL-3` and `DL-2` under
`RULES FIRED`. Every figure in it should match the claim: `$3,000`, `$25` excess, `HUMAN_REVIEW`.

**2. Run one escalation in the browser** — PDP100294, Jordan Rivera, *"I spilled a full glass of
water on my MacBook and now it won't turn on at all"*. Watch that the agent hands off to a
specialist **without** mentioning an assessor email, a packet, a case record, or any of the six
headings. The customer-facing experience must be identical to before this change.

**3. Regression canaries, in the same browser session:** run the auto-approve path afterwards
(*"some of the physical keys have stopped working, the screen is completely fine and undamaged,
there's no liquid, it works normally otherwise"*) and confirm the three things that are still
unwatched from 05-02 while you are there — the spoken decision line printing **once** above the
card, the card drawing as it did on 2026-08-09 (grey tile, title, right-aligned price, subtitle,
two divider rules), and the `Add it` / `Not now` buttons drawing in a single row after you say
*"that's everything, thanks"*.

**Report specifically:**
  - Did both emails arrive? Were the subjects distinguishable at a glance?
  - Any `{placeholder}` text or missing section in the packet?
  - Did the customer-facing conversation leak any mention of the packet?
  - Did the spoken decision line print once or twice?
  - Did the card and the buttons draw as described?
  </how-to-verify>

  <acceptance_criteria>
    - User confirms on screen: two emails arrived for the same claim with distinguishable subjects.
    - User confirms on screen: the packet body contains all six headings with substituted values and no `{placeholder}` braces.
    - User confirms on screen: the customer-facing escalation conversation contains no mention of the packet, an assessor email, or any packet heading.
    - User confirms on screen: the decision line, the decision card and the cross-sell buttons render as described — or reports the specific deviation.
    - The result is recorded in `05-05-SUMMARY.md` verbatim, including any deviation, and 05-02-SUMMARY.md's three open on-screen items are updated to closed or restated with the new evidence.
  </acceptance_criteria>

  <resume-signal>Type "approved" if all four confirmations hold, or describe exactly what you saw instead.</resume-signal>

  <done>Success criterion 3 is confirmed end to end on the chat channel by a human, and the three on-screen items 05-02 left open are either closed or restated with fresh evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model turn → `packet_text` | Packet text crosses a model turn between two tool calls and could be altered |
| agent → customer transcript | The packet is an internal artifact; leaking it to the customer transcript is a confidentiality failure and a demo failure |
| draft app → deployment `d7bfbb93` | The demo rig must never serve an unproven build |
| agent → Resend | A second outbound email path now exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | `packet_text` altered between `generate_case_summary` and `send_case_record_email` | mitigate | Word-for-word sentinel in the new `claim_intake` step, in the register proven by `260810-k5x`; byte-identity asserted as live assertion 4; failing it blocks the repoint |
| T-05-02 | Information disclosure | Packet content shown to the customer | mitigate | Explicit silence clause in the step; live assertion 7 scans every customer-facing text chunk for the six headings, `assessor` and the subject prefix; human confirmation in Task 3 |
| T-05-10 | Tampering | Double-send of the customer confirmation email | mitigate | Ordering clause in the step forbids delaying/duplicating `send_claim_email`; live assertion 6 requires exactly one firing turn |
| T-05-11 | Tampering | The instruction edit silently reverts the k5x/ifr fixes | mitigate | Anchor-uniqueness gate, single contiguous region, bounded delta, ten preserved-literal assertions, post-PATCH byte-comparison |
| T-05-05 | Tampering | Demo rig serves an unverified build | mitigate | Draft-app run; repoint conditional on nine assertions; rollback version `160dc3b2` named in the plan and in the runbook |
| T-05-01 | Information disclosure | Resend key | mitigate | No tool source read or printed; runbook key-revocation note extended to name the new tool |
| T-05-04 | Denial of service (self-inflicted) | CES quota | mitigate | One conversation, ≤5 turns, ≥55 s pacing, at most one spaced retry, no loops |
| T-05-SC | Tampering | Package installs | n/a | None in this phase |

No `high`-severity threat is left unmitigated.
</threat_model>

<verification>
- `claim_intake` read back byte-identical to what was sent; delta bounded; ten literals preserved.
- Nine live assertions pass on one escalated conversation.
- `d7bfbb93` serves chat v10 (pass) or `160dc3b2` (fail) — never anything else.
- DEMO-RUNBOOK.md carries the packet beat and the extended key-revocation note.
- A human has seen both emails and the unchanged customer-facing conversation.
</verification>

<success_criteria>
- Success criterion 3 holds on the chat channel: every resolved claim produces a reviewable artifact, and the escalated path's artifact is delivered as a real message.
- Success criterion 6 holds: nothing from v1→v11, 05-01 or 05-02 regressed on the same run.
</success_criteria>

<output>
Create `.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-05-SUMMARY.md` when done.
</output>
