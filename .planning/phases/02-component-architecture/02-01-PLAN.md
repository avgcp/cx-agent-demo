---
phase: 02-component-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/spec/section-5-agent-architecture.md]
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03]

must_haves:
  truths:
    - "A reader can find one §5 subsection per agent for the root Claims Concierge plus all five named sub-agents (Intake, Decisioning, Escalation/Human-Handoff, Backend Claims-Processing, Upsell/Cross-Sell), each stating responsibility, trigger, instruction guidance, inputs consumed, outputs produced, and tools called"
    - "The shallow-nesting design rule is stated explicitly as a rule other authors must follow, and damage assessment is specified as a native-multimodal CAPABILITY of the Intake sub-agent — not a separate deep-nested agent (ARCH-03)"
    - "The Backend Claims-Processing sub-agent is specified as ONE shared component across the autonomous and HITL branches with two output modes (customer email vs. human-assessor briefing packet) selected by the backend_output_mode variable — never duplicated as two components (ARCH-02)"
    - "A session-variable contract table exists with columns variable-name / type / produced-by-agent / consumed-by-agent(s) / Decision-Logic-row-or-Handoff-Rule-that-reads-it, and every variable traces to a named DL-N row or Handoff Rule in Decision Logic (Section 3)"
    - "§5 cross-references Phase 1 sections BY NAME (Decision Logic Section 3, Mock Data Appendix Section 4) rather than re-deriving thresholds, field names, or seed values"
    - "Instruction guidance for every agent is behavioral bullets plus a SHORT illustrative XML skeleton hint (<role>/<persona>/<constraints>/<taskflow> with <trigger>/<action>) — no near-final/paste-ready prompt text"
    - "Exact CX Agent Studio session-variable syntax and Handoff Rule UI mechanics are flagged as console-confirm items, not asserted as settled (SC#5)"
    - "Traceability to locked CONTEXT decisions: D-15: Upsell is the 5th named sub-agent, noted as opportunistic and MAY be an agent-as-tool, cross-sell target = uninsured other devices; D-17: explicit session-variable contract table is authored with the mandated columns and console-confirm flag; D-18: instruction guidance = behavioral bullets + short XML skeleton hint, no paste-ready prompts; D-05: total_loss_flag is a component-level always-escalate input; D-08: uninsured-device signal drives the Upsell agent"
  artifacts:
    - path: ".planning/spec/section-5-agent-architecture.md"
      provides: "Agent Architecture Spec (§5) — root Claims Concierge + five sub-agents + shallow-nesting design rule + session-variable contract table"
      contains: "## Agent Architecture Spec"
  key_links:
    - from: ".planning/spec/section-5-agent-architecture.md"
      to: "Decision Logic (Section 3)"
      via: "session-variable contract table mapping each variable to a DL-N row / Handoff Rule"
      pattern: "escalation_required"
    - from: ".planning/spec/section-5-agent-architecture.md"
      to: "Mock Data Appendix (Section 4)"
      via: "agent inputs/outputs reference named seed fields (claim_amount, total_loss_flag, uninsured_device)"
      pattern: "total_loss_flag"
---

<objective>
Author the **Agent Architecture Spec (Section 5)** — the root "Claims Concierge" agent plus a single shallow layer of five sub-agents (Intake, Decisioning, Escalation/Human-Handoff, Backend Claims-Processing, Upsell/Cross-Sell), one subsection each, plus the explicit shallow-nesting design rule (SC#3) and the session-variable contract table (D-17) that is the interface glue tying every multi-agent handoff back to the Phase 1 Decision Logic (Section 3).

Purpose: ARCH-01/02/03 require a concrete agent inventory precise enough that Phase 3's per-use-case specs can reference each agent by name without re-deriving. This section translates the Foundations (Sections 1–4) and the recommended composition in research/ARCHITECTURE.md into a buildable component spec — behavioral guidance and XML skeleton hints, NOT paste-ready prompt text (D-18).
Output: `.planning/spec/section-5-agent-architecture.md`

This is a spec-authoring task. It produces Markdown spec text, not code. "Acceptance" = properties of the spec text (unambiguous, buildable, internally consistent, traceable), not running-software behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-component-architecture/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/spec/section-3-decision-logic.md
@.planning/spec/section-4-mock-data-appendix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §5 header, shallow-nesting design rule, root Claims Concierge agent, and session-variable contract table</name>
  <files>.planning/spec/section-5-agent-architecture.md</files>
  <read_first>
    - .planning/phases/02-component-architecture/02-CONTEXT.md — SC#1 (root + 5 sub-agents, no deep nesting), SC#3 (shallow-nesting guardrail), SC#5 (console-confirm flags), D-17 (session-variable contract table columns + candidate variables), D-18 (behavioral bullets + short XML skeleton, no paste-ready prompts)
    - .planning/research/ARCHITECTURE.md — §"Recommended Agent/Tool/Data Composition" (root agent role), §"Structure Rationale", Anti-Pattern 3 (deep sub-agent nesting), §"Internal Boundaries" ({@AGENT:} delegation, Handoff Rule on session variable)
    - .planning/spec/section-3-decision-logic.md — DL-1..DL-4 rows, the `escalation_required`/`cross_sell_eligible` session variables, `evaluate_claim` + Handoff Rule mechanism (the contract table must trace to these)
    - .planning/spec/section-4-mock-data-appendix.md — named seed fields (`claim_amount`, `coverage_valid`, `total_loss_flag`, `data_loss_flag`, `uninsured_device`, `claim_id`) the agent inputs/outputs reference by name
    - .planning/research/STACK.md — session variables, Handoff Rules, {@AGENT:} routing primitives
  </read_first>
  <action>
    Create `.planning/spec/section-5-agent-architecture.md` starting with the top heading `## Agent Architecture Spec` and a one-paragraph intro stating this section defines the runtime component inventory for the white-label FNOL Claims Concierge, that it cross-references **Decision Logic (Section 3)** and the **Mock Data Appendix (Section 4)** by name rather than re-deriving thresholds or seed values, and that Phase 3 use-case specs reference each agent here by name.

    Then author these subsections:

    **### Shallow-Nesting Design Rule (SC#3 / ARCH-03)** — state as an explicit rule other authors MUST follow: the architecture is exactly two levels — one root ("steering") agent plus ONE flat layer of specialist sub-agents; sub-agents MUST NOT invoke further nested sub-agents (research Anti-Pattern 3: added routing hops increase latency and routing-confusion risk in a time-boxed live demo). State the corollary explicitly: **damage assessment is a native-multimodal CAPABILITY of the Intake sub-agent, NOT a separate agent** (Gemini image understanding is a model capability, so Intake receives the photo in-conversation and reasons over it directly). Note that the Upsell/Cross-Sell component MAY be realized as an "agent-as-tool" (a callable capability) rather than a full conversation-owning handoff (see D-15), which stays within — does not violate — the single-layer rule.

    **### Root Agent: Claims Concierge** — a subsection with these labeled fields:
    - **Responsibility:** single entry point for both chat and voice channels; sets persona/tone and white-label framing; understands the caller's goal; owns cross-cutting session variables; routes to the correct sub-agent via `{@AGENT: Name}` delegation.
    - **Trigger:** conversation start (any channel).
    - **Instruction guidance (D-18):** lead with behavioral bullets — what it owns (greeting, goal understanding, top-level routing, persona), must-dos (state the configurable-threshold framing out loud per Decision Logic §3, keep white-label/generic tone), must-nevers (never decide the dollar threshold itself — that is Decisioning's deterministic tool; never expose raw chain-of-thought). Then include a SHORT illustrative XML skeleton hint using `<role>`, `<persona>`, `<constraints>`, `<taskflow>` with `<trigger>`/`<action>` tags showing the intended shape only — 8-15 lines, clearly labeled "illustrative skeleton, not paste-ready prompt text."
    - **Inputs consumed / Outputs produced:** none of the branch variables directly; owns/initializes the top-level session variables listed in the contract table below.
    - **Tools called:** none directly (delegates); routing only.

    **### Session-Variable Contract (D-17)** — a Markdown table that is the interface glue. Columns EXACTLY: `Variable | Type | Produced by (agent) | Consumed by (agent(s)) | Decision-Logic row / Handoff Rule that reads it`. Author one row per variable (names are the locked contract for this spec):
    - `claim_estimate_amount` | number | Intake (damage-assessment capability + `damage_valuation` tool) | Decisioning | DL-1 / DL-2 (maps to §4 `claim_amount` for seeded claims)
    - `coverage_valid` | boolean | Intake (`coverage_lookup`) | Decisioning | DL-1
    - `total_loss_flag` | boolean | Intake | Decisioning | DL-3 (the D-05 always-escalate flag)
    - `escalation_required` | boolean | Decisioning (`evaluate_claim`) | Handoff Rule → Escalation sub-agent | the fork (Handoff Rule; §3 Determinism Mandate)
    - `backend_output_mode` | enum (`customer_email` \| `assessor_briefing`) | Decisioning / branch outcome | Backend Claims-Processing | selects SC#2 output mode
    - `uninsured_devices_present` | boolean | Intake (`coverage_lookup`) | Upsell/Cross-Sell | DL-4 (the D-08 device cross-sell trigger; maps to §3 `cross_sell_eligible`)
    - `claim_number` | string | Decisioning (`claim_number_generator`, autonomous path) | Backend Claims-Processing (customer email) | — (output artifact)

    Immediately after the table, add an **explicit SC#5 console-confirm flag** sentence: the table specifies variable NAMES and the data-flow contract; the exact CX Agent Studio session-variable declaration syntax and the exact Handoff Rule UI mechanics (variable-condition builder vs. Python) MUST be confirmed against the live console before building and are NOT asserted as settled here.
  </action>
  <acceptance_criteria>
    - File `.planning/spec/section-5-agent-architecture.md` exists and contains the heading `## Agent Architecture Spec`
    - Contains headings `### Shallow-Nesting Design Rule`, `### Root Agent: Claims Concierge`, and `### Session-Variable Contract`
    - Shallow-Nesting Design Rule section contains the words `native-multimodal` (or `native multimodal`) and `Intake`, and states damage assessment is a capability of Intake, not a separate agent
    - Session-Variable Contract table contains all seven variable tokens: `claim_estimate_amount`, `coverage_valid`, `total_loss_flag`, `escalation_required`, `backend_output_mode`, `uninsured_devices_present`, `claim_number`
    - The table's columns include `Produced by` and `Consumed by` and a Decision-Logic/Handoff-Rule column referencing at least `DL-1`, `DL-3`, and `DL-4`
    - Contains an explicit SC#5 console-confirm statement referencing `Handoff Rule` and session-variable syntax
    - Root Agent subsection contains a `<role>` and `<taskflow>` XML skeleton hint labeled illustrative/not paste-ready
    - Cross-references `Decision Logic (Section 3)` and `Mock Data Appendix (Section 4)` by name
  </acceptance_criteria>
  <verify>
    grep -q "## Agent Architecture Spec" .planning/spec/section-5-agent-architecture.md &&
    grep -q "### Shallow-Nesting Design Rule" .planning/spec/section-5-agent-architecture.md &&
    grep -q "### Session-Variable Contract" .planning/spec/section-5-agent-architecture.md &&
    grep -q "backend_output_mode" .planning/spec/section-5-agent-architecture.md &&
    grep -q "uninsured_devices_present" .planning/spec/section-5-agent-architecture.md &&
    grep -q "escalation_required" .planning/spec/section-5-agent-architecture.md &&
    grep -q "Handoff Rule" .planning/spec/section-5-agent-architecture.md &&
    grep -q "Section 3" .planning/spec/section-5-agent-architecture.md
  </verify>
  <done>§5 header, explicit shallow-nesting design rule (damage assessment = Intake capability), root Claims Concierge subsection (behavioral bullets + illustrative XML skeleton), and the seven-row session-variable contract table with produced-by/consumed-by/DL-mapping columns and an SC#5 console-confirm flag are all present and cross-reference Sections 3 and 4 by name.</done>
</task>

<task type="auto">
  <name>Task 2: Intake (with damage-assessment capability), Decisioning, and Escalation/Human-Handoff sub-agent subsections</name>
  <files>.planning/spec/section-5-agent-architecture.md</files>
  <read_first>
    - .planning/spec/section-5-agent-architecture.md — the session-variable contract table and root-agent subsection authored in Task 1 (each sub-agent's inputs/outputs MUST use those exact variable names; append, do not duplicate)
    - .planning/phases/02-component-architecture/02-CONTEXT.md — SC#3 (damage assessment inside Intake), D-18 (bullets + short XML skeleton), D-05 (total_loss_flag always-escalate)
    - .planning/research/ARCHITECTURE.md — §"Component Responsibilities" and §"Data Flow" (Intake + damage assessment, Decisioning deterministic fork Pattern 1, Escalation via end_session), §"Divergence Point"
    - .planning/spec/section-3-decision-logic.md — DL-1/DL-2/DL-3, `evaluate_claim`, Handoff Rule on `escalation_required`
    - .planning/spec/section-4-mock-data-appendix.md — Damage-Image Library expected outputs (IMG-CRACK-01/IMG-LIQUID-01), anchor claims CLM-24001/CLM-24002, deductible/coverage_limit
  </read_first>
  <action>
    Append three sub-agent subsections to `.planning/spec/section-5-agent-architecture.md`. Each subsection uses the SAME field set: **Responsibility**, **Trigger (when root delegates)**, **Instruction guidance** (behavioral bullets + a SHORT illustrative `<role>/<persona>/<constraints>/<taskflow>` XML skeleton hint, 8-15 lines, labeled "illustrative, not paste-ready" per D-18), **Inputs (variables/data consumed)**, **Outputs (variables produced)**, **Tools called**. Use the exact session-variable names from the Task 1 contract table and reference Section 3 / Section 4 by name (never re-derive thresholds or seed values).

    **### Sub-Agent: Intake** —
    - Responsibility: verify claimant identity, look up policy/coverage, capture FNOL loss details, request and reason over the damage photo.
    - Trigger: root delegates immediately after greeting/goal understanding.
    - Instruction guidance: must-dos (confirm identity against `policy_lookup`, capture loss type/date, request photo, run the damage-assessment capability), must-nevers (never guess a dollar figure not derivable from the photo/seed data; never make the escalate/approve decision — that is Decisioning). Include the illustrative XML skeleton.
    - **#### Damage-Assessment Capability (SC#3 / ARCH-03):** a nested `####` subsection stating this is a native Gemini multimodal capability OF the Intake agent (not a separate sub-agent); Intake receives the uploaded photo in-conversation and reasons over it, and the `damage_valuation` tool turns that read into structured severity + `claim_estimate_amount` + `total_loss_flag`, referencing the Section 4 Pre-Validated Damage-Image Library expected outputs (IMG-CRACK-01 → low severity / within auto-approve; IMG-LIQUID-01 → high severity / total loss).
    - Inputs: policy_id / claimant details; uploaded damage image; Section 4 seed records.
    - Outputs: `claim_estimate_amount`, `coverage_valid`, `total_loss_flag`, `uninsured_devices_present`.
    - Tools called: `policy_lookup`, `coverage_lookup`, `damage_valuation` (all Section 6 rows).

    **### Sub-Agent: Decisioning** —
    - Responsibility: apply the deterministic business rules from Decision Logic (Section 3) — DL-1..DL-4 — to set the routing variables; issue a claim number on the autonomous path.
    - Trigger: root delegates once `claim_estimate_amount`, `coverage_valid`, and `total_loss_flag` are set.
    - Instruction guidance: must-dos (call `evaluate_claim` to compute the branch deterministically, state the configurable-threshold/audit-trail framing per §3), must-nevers (NEVER let the LLM judge the threshold — the Python tool + Handoff Rule own the fork; never bypass DL-3 total-loss escalation even below $1,000 per D-05). Include the illustrative XML skeleton with a `<trigger>`/`<action>` showing the deterministic call.
    - Inputs: `claim_estimate_amount`, `coverage_valid`, `total_loss_flag`.
    - Outputs: `escalation_required`, `backend_output_mode`, and (autonomous path) `claim_number`.
    - Tools called: `evaluate_claim`, `claim_number_generator`.
    - State that the fork itself is a **Handoff Rule on `escalation_required`** (deterministic, not LLM-judged) — reference §3's Determinism Mandate by name; flag exact Handoff Rule syntax as SC#5 console-confirm.

    **### Sub-Agent: Escalation / Human Handoff** —
    - Responsibility: on `escalation_required = true`, compose a customer-facing escalation message and formally hand the session to a (simulated) human assessor — proving HITL is a warm, context-rich handoff, not a dead end.
    - Trigger: Handoff Rule fires when `escalation_required = true` (DL-2 amount > $1,000 OR DL-3 total_loss_flag).
    - Instruction guidance: must-dos (reassure the customer, set `backend_output_mode = assessor_briefing`, invoke Backend Claims-Processing before closing), must-nevers (never auto-approve; never drop the customer without the briefing packet). Include the illustrative XML skeleton.
    - Inputs: `escalation_required`, session state / transcript recap.
    - Outputs: sets/confirms `backend_output_mode = assessor_briefing`; session close signal.
    - Tools called: `end_session` (system tool — `ESCALATION_MESSAGE`, `session_escalated = true`, `LIVE_AGENT_HANDOFF`).
  </action>
  <acceptance_criteria>
    - Contains headings `### Sub-Agent: Intake`, `### Sub-Agent: Decisioning`, and `### Sub-Agent: Escalation / Human Handoff`
    - Intake subsection contains a `#### Damage-Assessment Capability` heading stating it is a native-multimodal capability of Intake (not a separate agent) and references `IMG-CRACK-01` or `IMG-LIQUID-01` and the `damage_valuation` tool
    - Decisioning subsection references `evaluate_claim`, sets `escalation_required`, and states the fork is a Handoff Rule (deterministic, not LLM-judged), referencing Decision Logic Section 3
    - Decisioning subsection states DL-3 / `total_loss_flag` always-escalates independent of amount (per D-05)
    - Escalation subsection references `end_session` and `backend_output_mode = assessor_briefing`
    - Each of the three subsections contains a `<taskflow>` XML skeleton hint labeled illustrative/not paste-ready
    - Each subsection uses the exact contract-table variable names (grep confirms `claim_estimate_amount` and `escalation_required` appear in these subsections)
  </acceptance_criteria>
  <verify>
    grep -q "### Sub-Agent: Intake" .planning/spec/section-5-agent-architecture.md &&
    grep -q "#### Damage-Assessment Capability" .planning/spec/section-5-agent-architecture.md &&
    grep -q "### Sub-Agent: Decisioning" .planning/spec/section-5-agent-architecture.md &&
    grep -q "### Sub-Agent: Escalation / Human Handoff" .planning/spec/section-5-agent-architecture.md &&
    grep -q "evaluate_claim" .planning/spec/section-5-agent-architecture.md &&
    grep -q "damage_valuation" .planning/spec/section-5-agent-architecture.md &&
    grep -q "end_session" .planning/spec/section-5-agent-architecture.md &&
    grep -q "assessor_briefing" .planning/spec/section-5-agent-architecture.md
  </verify>
  <done>Intake (with the native-multimodal Damage-Assessment capability subsection tying to Section 4 seed images and the damage_valuation tool), Decisioning (deterministic evaluate_claim + Handoff Rule on escalation_required, DL-3 always-escalate per D-05), and Escalation/Human-Handoff (end_session, assessor_briefing mode) are authored with the full six-field structure, illustrative XML skeletons, and exact contract-variable names.</done>
</task>

<task type="auto">
  <name>Task 3: Backend Claims-Processing (shared, dual output mode) and Upsell/Cross-Sell sub-agent subsections</name>
  <files>.planning/spec/section-5-agent-architecture.md</files>
  <read_first>
    - .planning/spec/section-5-agent-architecture.md — the contract table + Intake/Decisioning/Escalation subsections (reuse exact variable names; append, do not duplicate)
    - .planning/phases/02-component-architecture/02-CONTEXT.md — SC#2 (Backend = one shared component, two output modes), D-15 (Upsell = 5th named sub-agent, opportunistic, MAY be agent-as-tool, target = uninsured other devices), D-08 (device-bundle cross-sell), D-16 (repair-partner = deterministic Python tool feeding the autonomous email), D-18 (bullets + XML skeleton)
    - .planning/research/ARCHITECTURE.md — Pattern 2 (single backend sub-agent, dual output mode), Pattern 3 (opportunistic upsell), §"Data Flow" autonomous vs. HITL paths, §"Divergence Point" (both paths reconverge at Backend Claims-Processing)
    - .planning/spec/section-1-demo-narrative.md — the autonomous-path "recommended repair partner" email beat and the "cost center → profit center" cross-sell beat these subsections must map onto
    - .planning/spec/section-3-decision-logic.md — DL-4 (cross-sell trigger: uninsured_device present)
  </read_first>
  <action>
    Append two sub-agent subsections to `.planning/spec/section-5-agent-architecture.md`, using the same six-field structure (Responsibility, Trigger, Instruction guidance with bullets + short illustrative XML skeleton per D-18, Inputs, Outputs, Tools called) and the exact contract-table variable names.

    **### Sub-Agent: Backend Claims-Processing (shared, dual output mode — SC#2 / ARCH-02)** —
    - State explicitly and prominently: this is **ONE shared sub-agent invoked by BOTH the autonomous and HITL branches — never duplicated as two components**; the output differs only by the `backend_output_mode` variable. Both paths reconverge here (research §"Divergence Point").
    - Responsibility: consume the accumulated in-session state + a short transcript recap and produce a specialist-quality artifact; this is the "backend reveal" wow moment.
    - Trigger: root/branch delegates near end of conversation, after Decisioning (autonomous) or Escalation (HITL).
    - Instruction guidance: must-dos (branch on `backend_output_mode`: `customer_email` → sorry-about-your-laptop summary + issued `claim_number` + recommended repair partner from `repair_partner_lookup`; `assessor_briefing` → structured senior-specialist handoff packet for the human assessor), must-nevers (never build a real BigQuery/Cloud Function pipeline — this is in-session only, research Anti-Pattern 2; never duplicate the summarize logic into a second agent). Include the illustrative XML skeleton showing the `<taskflow>` branch on the mode variable.
    - Inputs: session state / transcript recap, `backend_output_mode`, `claim_number` (autonomous), `total_loss_flag` (briefing).
    - Outputs: drafted artifact (customer email OR assessor briefing packet) written to the Demo Outbox.
    - Tools called: `draft_email` (dual-template), `repair_partner_lookup` (autonomous email only).
    - Add a one-line note: real production would export conversation history to a pipeline — explicitly OUT of scope here ("path to production").

    **### Sub-Agent: Upsell / Cross-Sell (D-15)** —
    - State explicitly: this is the **fifth named sub-agent** (keeps the "root + 5 sub-agents" story literal), but it fires **opportunistically** — only when `uninsured_devices_present = true` (DL-4, the D-08 signal) — and **MAY be implemented as an "agent-as-tool"** (a callable capability) rather than a full conversation-owning handoff, so it NEVER hijacks the conversation (research Pattern 3: organic aha, not scripted step). Cross-sell target = **uninsured other devices** (per D-08), NOT a boat.
    - Responsibility: after the claim resolves, surface a natural, earned device-bundle offer that turns the interaction from cost center to profit center.
    - Trigger: opportunistic — activates only when `uninsured_devices_present = true` (a coverage-gap lookup confirmed an uninsured device); must not fire for a fully-insured profile (§4 Sam Okafor over-fire negative).
    - Instruction guidance: must-dos (reference the specific uninsured device from `coverage_lookup`, make one non-pushy bundle offer), must-nevers (never interrupt the claim resolution; never fire without a confirmed gap; never be pushy). Include the illustrative XML skeleton with a `<trigger>` gated on `uninsured_devices_present`.
    - Inputs: `uninsured_devices_present`, the uninsured-device record from `coverage_lookup` / Section 4.
    - Outputs: surfaced bundle offer (conversational; no new routing variable required).
    - Tools called: `coverage_lookup` (reused).
  </action>
  <acceptance_criteria>
    - Contains headings `### Sub-Agent: Backend Claims-Processing` and `### Sub-Agent: Upsell / Cross-Sell`
    - Backend subsection states it is ONE shared component across both branches with two output modes selected by `backend_output_mode`, and references both `customer_email` and `assessor_briefing`
    - Backend subsection references `draft_email` and `repair_partner_lookup` and notes the real-pipeline path-to-production is out of scope
    - Upsell subsection states it is the fifth named sub-agent, fires opportunistically gated on `uninsured_devices_present`, MAY be an agent-as-tool, and targets uninsured devices (not a boat)
    - Upsell subsection references the fully-insured over-fire negative (Sam Okafor) or that it must NOT fire without a confirmed gap
    - Both subsections contain a `<taskflow>` XML skeleton hint labeled illustrative/not paste-ready
  </acceptance_criteria>
  <verify>
    grep -q "### Sub-Agent: Backend Claims-Processing" .planning/spec/section-5-agent-architecture.md &&
    grep -q "### Sub-Agent: Upsell / Cross-Sell" .planning/spec/section-5-agent-architecture.md &&
    grep -q "customer_email" .planning/spec/section-5-agent-architecture.md &&
    grep -q "draft_email" .planning/spec/section-5-agent-architecture.md &&
    grep -q "repair_partner_lookup" .planning/spec/section-5-agent-architecture.md &&
    grep -q "agent-as-tool" .planning/spec/section-5-agent-architecture.md &&
    grep -qi "uninsured" .planning/spec/section-5-agent-architecture.md
  </verify>
  <done>Backend Claims-Processing is specified as ONE shared, dual-output-mode component (customer_email vs. assessor_briefing on backend_output_mode, in-session only) and Upsell/Cross-Sell is the fifth named but opportunistic, agent-as-tool-capable sub-agent gated on uninsured_devices_present targeting uninsured devices — both with full six-field structure and illustrative XML skeletons. §5 now covers root + all five sub-agents.</done>
</task>

</tasks>

<threat_model>
N/A — specification-authoring phase; no code, no runtime, no data surface. This plan produces Markdown spec text (agent responsibilities, session-variable contract, illustrative XML skeleton hints) only; there is no service, no live data path, and no attack surface to model. The determinism discipline (deterministic Python tool + Handoff Rule on a session variable, never LLM judgment) carried from Phase 1 is the relevant reliability control, not a security control.
</threat_model>

<verification>
- All three tasks' grep checks pass against `.planning/spec/section-5-agent-architecture.md`
- Exactly six agent subsections exist: root Claims Concierge + Intake + Decisioning + Escalation/Human-Handoff + Backend Claims-Processing + Upsell/Cross-Sell
- The session-variable contract table has all seven variables with produced-by/consumed-by/DL-mapping columns
- Damage assessment appears only as an Intake capability (`#### Damage-Assessment Capability`), never as its own `### Sub-Agent:` heading (shallow-nesting rule holds)
- Every threshold/seed value is referenced via Section 3 / Section 4 by name, never re-derived
- Each agent's instruction guidance includes both behavioral bullets and a short illustrative XML skeleton (D-18) — no paste-ready prompt text
</verification>

<success_criteria>
§5 is an unambiguous, buildable, internally consistent Agent Architecture Spec: root Claims Concierge + one shallow layer of five sub-agents (each with responsibility, trigger, instruction guidance, inputs, outputs, tools), the shallow-nesting design rule with damage assessment as an Intake capability, the shared dual-output-mode Backend Claims-Processing sub-agent, the opportunistic agent-as-tool-capable Upsell, and the session-variable contract table tracing every variable to Decision Logic (Section 3) — satisfying ARCH-01, ARCH-02, ARCH-03 and ROADMAP Phase 2 success criteria #1, #2, #3, and #5.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-01-SUMMARY.md`
</output>
