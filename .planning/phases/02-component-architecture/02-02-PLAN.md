---
phase: 02-component-architecture
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [.planning/spec/section-6-tool-data-inventory.md]
autonomous: true
requirements: [TOOL-01, TOOL-02]

must_haves:
  truths:
    - "The Tool & Data Inventory contains exactly one row per tool as a single source of truth, with columns name / type / purpose / inputs / outputs / source mock data (Section 4 ref) / called-by agent (Section 5 ref)"
    - "Every backend lookup (policy, coverage, claim creation, device-upsell/coverage-gap check, damage valuation) is specified as a deterministic mock Python code tool returning seeded JSON — NOT a Data Store/RAG lookup (TOOL-02)"
    - "The inventory is complete: it includes not only the 5 named backend lookups but ALL supporting tools the architecture needs — evaluate_claim, claim_number_generator, draft_email (dual-template), repair_partner_lookup, and the end_session system tool"
    - "The repair-partner lookup is a deterministic mock Python tool (not RAG), so the data-store count is stated as 0 — the demo has ZERO RAG/Data Store components — with a one-line 'path to production' footnote"
    - "Exact Handoff Rule UI/syntax and exact Python-tool session-variable mechanics are explicitly flagged as console-confirm items, not asserted as settled (SC#5)"
    - "§6 cross-references Phase 1 Mock Data Appendix (Section 4) and Decision Logic (Section 3) and the §5 agents BY NAME rather than re-deriving seed values, thresholds, or agent responsibilities"
    - "Traceability to locked CONTEXT decisions: D-16: repair-partner lookup is a deterministic mock Python tool, every tool in the inventory is Python (or the end_session system tool), data-store count = 0 with a path-to-production footnote, superseding the research repair_partner_search Data Store; D-17: each tool row's inputs/outputs use the exact session-variable names from the §5 contract table; D-12/D-13: evaluate_claim is the deterministic threshold tool routed via Handoff Rule; D-08: coverage_lookup performs the uninsured-device coverage-gap check"
  artifacts:
    - path: ".planning/spec/section-6-tool-data-inventory.md"
      provides: "Tool & Data Inventory (§6) — single-source-of-truth table, one row per tool; zero-RAG data-store note; SC#5 console-confirm flags"
      contains: "## Tool & Data Inventory"
  key_links:
    - from: ".planning/spec/section-6-tool-data-inventory.md"
      to: "Mock Data Appendix (Section 4)"
      via: "each tool row's 'source mock data' column names the Section 4 record it returns"
      pattern: "Section 4"
    - from: ".planning/spec/section-6-tool-data-inventory.md"
      to: "Agent Architecture Spec (Section 5)"
      via: "each tool row's 'called-by agent' column names the §5 agent + the session variables it reads/writes"
      pattern: "evaluate_claim"
---

<objective>
Author the **Tool & Data Inventory (Section 6)** — a single-source-of-truth table with exactly one row per tool (and the data-store count), that every Phase 3 use-case spec references by name instead of re-deriving. Every backend lookup is a deterministic mock Python code tool returning seeded JSON; per D-16 the repair-partner lookup is ALSO a Python tool, so the demo has ZERO RAG/Data Store components. Exact Handoff Rule syntax and Python-tool session-variable mechanics are flagged as console-confirm (SC#5).

Purpose: TOOL-01/TOOL-02 require one authoritative inventory so §5's agents and Phase 3's use cases reference tools by name without drift. This section makes the §5 agent architecture executable at the tool layer and locks the zero-RAG architectural property (D-16).
Output: `.planning/spec/section-6-tool-data-inventory.md`

This is a spec-authoring task. It produces Markdown spec text, not code. "Acceptance" = properties of the spec text (unambiguous, buildable, complete, traceable), not running-software behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-component-architecture/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/spec/section-3-decision-logic.md
@.planning/spec/section-4-mock-data-appendix.md
@.planning/spec/section-5-agent-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tool Inventory table — one deterministic row per tool, cross-referenced to §4 seed data and §5 agents</name>
  <files>.planning/spec/section-6-tool-data-inventory.md</files>
  <read_first>
    - .planning/spec/section-5-agent-architecture.md — the session-variable contract table + which agent calls which tool (this is the SOURCE for the "called-by agent" and inputs/outputs columns; reuse exact tool names and variable names)
    - .planning/phases/02-component-architecture/02-CONTEXT.md — SC#4 (one row per tool, every backend lookup = deterministic mock Python tool), the "full tool roster completeness" discretion note (evaluate_claim, claim_number_generator, draft_email dual-template, repair-partner lookup, end_session system tool), D-16 (repair-partner = Python tool), D-17 (session-variable names)
    - .planning/spec/section-4-mock-data-appendix.md — the seed records each tool returns (Primary Claimant Profile, Policy & Coverage, Anchor Claims, Damage-Image Library, uninsured_device) — the "source mock data" column names these by record/field
    - .planning/spec/section-3-decision-logic.md — DL-1..DL-4 + `evaluate_claim` mandate (the evaluate_claim row must reference this)
    - .planning/REQUIREMENTS.md — TOOL-01, TOOL-02
  </read_first>
  <action>
    Create `.planning/spec/section-6-tool-data-inventory.md` starting with the top heading `## Tool & Data Inventory` and a one-paragraph intro stating this is the single source of truth for all tools; that every Phase 3 use-case spec references these rows BY NAME rather than re-describing them; and that it cross-references **Mock Data Appendix (Section 4)** for seed data and **Agent Architecture Spec (Section 5)** for calling agents / session variables, never re-deriving.

    Then author a `### Tool Inventory` subsection containing a single Markdown table with EXACTLY these columns: `Tool | Type | Purpose | Inputs | Outputs | Source mock data (§4) | Called-by agent (§5)`. Author one row per tool below. Every backend lookup is `Python (deterministic)` returning seeded JSON — state that explicitly in the Type column; the only non-Python row is `end_session` (System tool). Use the exact §5 session-variable names in Inputs/Outputs.

    1. `policy_lookup` | Python (deterministic) | Verify claimant + return policy/claimant record | policy_id / claimant details | policy record, `coverage_valid` | §4 Primary Claimant Profile, Policy & Coverage | Intake
    2. `coverage_lookup` | Python (deterministic) | Return coverage validity AND run the uninsured-device coverage-gap check (per D-08) | policy_id / claimant profile | `coverage_valid`, `uninsured_devices_present` + uninsured-device record | §4 Policy & Coverage, `uninsured_device` field | Intake, Upsell/Cross-Sell
    3. `damage_valuation` | Python (deterministic) | Turn Intake's native-multimodal photo read into structured severity + estimate | damage-image ref + Gemini image read | `claim_estimate_amount`, `total_loss_flag` | §4 Pre-Validated Damage-Image Library (IMG-CRACK-01 / IMG-LIQUID-01 expected outputs) | Intake (damage-assessment capability)
    4. `evaluate_claim` | Python (deterministic) | Apply DL-1..DL-4 threshold/flag math (Decision Logic §3) → routing variables | `claim_estimate_amount`, `coverage_valid`, `total_loss_flag`, `uninsured_devices_present` | `escalation_required`, `backend_output_mode` | (reads session state; thresholds from §3) | Decisioning
    5. `claim_number_generator` | Python (deterministic/seeded) | Issue a claim number on the autonomous path (claim creation) | approved claim context | `claim_number` | §4 Anchor Claims (claim_id namespace) | Decisioning
    6. `draft_email` | Python (deterministic, dual-template) | Produce the backend-reveal artifact — customer email OR assessor briefing packet, selected by `backend_output_mode` | session state / transcript recap, `backend_output_mode`, `claim_number` | drafted email or briefing packet (to Demo Outbox) | §4 records (claimant, claim, repair partner) | Backend Claims-Processing
    7. `repair_partner_lookup` | Python (deterministic — NOT RAG, per D-16) | Return seeded recommended-repair-partner records for the autonomous customer email | loss type / location | repair-partner record(s) | §4 seeded repair-partner records | Backend Claims-Processing
    8. `end_session` | System tool (built-in) | Escalation/close: end session with reason + `session_escalated`, optional live-agent handoff | reason, `session_escalated`, handoff params | terminated session / handoff signal | — | Escalation/Human-Handoff (and normal close)

    After the table, add a short note: SC#4's five named backend lookups map to rows 1 (policy), 2 (coverage + upsell/coverage-gap check — note this supersedes the research "boat-insurance" framing; the target is uninsured devices per D-08), 5 (claim creation), 3 (damage valuation); rows 4, 6, 7, 8 are the supporting tools that complete the roster (inventory completeness = correctness).
  </action>
  <acceptance_criteria>
    - File `.planning/spec/section-6-tool-data-inventory.md` exists and contains the heading `## Tool & Data Inventory` and `### Tool Inventory`
    - The table contains all eight tool names: `policy_lookup`, `coverage_lookup`, `damage_valuation`, `evaluate_claim`, `claim_number_generator`, `draft_email`, `repair_partner_lookup`, `end_session`
    - The table columns include `Source mock data` (referencing `§4` / Section 4) and `Called-by agent` (referencing §5 agents)
    - Every backend-lookup row is typed `Python` and `deterministic`; `repair_partner_lookup` is explicitly marked Python / NOT RAG
    - `end_session` is typed as a System tool
    - `evaluate_claim` row references DL rules / Decision Logic and outputs `escalation_required` and `backend_output_mode`
    - `coverage_lookup` row outputs `uninsured_devices_present` (the D-08 coverage-gap check)
    - grep confirms `grep -c 'Python' section-6-tool-data-inventory.md` returns ≥ 7
  </acceptance_criteria>
  <verify>
    grep -q "## Tool & Data Inventory" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "### Tool Inventory" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "policy_lookup" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "repair_partner_lookup" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "evaluate_claim" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "claim_number_generator" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "draft_email" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "end_session" .planning/spec/section-6-tool-data-inventory.md &&
    [ "$(grep -c 'Python' .planning/spec/section-6-tool-data-inventory.md)" -ge 7 ]
  </verify>
  <done>A single Tool Inventory table with one row per tool (8 tools: 7 deterministic Python + end_session system tool) is authored, every backend lookup returns seeded JSON, repair_partner_lookup is Python (not RAG) per D-16, and every row cross-references Section 4 seed data and Section 5 calling agents / session variables by name.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-RAG data-store note (count = 0, path-to-production footnote) and SC#5 console-confirm flags</name>
  <files>.planning/spec/section-6-tool-data-inventory.md</files>
  <read_first>
    - .planning/spec/section-6-tool-data-inventory.md — the Tool Inventory table from Task 1 (append; do not duplicate)
    - .planning/phases/02-component-architecture/02-CONTEXT.md — D-16 (zero RAG, data-store count 0, one-line path-to-production footnote, supersedes research repair_partner_search Data Store), SC#5 (Handoff Rule UI/syntax + Python-tool session-variable mechanics flagged console-confirm)
    - .planning/research/ARCHITECTURE.md — Anti-Pattern 1 (Data Store for structured/threshold data is wrong), the superseded repair_partner_search Data Store row (now Python per D-16), MEDIUM-confidence note on exact UI mechanics
    - .planning/spec/section-3-decision-logic.md — Determinism Mandate (the Handoff Rule + session-variable mechanism whose exact syntax is console-confirm)
  </read_first>
  <action>
    Append two subsections to `.planning/spec/section-6-tool-data-inventory.md`:

    **### Data Store Inventory** — state plainly: **Data-store / RAG count = 0.** All structured mock data lives inside the deterministic Python tools above (seeded dicts/JSON), not in any Vertex AI Search Data Store. Explain WHY in one or two sentences: per D-16 the repair-partner directory — the research's one remaining RAG carve-out (`repair_partner_search`) — is superseded by the deterministic `repair_partner_lookup` Python tool, and per research Anti-Pattern 1 RAG is the wrong tool for any must-land/deterministic behavior in a live sales demo. Call out the ZERO-RAG property as a clean architectural + reliability win (deterministic on stage every time). Add a one-line **path-to-production footnote**: a real deployment could later move the repair-partner directory (and any FAQ/policy-wording content) to a RAG Data Store — out of scope for this demo.

    **### Console-Confirm Flags (SC#5)** — an explicit list of the items the implementation team MUST confirm against the live CX Agent Studio console before building, rather than treating this spec as settled UI truth:
    - Exact **Handoff Rule** UI/syntax and mechanics (variable-condition builder vs. Python) for forking on `escalation_required` (Decisioning → Escalation).
    - Exact **Python-tool session-variable mechanics** — how a Python tool reads from and writes to session state (the `context`/`ToolContext` object) and the exact variable-declaration syntax for the §5 contract-table variables.
    - Exact `end_session` parameter names/behavior (`session_escalated`, escalation message, live-agent handoff).
    State that this inventory specifies tool NAMES, purposes, and the data-flow contract — not the unverified console UI mechanics (this research is MEDIUM confidence on exact UI labels for a ~6-month-old product).
  </action>
  <acceptance_criteria>
    - Contains headings `### Data Store Inventory` and `### Console-Confirm Flags`
    - Data Store Inventory states the count is `0` (zero) and that every tool is a Python tool (no RAG/Data Store), referencing D-16 and superseding `repair_partner_search`
    - Contains a one-line path-to-production footnote about moving the partner directory to a RAG Data Store in a real deployment
    - Console-Confirm Flags list references `Handoff Rule` syntax, Python-tool session-variable mechanics, and `end_session` parameters as items to verify against the live console
  </acceptance_criteria>
  <verify>
    grep -q "### Data Store Inventory" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "### Console-Confirm Flags" .planning/spec/section-6-tool-data-inventory.md &&
    grep -qi "count = 0\|count of 0\|count: 0\|zero" .planning/spec/section-6-tool-data-inventory.md &&
    grep -q "Handoff Rule" .planning/spec/section-6-tool-data-inventory.md &&
    grep -qi "path to production\|path-to-production" .planning/spec/section-6-tool-data-inventory.md
  </verify>
  <done>The zero-RAG property is stated (data-store count = 0, all structured data in deterministic Python tools per D-16, repair_partner_search superseded) with a path-to-production footnote, and the SC#5 console-confirm flags (Handoff Rule syntax, Python-tool session-variable mechanics, end_session params) are explicitly listed as items to verify against the live console.</done>
</task>

</tasks>

<threat_model>
N/A — specification-authoring phase; no code, no runtime, no data surface. This plan produces a Markdown tool/data inventory table and console-confirm flags only; there is no service, no live data path, and no attack surface to model. The zero-RAG determinism property (all backend lookups are deterministic Python tools over seeded data) is the relevant reliability control, not a security control.
</threat_model>

<verification>
- Both tasks' grep checks pass against `.planning/spec/section-6-tool-data-inventory.md`
- Exactly one row exists per tool; all eight named tools present; 7 typed Python (deterministic), 1 (`end_session`) typed System tool
- No tool is a Data Store/RAG lookup; data-store count is stated as 0 (D-16)
- Every tool row's "source mock data" traces to a Section 4 record and "called-by agent" traces to a Section 5 agent — no re-derivation
- SC#5 console-confirm flags are present and explicit
</verification>

<success_criteria>
§6 is a complete, unambiguous, single-source-of-truth Tool & Data Inventory: one row per tool, every backend lookup a deterministic mock Python tool returning seeded JSON, a zero-RAG data-store count with path-to-production footnote (D-16), and explicit SC#5 console-confirm flags — cross-referencing Sections 3, 4, and 5 by name — satisfying TOOL-01, TOOL-02 and ROADMAP Phase 2 success criteria #4 and #5.
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-architecture/02-02-SUMMARY.md`
</output>
