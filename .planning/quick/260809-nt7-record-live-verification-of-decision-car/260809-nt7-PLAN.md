---
quick_id: 260809-nt7
type: quick
plan: 01
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/STATE.md
  - .planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/spec/DEMO-RUNBOOK.md
requirements: []
must_haves:
  truths:
    - "STATE.md records the widget-rendering blocker as VERIFIED, with the observed card text as evidence, and preserves the diagnosis history."
    - "STATE.md's 260806-u21 live-verification caveat is narrowed to the photo CONTRADICTION path only, because the CONFIRM path is now verified live."
    - "No project document claims cover_offer_actions works, that the contradiction path is verified live, or that the agent speaks the decision."
    - "05-02-SUMMARY.md's rendering section reads as confirmed-by-eye and justifies its status field explicitly."
    - "DEMO-RUNBOOK.md's chat section names chat v7, tells a presenter how to launch the widget, states the expected card text, and pre-warns about the cosmetic artifacts and the silent decision turn."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Revised blockers, new quick-task row, refreshed activity line"
    - path: ".planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md"
      provides: "Live-verification record and status justification"
    - path: ".planning/ROADMAP.md"
      provides: "Updated 05-02 plan line and current-version pointer"
    - path: ".planning/spec/DEMO-RUNBOOK.md"
      provides: "Presenter-runnable chat + photo + card path"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/phases/05-.../05-02-SUMMARY.md"
      via: "both name chat v7 bb14cdcc as the verified build"
      pattern: "bb14cdcc"
---

<objective>
Record the live user verification of decision-card rendering and photo upload through the
real web widget across four planning documents.

Purpose: three items that have been open for a week — widget rendering, photo upload through
the real embed, and the end-to-end chat claim — were closed by a single observed run. The
documents still say they are open, and the runbook still points a presenter at a stale
version and a scenario that no longer matches what the screen shows.

Output: revised `STATE.md`, `05-02-SUMMARY.md`, `ROADMAP.md`, `DEMO-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<hard_constraints>
**DOCUMENTATION ONLY.** No CES API calls of any kind. No `exportApp`, no `importApp`, no
`apps.patch`, no deployment reads or writes, no `runSession`, no `retrieveToolSchema`, no
`gcloud`. Nothing in this task touches a running system. If you find yourself reaching for
`curl` against `ces.googleapis.com`, stop — it is out of scope.

**Do not overstate.** Everything in `<observed_evidence>` was seen on screen. Everything in
`<still_open>` stays open and must not be marked done, ticked, or softened. Where you write
something that was inferred rather than observed, say so.

**Preserve history.** Revise STATE entries in place; do not delete the record of how a
problem was found or what was believed before. The corrections are part of the value.
</hard_constraints>

<observed_evidence>
This is the entire factual basis for the task. A fresh executor has no memory of the run —
everything you need is here. Quote from it; do not embellish it.

**The run.** On 2026-08-09 the user drove a full chat claim through the **real web widget
embed** — the console-generated embed on chat-messenger SDK **v1.16**, served locally at
`http://localhost:3000` with a token broker — against deployment
`d7bfbb93-8cee-43fe-9095-bc5775f353bd`, serving chat **v7 `bb14cdcc-d723-4be1-85af-9f4451e22ed5`**
of app `a2f621e4-9faf-505a-b804-22471f022366`. Screenshot evidence was reviewed.

**The conversation, verbatim:**

| Turn | Content |
|---|---|
| User | *"Hi"* |
| Agent | asks for name and policy ID |
| User | *"PDP100294, Jordan Rivera"* |
| Agent | *"Thanks, Jordan - putting you through now."* then *"I've got your Apple MacBook Pro 16\" here, what's happened to it?"* |
| User | *"cracked. no water but it still works"* |
| Agent | *"Since you've confirmed the screen is cracked, could you please attach a photo of the damage? I just need to see the screen clearly in the shot."* |
| User | **uploads a real photo of a cracked MacBook screen through the widget** — it works; the image displays inline in the conversation |
| Agent | *"I can see the cracks on the screen there."* — accurate vision read, correct device, matching device/policy pair |
| Agent | **the decision card RENDERS** (see below) |

**The card, as drawn:**

- Title `Apple MacBook Pro 16"`, right-aligned price **`$840.00`** on the same row
- Subtitle: `Screen replacement · CLM-24442 · $840 less $25 excess = $815 to you · Approved on the spot (on-the-spot limit $1,500)`
- Cosmetic artifacts, **exactly as predicted** by `260809-n1b`: a small empty grey image tile
  at the left, and two divider rules with nothing after them — the consequences of
  deliberately dropping `costBreakdown` and `actions`
- **None** of the failure modes the SDK contract warned about: no `$4,200.00` (the
  units-as-string 10× bug), no `$NaN`, no `$0.00` row, no broken-image glyph

**Arithmetic**, correct against the deterministic tariff: $840 screen tariff, $25 excess,
$815 payout, under the $1,500 cutoff → auto-approve.

**Three items closed by this one run:**

1. **Widget rendering** — open since 05-02 — VERIFIED by eye.
2. **Photo upload through the real web widget** — VERIFIED for the first time. Previously the
   photo path had only ever been proven via the API and the simulator.
3. **The live end-to-end chat claim through the real embed** — VERIFIED.

**A prior worry retired.** An image-upload error the user hit earlier in the session was a
race against the in-flight redeploy, not a defect. Specifically: the concern that browser
file upload might need Cloud Storage bucket configuration is **DISPROVEN** —
`enable-file-upload` worked with no storage config at all.
</observed_evidence>

<still_open>
Four things stay open. None of them may be ticked, closed, or softened.

1. **The decision explanation is not spoken.** The rule-and-why sentence appears ONLY in the
   card's small subtitle; the agent says nothing stating the decision. This weakens the
   "make the decision visible" wow moment — the audience gets *"I can see the cracks"* and
   then a card to squint at. Related to the `textResponseConfig: NONE` finding in
   `260809-n1b` (it means *"the LLM decides"*, not *"no text"*) and to `claim_intake`'s
   self-contradictory instruction. Record as an **open demo-quality issue** with the
   suggested direction: the agent states the decision aloud, the card reinforces it.
2. **`cover_offer_actions` is still broken** — same class of defect against the
   `quick_actions` contract, and worse: `b.actions.map()` throws on undefined rather than
   degrading to JSON. It has never fired in any test.
3. **The photo CONTRADICTION path is still not verified live** (crack reported, none visible
   → `photo_contradiction` → DL-5 human review). This run was the CONFIRM path. The
   `260806-u21` entry's *"NOT YET VERIFIED LIVE"* note must stay, **narrowed to the
   contradiction path only**.
4. **The accepted limitation stands:** photos confirm damage, not device identity. This run
   used a matching device/policy pair, exactly as required.
</still_open>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md
@.planning/spec/DEMO-RUNBOOK.md
@.planning/quick/260809-n1b-reshape-claim-decision-card-to-the-sdk-o/260809-n1b-SUMMARY.md
@.planning/quick/260806-u21-remove-wrong-subject-photo-guard-from-ch/260806-u21-SUMMARY.md
</context>

<facts>
Carry these identifiers exactly; do not paraphrase or truncate them.

| | |
|---|---|
| Chat app | `a2f621e4-9faf-505a-b804-22471f022366` — *Meridian Claim - Chat (hardened)* |
| Version verified | chat v7 `bb14cdcc-d723-4be1-85af-9f4451e22ed5` |
| Previous version | chat v6 `56a8b22a-2baf-4b18-9540-cdc6185acbee` |
| Deployment | `d7bfbb93-8cee-43fe-9095-bc5775f353bd` (`WEB_UI` / `CHAT_ONLY`) |
| Widget SDK | chat-messenger `v1.16`, console-generated embed, token broker, `http://localhost:3000` |
| Claim | `CLM-24442` |
| Policy | `PDP100294` — Jordan Rivera, Apple MacBook Pro 16" |
| Voice app (untouched) | `6e01e4a5-42a8-5213-b3da-c9053ff8ea52`, v11 `b17c9a26` |
| Date of run | 2026-08-09 |
</facts>

<tasks>

<task type="auto">
  <name>Task 1: Revise STATE.md — close what closed, narrow what narrowed, keep what is open</name>
  <files>.planning/STATE.md</files>
  <action>
Five edits, all in `.planning/STATE.md`. Preserve every existing sentence of history unless
this task says to change it.

**1. The widget-rendering blocker (currently the line beginning `- Phase 5 DIAGNOSED AND
FIXED, awaiting the user's eyes (2026-08-09, quick task 260809-n1b)`).**

Change the leading status from *"DIAGNOSED AND FIXED, awaiting the user's eyes"* to
**`VERIFIED LIVE (2026-08-09, quick tasks 260809-n1b diagnosis + fix, 260809-nt7
verification)`**. Keep the ROOT CAUSE narrative, the CORRECTION about *well-formed is not the
same as matching the renderer's contract*, and the note that the fix deliberately dropped
`costBreakdown` and `actions` — all of it earns its place.

Replace only the closing `WHAT REMAINS:` sentence with the verification record. It must state:
the user drove the claim through the real console-generated v1.16 widget embed at
`http://localhost:3000` with a token broker, against `d7bfbb93` serving chat v7 `bb14cdcc`;
**the card rendered**; it drew title `Apple MacBook Pro 16"` with right-aligned price
`$840.00` and the subtitle `Screen replacement · CLM-24442 · $840 less $25 excess = $815 to
you · Approved on the spot (on-the-spot limit $1,500)`; the two predicted cosmetic artifacts
appeared (an empty grey image tile at left, two divider rules with nothing after them) and
none of the warned-of failures did (no `$4,200.00`, no `$NaN`, no `$0.00` row, no
broken-image glyph). Note that the run was the photo CONFIRM path with a matching
device/policy pair, and that the SDK contract in `05-02-SUMMARY.md` is now
verified-by-observation rather than read-from-source-only.

**2. The 260806-u21 accepted-limitation entry** (the long line beginning `- Phase 5 ACCEPTED
LIMITATION (resolved 2026-08-06, quick task 260806-u21)`).

Its final clause currently reads `NOT YET VERIFIED LIVE against chat v6: the contradiction
path — proven offline in phototest2.py only; needs a photo of an undamaged device through the
widget.` Narrow it, do not delete it. The replacement must say: the photo CONFIRM path is now
**verified live** through the real widget on chat v7 `bb14cdcc` (2026-08-09) — a real
cracked-MacBook photo uploaded through the browser, read correctly (*"I can see the cracks on
the screen there."*), priced from the tariff at $840 and auto-approved as `CLM-24442`; the
**CONTRADICTION path remains NOT VERIFIED LIVE**, proven offline in `phototest2.py` only, and
still needs a photo of an undamaged device through the widget. Keep the STANDING DEMO
CONSTRAINT sentence about matching device/policy pairs exactly as it is.

**3. Add a new resolved-concern bullet** to Blockers/Concerns, immediately after the
widget-rendering entry:

`- Phase 5 (RESOLVED 2026-08-09, quick task 260809-nt7): browser file upload through the web
widget needs no Cloud Storage bucket configuration. The `enable-file-upload` flag on the
console-generated v1.16 embed was sufficient on its own — a real photo uploaded, displayed
inline in the conversation and reached the model, with no storage config anywhere. An
image-upload error seen earlier in the same session was a race against the in-flight
redeploy, not a defect. This was the first time the photo path had ever been exercised
through the real embed rather than the API or the simulator.`

**4. Strengthen the silent-decision-turn entry** (the line beginning `- Phase 5: the decision
turn draws the card but the agent says NOTHING alongside it`). It stays OPEN. Append that it
was **confirmed again on the 2026-08-09 real-widget run**: the agent said *"I can see the
cracks on the screen there."* and then produced the card with no sentence stating the
decision, so the rule-and-why lives only in the card's small subtitle. Add the demo-quality
consequence in one sentence — the audience gets a vision line and then a card to squint at,
which weakens the "make the decision visible" beat — and the suggested direction: the agent
should state the decision aloud and the card reinforce it, which needs
`textResponseConfig: {type: "LLM_GENERATED", ...}` plus resolving `claim_intake`'s
self-contradictory instruction block, then a live test. Do not claim any fix was applied.

**Leave the `cover_offer_actions` entry entirely unchanged.** It is still broken and still
has never fired.

**5. Housekeeping.**
- Frontmatter `last_updated`: `"2026-08-09T23:30:00.000Z"`.
- Frontmatter `last_activity` and the body `Last activity:` line: both to
  `2026-08-09 - Phase 5: decision card rendering, photo upload and the end-to-end chat claim
  all VERIFIED LIVE by the user through the real v1.16 widget embed against chat v7 bb14cdcc
  (CLM-24442, $840/$815, auto-approved); cover_offer_actions, the silent decision turn and
  the photo contradiction path remain open`.
- The body `Plan:` line under Current Position: replace *"decision-card rendering diagnosed
  and fixed in chat v7 `bb14cdcc`, awaiting the user's visual confirmation at
  http://localhost:3000"* with *"decision-card rendering VERIFIED LIVE through the real
  widget embed on chat v7 `bb14cdcc` (2026-08-09)"*. Keep the rest of that line, including
  the `cover_offer_actions` clause and the wrong-subject-guard clause.
- Add a row to the **Quick Tasks Completed** table:
  `| 260809-nt7 | Record live verification of decision-card rendering and photo upload through the real web widget | 2026-08-09 | (see log) | [260809-nt7-record-live-verification-of-decision-car](./quick/260809-nt7-record-live-verification-of-decision-car/) |`

Do not touch the progress counters — no plan completed.
  </action>
  <verify>
    <automated>F=.planning/STATE.md; grep -q "VERIFIED LIVE (2026-08-09" $F && grep -q "CLM-24442" $F && grep -q '\$840 less \$25 excess = \$815 to you' $F && grep -q "CONTRADICTION path" $F && grep -q "260809-nt7" $F && grep -q "no Cloud Storage bucket configuration" $F && grep -q "cover_offer_actions" $F && ! grep -q "awaiting the user's eyes" $F && ! grep -q "awaiting the user's visual confirmation" $F && echo PASS || echo FAIL</automated>
  </verify>
  <done>
STATE.md marks widget rendering VERIFIED with the observed card text as evidence and no
surviving "awaiting the user" wording; the 260806-u21 caveat is narrowed to the contradiction
path; the Cloud Storage concern is recorded as disproven; the silent-decision-turn and
`cover_offer_actions` entries remain open; the quick-task row and activity lines are updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close the rendering question in 05-02-SUMMARY.md and update ROADMAP Phase 5</name>
  <files>.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md, .planning/ROADMAP.md</files>
  <action>
**A. `05-02-SUMMARY.md`.**

Add a new section immediately after the *"What was built instead (chat v7 `bb14cdcc-...`)"*
section, headed **`## Rendering — CONFIRMED BY EYE 2026-08-09 (quick task 260809-nt7)`**. It
records the verification with the evidence: the real console-generated v1.16 embed at
`http://localhost:3000` with a token broker, against `d7bfbb93` serving chat v7 `bb14cdcc`;
the drawn card's title, right-aligned `$840.00` price and full subtitle verbatim; the two
predicted cosmetic artifacts present; and — stated as the load-bearing part — **none of the
five failure modes the SDK contract predicted occurred**: no `$4,200.00` from a
units-as-string, no `$NaN` from an absent `nanos`, no hardcoded `Sales tax $0.00` row, no
broken-image glyph in place of the 1×1 GIF, and no raw JSON. Say plainly that this makes the
`order_summary` contract documented above **verified by observation**, not only read from
source, and therefore safe to reuse for future widgets.

Note that this run also closed two things beyond rendering, neither of which 05-02 had ever
had evidence for: photo upload through the real browser widget (it needed no Cloud Storage
configuration — `enable-file-upload` alone sufficed), and the end-to-end chat claim through
the real embed. Note the vision read was accurate on a matching device/policy pair
(*"I can see the cracks on the screen there."*).

Then update the existing **"Still open on 05-02"** list at the end of the file. Rewrite its
first bullet — *"The card's on-screen render is still unconfirmed by eye"* — as **CLOSED
2026-08-09**, with a one-line pointer to the new section; strike it through or mark it
resolved rather than deleting it, so the record of what was open survives. Leave the other
three bullets (`cover_offer_actions`, the silent decision turn, the contradiction path) open
and untouched in substance — but narrow the contradiction bullet to say the **confirm** path
is now verified live and only the contradiction path is outstanding.

**Frontmatter `status`: keep `incomplete`, and justify it in the file.** Add one sentence at
the head of the "Still open" list explaining the decision: this plan is *"Decision card +
quick actions"*, and while the decision card is now built, deployed and visually confirmed,
`cover_offer_actions` — the other half of the plan's own title — has never fired once and is
known defective against the `quick_actions` contract, so the plan cannot be complete. Name
the silent decision turn as a second, lesser reason. State that exactly one of the plan's
four open items closed on 2026-08-09.

**B. `.planning/ROADMAP.md`, Phase 5 only.**

1. The `- [~] 05-02` plan line. Rewrite the middle of it so it no longer says
   `**rendering unverified** (Preview shows raw JSON; needs the real embed)`. It should now
   read that the card is **built, deployed and rendering-verified live through the real v1.16
   widget embed on chat v7 `bb14cdcc` (2026-08-09, `CLM-24442`, `$840`/`$815` auto-approved)`,
   and that what keeps it `[~]` is `cover_offer_actions` — never fired, defective against the
   SDK's `quick_actions` contract — plus the decision turn producing no spoken sentence
   alongside the card. **The checkbox stays `[~]`.** Keep the existing wrong-subject-guard
   clause and the `See 05-02-SUMMARY.md` pointer.
2. Success criterion **1**: append a dated verified-live note in parentheses — the confirm
   path was driven end to end through the real widget embed on 2026-08-09 (a real
   cracked-MacBook photo uploaded in the browser, read correctly, priced from the tariff at
   $840, auto-approved as `CLM-24442`), which is the first time it was exercised anywhere
   other than the API and the simulator. Do not alter the criterion's substance.
3. Success criterion **4**: append that the `WEB_UI` deployment's **file upload is now proven
   through the real browser embed and required no Cloud Storage configuration**, and that the
   phone deployment remains pinned and untouched at v11 `b17c9a26`.
4. The **Built so far** line: change the version from `56a8b22a` (chat v6 — wrong-subject
   guard removed) to **`bb14cdcc-d723-4be1-85af-9f4451e22ed5` (chat v7 — decision card
   reshaped to the SDK `order_summary` contract, rendering verified live 2026-08-09)**. This
   pointer is currently stale by one version. Leave the voice-app clause as it is.

**Do NOT touch success criterion 3.** It is about the customer confirmation email and the
assessor briefing packet — 05-03's scope. This run produced no evidence for it. Do not touch
criterion 2 either; the contradiction path it describes is still unverified live. Do not tick
any checkbox anywhere.
  </action>
  <verify>
    <automated>S=.planning/phases/05-demo-build-multimodal-backend-reveal-multilingual/05-02-SUMMARY.md; R=.planning/ROADMAP.md; grep -q "CONFIRMED BY EYE 2026-08-09" $S && grep -q "260809-nt7" $S && grep -q '\$815' $S && grep -q "^status: incomplete" $S && grep -q "bb14cdcc" $R && ! grep -q "rendering unverified" $R && grep -q '\- \[~\] 05-02' $R && ! grep -q '56a8b22a (chat v6' $R && echo PASS || echo FAIL</automated>
  </verify>
  <done>
05-02-SUMMARY.md has a dated rendering-confirmed section with the observed card text, keeps
`status: incomplete` with a written justification naming `cover_offer_actions`, and its
"Still open" list shows one item closed and three still open. ROADMAP Phase 5's 05-02 line no
longer claims rendering is unverified, still shows `[~]`, criteria 1 and 4 carry dated
verified-live notes, criteria 2 and 3 are untouched, and "Built so far" names chat v7.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make the chat + photo + card path presenter-runnable in DEMO-RUNBOOK.md</name>
  <files>.planning/spec/DEMO-RUNBOOK.md</files>
  <action>
**Read the file first.** Note a correction to the brief that spawned this task: the runbook is
**not** voice-only. It already has a `# Chat channel — the photo demo` section (around line
109) with Scenario C (photo confirms) and Scenario D (photo disagrees), the matching
device/policy-pair callout, and the seeded-policy table. So this is an **update of an existing
section, not a new section** — which is why it fits in this task rather than becoming a
follow-up.

Five edits, all inside or below the chat section. Keep the runbook's voice: short, direct,
readable standing up.

**1. The chat-channel fact table** (`App` / `Version live` / `Deployment` / `Widget embed`).
- `Version live` is stale at `56a8b22a-…` *chat v6*. Change to
  `bb14cdcc-d723-4be1-85af-9f4451e22ed5` — *chat v7, decision card renders*.
- `Widget embed` currently reads `☐ get from the console — Deployments → chat - meridian demo`.
  Replace with what actually worked: the console-generated embed snippet from **Deployments →
  *chat - meridian demo* → the embed/integration panel**, served from a local page with a
  token broker at `http://localhost:3000`, on chat-messenger SDK **v1.16**, with
  `enable-file-upload` set on the `chat-messenger-container` element. Add one line: **no
  Cloud Storage bucket or `url-allowlist` configuration is needed** — file upload worked with
  none, and the card's placeholder image is a `gstatic.com` URL the SDK hard-trusts.

**2. The "Verified on a real photo" callout.** It currently credits the **simulator**. Upgrade
it, and be precise about what changed: on 2026-08-09 the whole path was driven through the
**real widget in a browser** — a real cracked-MacBook photo was uploaded, displayed inline,
and read correctly (*"I can see the cracks on the screen there."*), then priced from the
tariff at $840 and auto-approved as `CLM-24442` with the decision card drawn on screen. Keep
the existing advice to rehearse once with *your own* photo, since accuracy depends on the
image.

**3. Scenario C — add the card, and match the observed dialogue.** The scenario table is
close to what happened but stops before the card. Extend it:
- Add a row after the photo row for the decision card, and immediately below the table show
  the presenter exactly what to expect on screen, so nothing is a surprise:
  - Title `Apple MacBook Pro 16"` with `$840.00` right-aligned on the same row
  - Subtitle `Screen replacement · CLM-24442 · $840 less $25 excess = $815 to you · Approved
    on the spot (on-the-spot limit $1,500)` (the claim number will differ each run)
- Add a short **"Expected, not a bug"** note listing the two cosmetic artifacts a presenter
  will see and should not be alarmed by: **a small empty grey image tile at the left of the
  card**, and **two horizontal divider rules below the row with nothing after them**. Explain
  both in one clause — there is no product image, and no cost breakdown, payment method or
  buttons are sent, deliberately, because the SDK would otherwise print an unchangeable
  "Sales tax $0.00" line and live buttons that inject text into the conversation when tapped.
- Add a **presenter warning** that the agent will most likely say **nothing** alongside the
  card — the decision and the reason are in the card's small subtitle only, so **read the
  card out loud** ("$840 to fix, $25 excess, $815 to you, approved on the spot because it's
  under the $1,500 limit") rather than waiting for the agent to say it. Note this is a known
  open issue, not a fault of the run.
- Note that terse input works: the verified run used just *"PDP100294, Jordan Rivera"* and
  *"cracked. no water but it still works"*.

**4. Scenario D — add an honest, prominent caveat.** It is currently written as if it will
certainly work. It is proven **offline only** (`phototest2.py`) and has **never been run live
through the widget**. Add a short warning immediately under the Scenario D heading saying so,
and advise rehearsing it with the undamaged-device photo before showing it to a customer.
Keep the rest of the scenario, including the non-accusatory quote and the DL-5 note — the
logic is unchanged and the risk is low, it is simply unverified.

**5. Known cosmetic issues.** Add two chat-channel entries to the existing list: the card's
empty grey tile plus two trailing divider rules (cross-referencing the Scenario C note), and
the silent decision turn (agent draws the card without a sentence — read the card aloud).

**Constraint on the matching device/policy-pair callout**: leave it exactly as it is. It is
still true, still load-bearing, and the verified run honoured it.
  </action>
  <verify>
    <automated>F=.planning/spec/DEMO-RUNBOOK.md; grep -q "bb14cdcc" $F && grep -q "localhost:3000" $F && grep -q "enable-file-upload" $F && grep -q '\$815' $F && grep -q "PDP100294" $F && grep -q -i "divider" $F && grep -q -i "read the card" $F && ! grep -q "56a8b22a-2baf-4b18-9540-cdc6185acbee" $F && echo PASS || echo FAIL</automated>
    <human-check>A presenter who has never seen this build could open the runbook, launch the widget, run the claim, and be surprised by nothing on screen.</human-check>
  </verify>
  <done>
DEMO-RUNBOOK.md's chat section names chat v7 `bb14cdcc`, tells a presenter concretely how to
launch the widget (console embed, token broker, `http://localhost:3000`, SDK v1.16,
`enable-file-upload`, no storage config), carries the exact expected card text, pre-warns
about the grey tile, the two dividers and the silent decision turn, flags Scenario D as
unverified live, and leaves the device/policy-pair callout intact.
  </done>
</task>

</tasks>

<verification>
- No CES API call, deployment change or agent edit was made. `git status` shows changes only
  under `.planning/`.
- Every dollar figure, claim number, version ID and quoted agent line in the four documents
  traces to `<observed_evidence>` above.
- Search the four files for overstatement: no document says `cover_offer_actions` works, that
  the contradiction path is verified live, or that the agent speaks the decision.
- The record of how the rendering bug was found and what was wrongly believed about it
  survives in both STATE.md and 05-02-SUMMARY.md.
</verification>

<success_criteria>
1. STATE.md's widget-rendering blocker reads VERIFIED with the observed card text as evidence
   and no "awaiting the user" wording anywhere in the file.
2. STATE.md's 260806-u21 caveat is narrowed to the contradiction path; the confirm path is
   recorded as verified live.
3. The Cloud Storage / file-upload concern is recorded as disproven, in STATE.md and the
   runbook.
4. 05-02-SUMMARY.md keeps `status: incomplete` with a written justification, and its open
   list shows exactly one of four items closed.
5. ROADMAP Phase 5's 05-02 line no longer claims rendering is unverified, stays `[~]`, and
   "Built so far" names chat v7 `bb14cdcc`. Criteria 2 and 3 untouched.
6. DEMO-RUNBOOK.md's chat section is runnable by a presenter with no prior context and warns
   about every cosmetic artifact and the silent decision turn.
</success_criteria>

<output>
Create `.planning/quick/260809-nt7-record-live-verification-of-decision-car/260809-nt7-SUMMARY.md`
when done. It must state, separately and explicitly: what was OBSERVED (the three closures),
what remains OPEN (the four items), and any place a document disagreed with this plan's
premises — notably the runbook's pre-existing chat section.
</output>
